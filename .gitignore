# Engine System generated cache (doctor pending notes, self-view snapshots) — regenerable, never authority
engine/.cache/

# subagent-driven-development scratch (task briefs, reports, review packages)
.superpowers/

# Downloaded/extracted engine snapshot + scratch outputs (not part of the deliverable)
engine1.zip
engine1_extracted/
engine2.zip
outputs/

# workbuddy scratch (agent memory doodles, not part of the deliverable)
.workbuddy/

# Claude Code local skill installation (source of truth: plugin/.claude/skills/)
.claude/skills/
