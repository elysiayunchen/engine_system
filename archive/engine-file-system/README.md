# Archived Engine File System Prompts

These files are historical snapshots only.

Use the project-root `ENGINE_FILE_SYSTEM_v5.md` as the single active web prompt entry point.
When Engine System initialization rules change, update that stable root file and archive older
snapshots here only when they need to remain available for reference.
