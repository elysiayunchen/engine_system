# CHANGE-2026-07-03-03 — v6 S2 Fractal Memory

> Engine System · 2026-07-03 · Task: T-002 · Decision: D-002

## Goal
落地 v6 S2「分形记忆」:把平坦记忆架构升级为分形分区——每任务上下文 = f(任务),不是 f(仓库),回答百万行(G3)。

## Actual Changes
- 联邦表 `engine/domains/federation.json`:path-glob → domain 路由表(机读,与 rules.json 风格一致)。
- 域引擎目录 `engine/domains/<domain>/{CONTEXT,PITFALLS}.md`:每域自带状态 + 陷阱 + 检索配方,无全局 500 行天花板。
- 路由 read-gate(Stop hook):code_path → 联邦表 → 所属域 ∈ 任务卡 domain 集合,越域 = block。无 federation.json / 无 domain → 回退 S1。
- L2 装配(SessionStart):按 active 任务卡 domain 注入对应域 CONTEXT+PITFALLS(各受预算约束)。
- 汇总协议:域仪表盘——SessionStart 提取每域 summary 拼成 O(域数) 仪表盘注入。
- 检索配方:域 PITFALLS 登记 rg recipe,归档不再等于遗忘。
- INIT 采访 BLOCK C 加 16a「项目分几大块」——产品分区 → federation.json。
- 契约 Fractal Memory Rule(v6 S2)。
- 自托管狗粮:本仓库铺设 2 域(engine-runtime + project-meta)补 D6 缺口。
- tests/fractal-memory/run-fractal-tests.sh:9 场景 sh/ps1 等价,21/21 PASS。

## Impact Scope
- 新文件类型 engine/domains/**(federation.json + 域 CONTEXT/PITFALLS)。
- Stop hook 第 1 层加路由一致性校验;SessionStart 加域仪表盘 + L2 装配段。
- 向后兼容:无 federation.json / 无任务卡 domain → 回退 S1/v5.6 行为。
- 分发:路由/装配/汇总逻辑内联进 hook,不新增独立可执行,install 清单不变。

## Risk & Watchpoints
- awk 解析 federation.json 依赖 gawk 三参数 match;非 gawk 环境 fail-open(跳过路由,回退 S1)。等价测试在 Git Bash(gawk)守门。
- 域划分来自产品采访;若架构师答不出「几大块」,默认单域 root,机制仍跑通。
- Doctor 不扫 domains/:联邦表段标题不带 §N,避免落入 plan 扫描区(§2-§3)。

## Verification
- tests/fractal-memory/run-fractal-tests.sh:21/21 PASS(A 联邦表解析 + B 路由门禁 6 场景 + C L2 装配/汇总 4 场景,sh/ps1 等价)。
- 修复两个真实 bug:① awk 解析 paths 同行闭合时 `/"paths"/` next 跳过 `]` 检测,in_paths 泄漏到 summary 行;② 测试第 2 层硬门禁假阳性(write_back 让 engine_written=1 暴露真路由校验)。
- scripts/check.sh 全绿(parity 19/19 + task-card 19/19 + fractal-memory 21/21 + 镜像校验)。

## Rollback
删 engine/domains/;回退 hook 脚本到 S1;回退契约 Fractal Memory Rule;回退 ENGINE_MAP 到 rev 7。无域时系统回退 S1 行为,无破坏性。

## Next Step
S3 契约编译:contract/src 拆分 + compile + dist 校验 + 减法规则。

## Responsibility Boundary
agent 实现 + 测试 + 自托管铺设;架构师批准 D-002 路线 + 域划分(engine-runtime / project-meta)。
