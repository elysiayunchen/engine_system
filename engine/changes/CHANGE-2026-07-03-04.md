# CHANGE-2026-07-03-04 — v6 S3 Contract Compile

> Engine System · 2026-07-03 · Task: T-003 · Decision: D-003

## Goal
落地 v6 S3 契约编译:把 2438 行契约单体拆成 src → compile → dist 编译模型 + 幂等校验 + 减法规则,拆掉复杂度棘轮。

## Actual Changes
- contract/src/ENGINE_FILE_SYSTEM.md:契约唯一真相源(从 dist 复制,2438 行)。
- contract/compile.sh + compile.ps1:编译器 = 头部横幅 + src → dist。幂等 compile(src) == dist。
- contract/budget.json:减法规则基线(max_lines=2438, max_rules=11)。
- ENGINE_FILE_SYSTEM_v5.md:头部加编译横幅(由 compile 产出,勿手改)。
- tests/contract-compile:6 场景(幂等 + 篡改检测 + 行数预算 + Rule 数预算 + sh/ps1 一致),6/6 PASS。
- scripts/check.sh:接入编译校验段。
- contract/README.md:编译机制说明。

## Impact Scope
- 契约从「agent 背诵的散文」变成「机器编译产物」。dist 必须由 src 编译,手改 dist 被检出。
- 减法规则:src 行数 ≤ 基线,新增 Rule 须净零增长(删并等量旧散文)或提升基线(需决策)。
- compile 脚本是引擎仓库工具,不分发(契约是引擎产品本身)。
- 向后兼容:dist 内容除头部横幅外不变;契约语义零变化。

## Risk & Watchpoints
- 本切片只建机制,未细分源模块(src 仍是单文件)。源模块细分留 S3-b——按主题(knowledge-classes / modes / rules / lifecycle)拆分 src。
- 减法规则用行数基线,是粗粒度指标;Rule 长度差异未计入。后续可加权。
- compile.ps1 横幅用 ASCII(避免 PowerShell 5.1 的 GBK 读 UTF8 乱码);em dash 在 ps1 字符串里不安全。

## Verification
- tests/contract-compile:6/6 PASS(A 幂等+篡改 3 + B 预算 2 + C sh/ps1 一致 1)。
- 修复:① Rule 计数 grep 误匹配——契约有 11 个 Rule(v5.2-v6 S2),非 4;② ps1 横幅 em dash 在 PS 5.1 乱码,改 ASCII。
- scripts/check.sh 全绿(parity + task-card + fractal-memory + contract-compile + 镜像)。

## Rollback
删 contract/;回退 dist 头部横幅(或重新 cp src→dist);回退 check.sh 编译段;回退 ENGINE_MAP。契约语义不变,无破坏性。

## Next Step
S3-b 源模块细分(按主题拆分 contract/src/ 为多模块) 或 S4 驾驶舱(status v2 + 决策队列 + engine verify 行为化验收)。

## Responsibility Boundary
agent 实现 + 测试 + 自托管;架构师批准 D-003 路线 + 减法基线(行数 2438 / Rule 11)。
