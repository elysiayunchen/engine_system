# CHANGE-2026-07-03-05 — v6 S4 Cockpit

> Engine System · 2026-07-03 · Task: T-004 · Decision: D-004

## Goal
落地 v6 S4 驾驶舱:engine verify T-NNN 行为化验收 + /engine-status v2,完成 G1 闭环——架构师判断行为而非代码,日常只看驾驶舱即可决策。

## Actual Changes
- engine/scripts/engine-verify.{sh,ps1}:行为化验收器——执行任务卡 AC verify 命令,PASS/FAIL+sha256 指纹写入 engine/evidence/T-NNN/AC-N.json。
- engine/bin/engine + .ps1:加 verify 子命令(调项目 engine-verify 脚本)。
- /engine-status v2:加「等你拍板」(决策队列) +「验收证据」段。
- 契约 Behavior Verification Rule(v6 S4):done 门 = verify 全绿 或 架构师豁免(豁免是决策)。
- contract/budget.json:基线提升至 2440/12(D-004 背书,纳入 Behavior Verification Rule)。
- tests/behavior-verify:10 场景 10/10(pass/fail/skip + JSON 格式 + CLI 子命令)。
- check.sh:接入 behavior-verify 段。

## Impact Scope
- AC verify 从声明式变机器执行;evidence 是 generated-cache 不登记 ENGINE_MAP。
- verify 用户主动跑(非 hook 自动),失败不阻断会话(写 evidence+非零 exit)。
- 向后兼容:无 verify 命令的 AC skip;无 evidence 时 status v2 提示「运行 engine verify」。

## Risk & Watchpoints
- verify 执行任务卡声明的命令(含 eval);架构师批准任务卡时即批准 verify。任意命令执行风险由任务卡批准门控制。
- evidence 的 fingerprint 是输出 sha256;非确定性命令(含时间戳)的 fingerprint 不稳定,但 status(pass/fail)仍可靠。
- PS verify 用 cmd /c 执行命令(支持 bash 命令在 Git Bash 环境)。

## Verification
- tests/behavior-verify:10/10 PASS(A pass/fail/skip 4 + B JSON 格式 3 + C 缺失 1 + D 全 pass 1 + E CLI 1)。
- scripts/check.sh 全绿(parity 19 + task-card 19 + fractal 21 + contract-compile 6 + behavior-verify 10)。
- 自托管:engine verify T-004 all pass(8 AC)。

## Rollback
删 engine-verify.{sh,ps1} + evidence/;回退 engine CLI verify 子命令;回退 status v2 两段;回退契约 Behavior Verification Rule;回退 budget 基线至 2438/11。无破坏性。

## Next Step
v6 路线 S0-S4 全部完成。后续:S3-b 源模块细分 / 真实大库试点 / 合并推送 main。

## Responsibility Boundary
agent 实现 + 测试 + 自托管;架构师批准 D-004 路线 + budget 基线提升(2440/12)。
