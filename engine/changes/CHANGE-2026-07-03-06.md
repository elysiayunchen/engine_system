# CHANGE-2026-07-03-06 — v6 体系完善批次1：分发完整性 + Doctor 编译检查

> Engine System · 2026-07-03 · Task: T-005 · Decision: D-005

## Goal
补齐 S3/S4 的机制闭环硬缺口:engine-verify 入分发清单(install/manifest),Doctor 检查 contract 编译幂等 + engine-verify bundled。

## Actual Changes
- install.sh/ps1 + manifest.json:加 engine-verify.{sh,ps1} 到分发清单。
- Doctor.sh/ps1:bundled maintenance script 清单 + required package files 清单加 engine-verify。
- Doctor.sh/ps1:加 check_contract_compile / Test-ContractCompile——检查 compile(src)==dist(幂等)+ src 行数 ≤ budget.max_lines(减法规则)。无 contract/ 时 fail-open。
- Doctor 镜像同步。

## Impact Scope
- 用户 install 后即得 engine verify 能力(S4 遗漏补齐)。
- dist 被手改时 Doctor 检出(S3 的 Doctor 缺口补齐)。
- 减法规则由 Doctor + check 双重守门。
- 双实现 sh/ps1 等价。

## Risk & Watchpoints
- Doctor 编译检查 fail-open(无 contract/ 时跳过,兼容旧项目)。
- Doctor.ps1 的 Test-ContractCompile 用 -ceq(大小写敏感)对比 dist;compile.ps1 用 WriteAllText UTF8NoBom 与 .sh 一致,编码不会误判。

## Verification
- Doctor: contract compile idempotent + budget + engine-verify bundled 全 PASS。
- scripts/check.sh 全绿(parity 19 + task-card 19 + fractal 21 + contract-compile 6 + behavior-verify 10)。
- Engine Doctor 0 failure 0 warning(双实现)。

## Rollback
回退 install/manifest 的 engine-verify 行;回退 Doctor 的 engine-verify 清单 + check_contract_compile 函数。无破坏性。

## Next Step
批次2:S3-b 源模块细分 / D6 狗粮缺口(SYSTEM/ARCHITECTURE/锚点/pre-commit 安装)。

## Responsibility Boundary
agent 实现 + 测试;架构师批准 D-005 路线。
