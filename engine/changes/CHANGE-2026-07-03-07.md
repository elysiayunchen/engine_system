# CHANGE-2026-07-03-07 — v6 体系完善批次2a：pre-commit 认 done + 安装

> Engine System · 2026-07-03 · Task: T-006 · Decision: D-006

## Goal
补齐 B 层门禁最大缺口:.git/hooks/pre-commit 未装(门禁形同虚设)+ pre-commit 只认 active 任务卡(done 后提交被拦)。

## Actual Changes
- pre-commit 加 done fallback:无 active 任务卡时,用最新 done 任务卡的 decision(scope 仍校验)。
- .git/hooks/pre-commit 安装(本仓库 dogfood)。
- tests/task-card 加 C6/C7:done fallback 通过 + scope 不覆盖拦截。

## Impact Scope
- B 层门禁从「形同虚设」变「真正生效」(本仓库 git commit 现触发 pre-commit)。
- done 后提交流程不再被拦(正常工作流)。
- fallback 仍校验 scope(不覆盖则拦),保持 N2 决策可追溯。

## Risk & Watchpoints
- fallback 用最新 done 任务卡的 decision;若新提交改受保护路径但 done 决策 scope 不覆盖,仍拦(需新决策)。
- pre-commit 是 POSIX sh,B 层跨平台用 git 自带 sh,无 ps1 孪生。
- 安装不覆盖已有 hook(install.sh 逻辑保护用户自定义)。

## Verification
- tests/task-card:21/21(含 C6 done fallback 通过 + C7 scope 不覆盖拦截)。
- scripts/check.sh 全绿。
- 本仓库 .git/hooks/pre-commit 已装(chmod +x)。

## Rollback
回退 pre-commit fallback 逻辑;删 .git/hooks/pre-commit。无破坏性。

## Next Step
批次2b:D6 狗粮(SYSTEM/ARCHITECTURE/锚点)/ 批次3:S3-b 源模块细分。

## Responsibility Boundary
agent 实现 + 测试 + 安装;架构师批准 D-006 路线。
