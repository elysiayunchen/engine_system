# CHANGE-2026-07-03-08 — v6 体系完善批次3：S3-b 源模块细分

> Engine System · 2026-07-03 · Task: T-007 · Decision: D-007

## Goal
S3-b:把 contract/src/ 2440 行单文件拆为 4 主题模块,compile 拼接多模块 → dist,减法规则可按模块预算。

## Actual Changes
- contract/src/ 拆为 4 模块:00-core(220行 核心规则) / 10-interview(458行 采访+PHASE) / 20-file-templates(1558行 文件模板) / 30-operational(204行 运维+SPEC TWIN)。
- compile.sh/ps1:改为拼接多模块(按文件名排序) → dist。横幅更新为 "compiled from contract/src/*.md"。
- Doctor.sh/ps1:check_contract_compile 改为多模块拼接对比 + 多模块行数求和。
- tests/contract-compile:compile_to + src_lines + rule_count 改为多模块。
- contract/README.md:多模块说明。

## Impact Scope
- 契约源从单文件变多模块,减法规则可按模块预算(新增 Rule 在其模块内净零增长)。
- dist 内容零变化(幂等保证:拼接 == 原 src)。
- 修复 ps1 的 Get-ChildItem -Filter 字符类通配不工作(改 Where-Object 正则)。

## Risk & Watchpoints
- 模块顺序靠文件名前缀(00/10/20/30)保证;新增模块用数字前缀。
- ps1 的 -Filter 不支持 0-9 字符类通配,用 Where-Object -match 替代。

## Verification
- tests/contract-compile:6/6(含 C1 sh/ps1 多模块拼接一致)。
- scripts/check.sh 全绿。
- dist 2441 行(横幅+2440),内容零变化。

## Rollback
合并 4 模块回单文件;回退 compile/Doctor 的多模块逻辑。dist 不变。

## Next Step
批次2b:D6 狗粮(SYSTEM/ARCHITECTURE/根锚点)。

## Responsibility Boundary
agent 实现 + 测试;架构师批准 D-007 路线。
