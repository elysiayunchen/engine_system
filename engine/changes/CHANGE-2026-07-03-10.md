# CHANGE-2026-07-03-10 — v6 体系完善批次4：契约债计数器（N4）

> Engine System · 2026-07-03 · Task: T-009 · Decision: D-009

## Goal
落地 N4 契约债计数器——Doctor 报告总 MUST + 门禁 Rule + 契约债(差值) + 基线趋势,让倒立金字塔变成可追踪下降的数字。

## Actual Changes
- Doctor.sh/ps1 加 check_contract_debt / Test-ContractDebt:报告 MUST 数 + 门禁 Rule 数 + debt(MUST-Rule) + 与 budget.debt_baseline 比。
- contract/budget.json 加 debt_baseline=46(当前 MUST 58 - Rule 12 = 46)。
- 双实现等价(sh grep -ho \bMUST\b + ps1 [regex]::Matches,都 58)。

## Impact Scope
- N4 完成:契约债可追踪下降。目标是 debt 递减(把散文 MUST 移进数据表:Rules/rules.json/federation.json)。
- Doctor 报告"contract debt: MUST=58, gated Rules=12, debt=46, baseline=46 - net-zero holding"。
- debt > baseline 时 Doctor warn(推动门禁化)。

## Risk & Watchpoints
- 契约债 = MUST - Rule(粗略代理:不是所有 MUST 都是门禁候选,但趋势可追踪)。
- \b 在 .NET vs grep 的 Unicode 边界差异:用 [regex]::Matches(Get-Content -Raw) 替代 Select-String -AllMatches(后者 56 漏匹配,前者 58 与 sh 一致)。

## Verification
- Doctor 双实现:contract debt 46 <= baseline 46,net-zero holding(0 warning)。
- scripts/check.sh 全绿。

## Rollback
删 check_contract_debt + budget.debt_baseline。无破坏性。

## Next Step
v6 体系完善全部完成(S0-S4 + 批次1-4)。N1-N5 全部达成。后续:真实大库试点 / 合并 main。

## Responsibility Boundary
agent 实现 + 测试;架构师批准 D-009 路线 + debt_baseline=46。
