# CHANGE-2026-07-03-11 — v6 review 缺口修复：dist 行尾 + N3 done-gate + dist 多产物

> Engine System · 2026-07-03 · Task: T-010 · Decision: D-010

## Goal
修复 3 子代理 review 暴露的 3 个高优先缺口:dist 行尾根因 + N3 done-gate 机器强制 + §5.5 dist 多产物。

## Actual Changes
- 缺口3 dist 行尾:.gitattributes 钉 ENGINE_FILE_SYSTEM_v5.md/runtime-law.md/rules.json/contract/src/*.md eol=lf;renormalize;00-core.md 删 S3-b 拆分残留旧横幅。Stop hook 行尾噪音根因消除。
- 缺口1 N3 done-gate:Doctor.sh/ps1 加 check_task_card_done_evidence/Test-TaskCardDoneEvidence——done 任务卡须有 engine/evidence/T-NNN/AC-*.json 或 exempt 标记,否则 fail。T-001..T-003/T-005..T-009 标 exempt(D-010 背书);T-004 有 evidence。
- 缺口2 dist 多产物:compile.sh/ps1 产出 3 个 dist——ENGINE_FILE_SYSTEM_v5.md(web-prompt)+ runtime-law.md(L0 宪法)+ rules.json(机读规则表聚合索引)。contract/src/L0-runtime-law.md(L0 宪法源)。

## Impact Scope
- N3 done-gate 机器强制(done 无 evidence 无 exempt → Doctor fail)。
- §5.5 dist 3 产物落地(web-prompt + runtime-law + rules.json)。
- dist 行尾稳定(二次 compile git status 干净)。
- L0 宪法源就绪。

## Risk & Watchpoints
- exempt 标记由 D-010 背书(v6 开发期任务卡,verify 机制 S4 后落地);后续任务卡须跑 engine verify 或显式 exempt(决策)。
- rules.json 是聚合索引(Doctor/hooks 直接读源文件);本文件信息性。
- runtime-law.md 是 L0 宪法源,SessionStart 注入待后续(当前注入 CONTEXT.md)。

## Verification
- scripts/check.sh 全绿(CHECK PASSED);Doctor 0 failure(双实现)。
- compile 3 dist 文件;二次 compile git status 干净(行尾稳定)。
- Doctor done-gate:T-001..T-009 exempt/evidence 全 pass。

## Rollback
回退 .gitattributes eol;删 check_task_card_done_evidence;compile 回单产物;删 L0-runtime-law.md。无破坏性。

## Next Step
v6 review 缺口修复完成。后续:中优先缺口(N1 注入行数计数器/L0 SessionStart 注入/flaky 调查)+ 低优先(v6 命名/决策记录/§7 度量)。

## Responsibility Boundary
agent 实现 + 测试;架构师批准 D-010 路线 + exempt 背书。
