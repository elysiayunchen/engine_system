# CHANGE-2026-07-03-12 — v6 中优先缺口：N1 注入计数器 + L0 注入 + flaky

> Engine System · 2026-07-03 · Task: T-011 · Decision: D-011

## Goal
修复 review 暴露的 3 中优先缺口:N1 注入行数≤400 机器强制 + L0 宪法 SessionStart 注入 + flaky 确认。

## Actual Changes
- N1 注入计数器:check.sh 加 "Session injection budget" 段——跑 session-start hook | wc -l,检查 ≤400(N1 机器强制)。本仓库 187 行。
- L0 SessionStart 注入:session-start.sh/ps1 加 L0 宪法注入(runtime-law.md 前 40 行,标题后 CONTEXT 前)。fail-open(无 runtime-law.md 跳过)。
- flaky:check 多次稳定(eol=lf 修复后,A1 不再 flaky)。

## Impact Scope
- N1 机器强制(注入 >400 → check fail)。
- L0 宪法注入 SessionStart(对抗漂移顶层锚点)。
- §5.1 L0 落地(runtime-law.md 注入)。

## Verification
- check.sh 全绿(CHECK PASSED);injection 187 <= 400 (N1)。
- L0 注入工作(grep "L0 宪法" 命中)。
- check 多次稳定(无 flaky)。

## Rollback
删 check.sh injection 段;删 session-start L0 注入。无破坏性。

## Next Step
v6 中优先缺口修复完成。后续:低优先(v6 命名/决策记录/§7 度量/Q2 试点)。

## Responsibility Boundary
agent 实现 + 测试;架构师批准 D-011 路线。
