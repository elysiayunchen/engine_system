# CHANGE-2026-07-04-01 — v6 验收缺口修复 + 外部 4 bug(D-015/T-014)

> Created: 2026-07-04 · Status: verified · Related plan: none · Decision: D-015 · Task: T-014

## Goal
对 v6 方向设计文档验收发现的两处高优先回归(init.md 契约漂移、16a 采访问题丢失)做根因修复,同时采纳外部项目 agent 报告的 4 个 bug(histexpand、VERSION 语义化、rules.json 契约不齐、迁移无版本化调度),让"单一真相源"和"自动更新"两条承诺重新为真。

## Actual Changes
- **init.md 纳入编译(G1 根因修复)**:`contract/compile.{sh,ps1}` 新增第 4 dist——`plugin/.claude/commands/engine-init.md` = 编译横幅 + `contract/src/cli-preamble.md`(新,CLI 执行前言,原 init.md 头 14 行)+ 同一契约 4 模块。旧 init.md(5.7.0 横幅、缺 v6 四 Rule)被编译产物替换;Doctor(sh/ps1)与 tests/contract-compile 幂等检查同步扩展(A4/A5 + C1 双实现比对含 init.md);`.gitattributes` 钉 eol=lf。
- **16a 回补(G2)**:「从产品角度,你的项目分几大块?」回补 `contract/src/10-interview.md`(S3 拆分时丢失,此前仅存于过期 init.md);随编译进入两个 dist;budget max_lines 2439→2441(D-015 背书,回补非新增)。
- **histexpand 修复(Bug1+2 因果链)**:`engine-migrate-contract.sh` 与 `engine-sync-agent-anchors.sh`(双树 ×4)MARK 赋值改单引号 + `set +H`——交互式 bash 中双引号内 `!-` 触发历史展开→赋值丢弃→`set -u` unbound。
- **VERSION 语义化(Q2)**:根/plugin/engine 三处 `6.0`→`6.0.0`,契约头同步;`engine-check-update.{sh,ps1}` 与 session-start 提示(双树)加 normalize_version 归一化比较(6.0≡6.0.0,防伪更新提示;非数字版本退回原样比较);migrator 版本回退值 6.0.0。
- **rules.json 契约对齐(Q3)**:migrator(sh/ps1 双树)创建的基线由 `{"rules":[]}` 改为 `{"rules":[],"protected_paths":[]}`——protected_paths 是 pre-commit 读取的键。
- **迁移版本化调度(Q5)**:`bin/engine{,.ps1}`(双树)run_migrate 重写:发现 `engine/migrations/v*` → 按归一化版本列排序(修正文件名 sort -V 会把 v6.0.sh 排在 v6.0.1.sh 之后导致低版本步被跳过的缺陷)→ 仅应用新于本地 engine/VERSION 的步 → 每步成功后回写 VERSION(ps1 侧无 BOM 写入,防击穿 sh 读取)→ 无待应用步回退幂等契约修复(保留 `engine migrate` 修复语义)。
- **测试与门禁**:新增 `tests/update-flow/run-update-tests.sh`(U1-U6:归一化判等/真更新/步序/回写/回退修复/rules 基线/histexpand 静态回归)接入 `scripts/check.sh`。
- **文档收尾(G6)**:设计文档 Status→absorbed;ENGINE_MAP rev 20 + N5 诚实标注(完整 init/Q2 试点未完成、G4/G5 契约债);域 PITFALLS 记 P006。

## Impact Scope
引擎产品本身(契约编译、更新链路、迁移器、CLI shim)+ 分发副本(plugin 双树)+ 测试与门禁。用户项目记忆不受影响;插件用户下次更新将拿到与 Web 契约一致的 v6 init 命令。

## Risk & Watchpoints
- 存量 6.0 用户因归一化不再收到 6.0.0 伪更新提示——**若要推送本批修复须发 6.0.1**(已在 CONTEXT 进行中列出,由架构师决定)。
- T-013 的 AC-1 钉死 `VERSION = "6.0"` 属历史证据快照,若重跑 `engine verify T-013` 该条会 FAIL——不改写历史,现行断言由 T-014 AC-4 接管。
- `engine migrate` 语义变化:先走版本化步,无待应用步才直跑迁移器;行为对老项目等价(v6.0 步本身就委托迁移器),U3/U4 有回归。

## Verification
| Check | Result | Evidence |
|-------|--------|----------|
| tests/update-flow | 7/7 PASS | U1-U6(本轮实跑) |
| tests/contract-compile | 8/8 PASS | A1-A5/B1-B2/C1(含 init.md 幂等与 sh/ps1 比对) |
| engine verify T-014 | 全绿 | engine/evidence/T-014/AC-*.json |
| scripts/check.sh | CHECK PASSED | 含新 Update flow 步 + Doctor init.md 幂等 |

## Rollback
`git revert` 本提交即可整体回滚;或单独回滚:恢复 compile.{sh,ps1} 三产物版 + 恢复旧 init.md;VERSION 三处回 6.0;migrator/bin 恢复上一版。无数据迁移,无不可逆操作。

## Next Step
合并 feature/v6-auto-update → Q2 试点库拍板 →(可选)发 6.0.1 推送修复到存量安装。

## Responsibility Boundary
- AI checked:编译幂等(4 dist)、双树字节一致、sh/ps1 判定一致、调度顺序与回写、门禁全绿。
- Architect should decide:是否发 6.0.1;Q2 试点库选谁;G4/G5 契约债何时清偿。
