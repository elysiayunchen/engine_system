# CHANGE-2026-07-04-02 — Doctor 占位符检测修复:剔除代码块与内联代码(D-016/T-015)

> Created: 2026-07-04 · Status: verified · Related plan: none · Decision: D-016 · Task: T-015

## Goal
D-015 的 CHANGE 胶囊(CHANGE-2026-07-04-01.md)在 Actual Changes 段用内联代码 `{"rules":[]}` 记录迁移基线变更,触发 Doctor check_change_capsule_semantics 的占位符正则 `\[.*\]` 误报 "still contains placeholders"。修复:检测前先剔除代码块与内联代码,使正则只扫描正文叙述,精准命中真占位符([TBD]/[items])且不误伤合法 JSON/markdown。

## Actual Changes
- **正则修复(双树 ×2 平台)**:`engine-doctor.sh` 第 375 行与 `engine-doctor.ps1` 第 442 行(及 plugin 副本,共 4 文件)由直接 `grep '\[.*\]|TBD|TODO' $latest` 改为先剔除代码块(```...``` )与内联代码(`...` )再检测——sh 侧 `sed -e '/^```/,/^```/d' -e 's/`[^`]*`//g'` 后 grep,ps1 侧 `$content -replace '(?s)```.*?```','' -replace '`[^`\r\n]*`',''` 后 -match。正则模式 `\[.*\]|TBD|TODO` 本身不变。
- **回归测试**:新增 `tests/doctor-parity/run-doctor-tests.sh`(D1 内联 JSON [] 不误报 / D2 代码块 JSON [] 不误报 / D3 真 [TBD] 仍检出 / D4 sh-ps1 一致),接入 `scripts/check.sh` 的 "Doctor placeholder parity (D-016)" 步骤。

## Impact Scope
引擎 Doctor 自检逻辑(sh/ps1 双树)+ 分发副本(plugin)+ 测试与门禁。CHANGE 胶囊、CONTEXT、HANDOFF 等引擎记忆文件内容不受影响。不改变用户可见的更新行为(不 bump VERSION)。

## Risk & Watchpoints
- 剔除代码块依赖 markdown 代码围栏配对;不闭合的 ``` 会使 sed ` /^```/,/^```/d ` 删到文件尾——但不闭合代码块本身是 markdown 错误,可接受。
- 内联代码 sh 侧 `sed 's/`[^`]*`//g'` 逐行处理天然不跨行;ps1 侧用 `[^`\r\n]` 显式限制不跨行,行为对齐。
- 这是 D-015 补丁,并入 PR #8(分支 feature/v6-auto-update),不单独发版;VERSION 保持 6.0.1。

## Verification
| Check | Result | Evidence |
|-------|--------|----------|
| tests/doctor-parity | 4/4 PASS | D1-D4(本轮实跑) |
| 双树 drift | sh/ps1 SHA256 一致 | check.ps1 Bundled duplicate drift |
| 真实胶囊无误报 | doctor 不再报 CHANGE-2026-07-04-01.md placeholders | grep -c = 0 |
| scripts/check.sh | CHECK PASSED | 含新 Doctor placeholder parity 步 |

## Rollback
`git revert` 本提交即可;或手动恢复 4 个 doctor 文件的第 375/442 行为单行 `grep '\[.*\]|TBD|TODO'`/`$content -match`,并移除 tests/doctor-parity + check.sh 的接入步。无数据迁移,无不可逆操作。

## Next Step
并入 PR #8 → 合 main → Q2 试点库拍板。

## Responsibility Boundary
- AI checked:双树字节一致、sh/ps1 行为一致、真 [TBD] 仍检出、JSON [] 不误报、check.sh 全绿。
- Architect should decide:PR #8 合并时机;Q2 试点库选谁;G4/G5 契约债何时清偿。
