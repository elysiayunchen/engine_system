# CHANGE-2026-07-05-05 — T-019 Phase 1 核心设计(D-018)+ init prompt 通用化分发 + engine init

## Goal

让 init 采访 prompt 不再是 Claude Code 专属:任何 AI agent(Codex/Copilot/Cursor/web)在装过引擎的项目里都有约定取用点 `engine/prompts/init.md`,终端也有 agent 无关入口 `engine init`。同时落 Phase 1 的整体实施设计(D-018)与三张任务卡,启动 D-017 路线的 Phase 1「通用化核心」。

## Actual Changes

- 新增 `contract/src/agent-preamble.md`(agent 中立前言);`contract/compile.{sh,ps1}` 增加第 5 个 dist:`engine/prompts/init.md`(正本)+ `plugin/engine/prompts/init.md`(镜像)——与 web 版、slash 版同源编译,prompt 不分叉
- engine CLI(`engine/bin/engine` + `engine.ps1` 孪生)新增 `init` 子命令:默认打印指引(prompt 路径 + 各 agent 用法示例 + 已初始化提醒),`--print` 输出原文供管道;**零写入**,真正生成引擎文件的是拿到 prompt 的 agent
- compile 新增 `plugin/bin/` 同步步(engine/bin 为唯一真相源)——修复既有的「bin 无同步机制」隐患
- `plugin/manifest.json` + `install.sh` + `install.ps1` 各增 `engine/prompts/init.md` 分发条目(protect=true)与 `engine/prompts` 目录创建
- 新增决策 `engine/decisions/D-018.md`(proposed,五项子决策 a–e + 任务拆分)+ 任务卡 T-019(done)/ T-020(paused,agent 检测器)/ T-021(paused,安装骨架)
- 顺带提交:`plugin/engine/scripts/` 下 4 个 ps1 副本追平(上一会话 compile 同步产物,与 engine/scripts 正本逐字节一致,非本次逻辑改动)

## Impact Scope

- 用户可见:`engine help`/`engine init` 新输出;安装/更新后项目内多一个 `engine/prompts/init.md`
- 结构性:compile 产物 4→5;`plugin/bin/` 从此由 compile 管理,不再依赖人工纪律
- 未动:契约正文(`contract/src/[0-9]*.md` 与 L0)一字未改;slash 命令 `engine-init.md` 内容不变(仍是全量编译产物);Claude Code 用户体验零变化

## Risk & Watchpoints

- fresh 模式重跑 install 于已装项目时,`engine/prompts/init.md` 命中「engine/*.md 已存在则跳过」规则不刷新;canonical 刷新路径是 `engine update`(update 模式 protect=true 必覆盖)。风险低,记录在案
- D-018a 修正了 D-017a「薄壳引用」的原措辞(编译同源已消除分叉,薄壳只剩间接层风险),需架构师认可该口径修正
- 指引里 codex/copilot 的调用旗标是文案示例而非机器契约,未逐一实测;T-020 检测器落地时校准

## Verification

- `engine verify T-019`:**9/9 PASS**(evidence: `engine/evidence/T-019/AC-*.json`,含 sha256 指纹)
- AC-9 = `bash scripts/check.sh` 14 步全绿(Doctor×2、脚本语法、hook parity、task-card/fractal/contract/behavior/update 五套 fixtures、注入预算、VERSION 一致、web-prompt 单入口、plugin 脚本漂移、manifest 覆盖)
- 冒烟:bash 侧(指引/--print/help)与 PowerShell 侧(指引/--print 位置参数/-Print 开关)六种调用形态人工验证输出正确

## Rollback

- `git revert` 本提交即可整体回滚;无数据迁移、无远端状态变化
- 若仅否决 D-018a:删除 `contract/src/agent-preamble.md` 后重跑 compile(agent-preamble 不存在时第 5 dist 自动跳过),再删两处 prompts 目录与三处分发条目即可

## Next Step

- T-020:agent 检测器孪生脚本 + init/installer 指引个性化(依赖本卡的 init 命令)
- T-021:快速安装骨架三模板(与 T-020 无依赖,可并行)
- 架构师:批 D-018(proposed → approved)+ 拍板 Q2 试点库

## Responsibility Boundary

- 本次由 AI 会话按「核心设计与验收」指令自主完成设计与实施;D-018 保持 proposed(决策批准权在架构师),实施本身落在 D-017(approved)的 Phase 1 范围内,提交门禁引用 D-017
- 架构师保留:D-018 批准/否决权(含 D-018a 口径修正)、Q2 试点库选择权;若否决,回滚路径见上,无沉没成本锁定
