# CHANGE-2026-07-06-02 — T-022 P0 防护卡 + 分发盲区修复

## Goal

D-019a 防护前置批:补齐编译产物漂移检测、protected_paths、manifest 校验、engine-verify 孪生四个防护面(7-05 评估发现 1/2 的整改)。同时修复架构师发现的分发盲区:用户项目 install 后核心数据文件缺失、hooks 全部 fail-open、引擎不可见。

## Actual Changes

### P0 防护前置(四任务)
- **Task 1 dist 漂移全覆盖**(`67d08e6`):新建 tests/dist-drift 套件(A 幂等/B 镜像/C 篡改/D sh-ps1 parity,9/9);compile.sh/ps1 加 ENGINE_COMPILE_OUT 环境变量(沙箱重算零逻辑二份);check.sh 删旧手写 17 文件块改由 B2 动态枚举;check.ps1 补 engine-verify/engine-check-update 两对漂移配对。**上岗第一天抓到真 bug**:compile.ps1 缺 UTF-8 BOM → PS 5.1 中文乱码写进 rules.json。
- **Task 2 protected_paths 补齐**(`6237c0e`):rules.json 加 `contract/src/**` + `runtime-law.md`;migrator sh/ps1 基线加 `runtime-law.md`(不加 contract/src/**,用户项目无此目录);tests/task-card 加 C8/C9 沙箱用例(真实验证 pre-commit 门行为,23/23)。
- **Task 3 manifest sha256 去空转**(`9d1f22c`):manifest 40→43 条全量真哈希(零 placeholder);compile.sh/ps1 加第 8 步回填(sed 清旧值 + sha256sum 算 + 插入,单行格式保持);install.sh/ps1 verify_checksums 硬失败(`return 1`/`throw`,修了 subshell 管道 bug `grep|while`→`while < <(grep)`);check.sh 加哈希门(缺键/占位/不匹配 fail);.gitattributes 13 条精确 eol=lf 规则;release.sh 加 manifest.json;tests/install-flow TDD(I1 正路/I2 篡改硬失败/I3 兼容,3/3,RED→GREEN)。
- **Task 4 engine-verify 孪生修复**(`68633d9`):三个 bug——ps1 param 位置(从未成功启动)/ps1 Git Bash 探测(优先 `C:\Program Files\Git\bin\bash.exe`,排除 System32 WSL 桩)/sh rc=0 复位(AC 失败后后续通过 AC 被误记 fail);审计发现 engine-check-update.ps1 同类 param 回归一并修;tests/behavior-verify 加 F1「先 fail 后 pass」用例 + PS_BIN 探测双侧断言;TDD RED→GREEN。

### 分发盲区修复(架构师最高优先级)
- **skeleton 骨架**(`38dbe2d`):plugin/engine/skeleton/{ENGINE_MAP,CONTEXT,HANDOFF}.md 三个自描述空模板,manifest 加 3 条(protect=false)→ 用户项目 install 后即见结构 + 引导跑 engine init。
- **install 自动 migrator**(`38dbe2d`):install.sh/ps1 末尾自动调用 engine-migrate-contract → 创建 tasks/decisions/domains/changes/evidence 目录 + federation.json + rules.json(fail-open)。
- **引导提示**(`38dbe2d`):install 末尾打印 Claude Code / 其他 agent / 终端三路引导。
- **manifest skeleton 条目**(`7e8ad36`):43 条回填 sha256。
- **install.ps1 BOM**(`7c5d518`):install.ps1 加 UTF-8 BOM(PS 5.1 无 BOM 按 GBK 解码中文 → 语法错误);install.sh/ps1 内嵌文件列表同步加 skeleton 3 条。

## Impact Scope

- **用户可见**:install 后用户项目获得骨架模板 + 目录结构 + 引导提示,引擎不再不可见。hooks 不再全部 fail-open。
- **防护面**:dist 漂移检测从部分覆盖到全量;manifest 校验从 100% 空转到硬失败;protected_paths 覆盖 L0 宪法 + 契约源;engine-verify.ps1 从从未启动到可用。
- **未动**:契约正文(contract/src/[0-9]*.md 与 L0)一字未改;slash 命令内容不变;VERSION 不变。

## Risk & Watchpoints

- **renormalize 归一化**:Task 3 的 `git add --renormalize .` 归一化 54 个文件(CRLF→LF),commit 体积大但都是行尾统一,不影响实质逻辑。3 个 .ps1(engine-doctor/install/engine-migrate-contract)仍有同款 BOM 潜在风险(Task 1 报告 flagged),无字节级断言覆盖,供后续参考。
- **旧项目 rules.json 不自动补 runtime-law.md**:migrator 只在 `if ! -f` 时创建,已有 rules.json 的旧项目不会自动获得 runtime-law.md 保护(Task 2 降级条款,报告已说明)。
- **check.sh 完整跑约 2-3 分钟**:PowerShell 嵌套启动慢,环境问题非代码缺陷。各套件单独跑均秒级全绿。
- **contract-compile 套件与 manifest**:跑 check.sh 时 contract-compile 套件会跑 compile 到沙箱(ENGINE_COMPILE_OUT),不影响真实 manifest。但建议后续观察 manifest 条目是否稳定。

## Verification

- `engine verify T-022`:**6 pass, 0 fail, 0 skip**(evidence: `engine/evidence/T-022/AC-*.json`,含 sha256 指纹)
  - AC-1 dist-drift 9/9 PASS | AC-2 protected_paths PASS | AC-3 manifest 43 sha256=43 src 零 placeholder PASS
  - AC-4 install-flow 3/3 PASS | AC-5 behavior-verify 含 F1 双侧 PASS | AC-6 check.sh CHECK PASSED
- `bash scripts/check.sh`:CHECK PASSED(exit 0,43 条 sha256 全验证)

## Rollback

- `git revert` 7 个 commit(67d08e6..7c5d518)即可整体回滚;无数据迁移、无远端状态变化。
- 分发盲区修复(skeleton/migrator)与 P0 防护可独立回滚——revert 38dbe2d + 7e8ad36 + 7c5d518 只回滚分发盲区,保留 P0 防护。

## Next Step

- 架构师:T-022 done 后,D-019 P0 完成;P1(技能+路由)可排队,但架构师已指示分发盲区是最高优先级——T-021(骨架,已随分发盲区修复落地)和 T-020(agent 检测器)的剩余部分待定。
- D-018 待批;Q2 试点库待拍板(现在引擎可见可用,试点条件成熟)。

## Responsibility Boundary

- 本次由 AI 会话按 D-019(approved)+ 架构师「优先解决分发盲区」指令自主完成设计与实施。子代理驱动(SDD)4 任务 + 分发盲区修复 + install BOM 修复,每任务实施→复审闭环。
- 架构师保留:D-019 P1/P2 方向、Q2 试点库选择、旧项目迁移策略(rules.json 不自动补)的最终决定权。
