# CHANGE-2026-07-12-01 — T-023 行为技能层 + 发布可用性准则

## Goal

落实 D-019 P1:把 Engine System 的“该怎么做”做成可分发的行为技能层,同时保证它不是本仓专用能力,而是能通过发布包安装到其他项目。

## Actual Changes

- 新增 5 个行为技能源:`contract/src/behaviors/{task-run,scout,verify-writeback,decision-draft,handoff}.md`。
- `contract/compile.{sh,ps1}` 新增 behavior dist:生成 `plugin/.claude/skills/engine-*/SKILL.md`、`engine/prompts/behaviors/*.md`、`plugin/engine/prompts/behaviors/*.md`。
- 新增 `engine/domains/routing.json`,记录触发条件、skill/prompt 路径和完成查点,并镜像到 `plugin/engine/domains/routing.json`。
- `install.sh` / `install.ps1` / `plugin/manifest.json` 分发 behaviors、skills 和 routing;安装器改为逐文件创建目标父目录,修复 nested skill 目录在外部项目安装失败的问题。
- `tests/behavior-skills/run-behavior-skills-tests.sh` 新增发布链路测试:源文件→dist→manifest/installers→隔离目录 `install.sh --local plugin`。
- `scripts/check.{sh,ps1}` 和 `tests/dist-drift` 纳入 behavior/routing 镜像检查。
- `engine/SYSTEM.md` 写入“发布可用性优先”项目开发准则;Doctor 不承载、不指向、不检查该准则。
- 修复 Windows 发布门禁暴露的问题:`contract/compile.ps1` 用 .NET SHA256 避免 `Get-FileHash` 依赖;`engine-migrate-contract.ps1` 改 ASCII 输出避免 Windows PowerShell 5.1 无 BOM 解码失败;bash 测试在 WSL+Windows PowerShell 混合路径下诚实跳过 ps1 分支。

## Impact Scope

- Claude Code 安装后会获得原生 `.claude/skills/engine-*` 技能。
- Codex/Cursor/Copilot/web 等其他 agent 可读取同源 `engine/prompts/behaviors/*.md`。
- 新能力已走 manifest + installers + plugin 镜像,并通过隔离目录安装验证,不是只在本仓可用。
- 本次未改契约正文模块,未增加常驻契约正文,保持 N1 注入预算。

## Risk & Watchpoints

- bash/WSL 环境中无法可靠驱动 Windows PowerShell 操作 WSL `/tmp` 工作树;相关 ps1 分支在 bash harness 下 skip,Windows 原生检查由 `scripts/check.ps1` 覆盖。
- Doctor 仍报告既有 warning:最新旧 capsule `CHANGE-2026-07-06-02.md` 含占位提示;contract debt 47 > baseline 46。这是既有债务,不阻塞本任务。
- 行为技能目前是入口层,不直接驱动 agent 进程;P2 的任务胶囊/token 账本仍待实施。

## Verification

- `engine verify T-023`:5 pass,0 fail,0 skip;证据在 `engine/evidence/T-023/AC-*.json`。
- `bash tests/behavior-skills/run-behavior-skills-tests.sh`:PASS=34,FAIL=0,包含隔离目录 local install。
- `bash scripts/check.sh`:CHECK PASSED。
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/check.ps1 -Root .`:CHECK PASSED。

## Rollback

- 回滚本变更涉及的 behavior 源、compile 分发、routing、install/manifest、tests/check 回归即可;无数据迁移、无版本 bump。
- 若要只撤回 Claude Code skills,可保留 `engine/prompts/behaviors` 和 routing,删除 manifest/install 中 `.claude/skills/**` 条目并重跑 compile/check。

## Next Step

- D-019 P2:实现 `engine capsule T-NNN` 任务胶囊与 token 账本/HANDOFF 裁剪。
- 或先完成 T-020 agent 检测器,让 install/init 根据已安装 agent 给更准确指引。

## Responsibility Boundary

- 本次由 AI 会话按架构师“发布后必须在其他项目可用”的核心准则实施。
- 架构师保留 P2 编排深度、首批 skill 名称是否调整、真实大库 Q2 试点选择的最终决定权。
