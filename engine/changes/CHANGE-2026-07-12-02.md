# CHANGE-2026-07-12-02 — T-024 SYSTEM/Doctor 职责边界修正

## Goal

纠正 T-023 收尾中的职责边界错误:发布可用性是本项目开发准则,权威位置应只在 `engine/SYSTEM.md`,不是 Doctor。

## Actual Changes

- `engine/SYSTEM.md` 保留“发布可用性优先”项目开发准则。
- `engine/ENGINE_DOCTOR.md` 删除对该准则的承载、指向和检查。
- `plugin/engine/ENGINE_DOCTOR.md` 同步删除该准则/指向,恢复为纯健康检查契约。
- `engine/CONTEXT.md`、`engine/HANDOFF.md`、`CHANGE-2026-07-12-01.md` 修正文案,明确 Doctor 不承载、不指向、不检查该准则。
- `plugin/manifest.json` 重新回填哈希。

## Impact Scope

- SYSTEM 负责项目级开发准则。
- Doctor 只负责健康检查契约和执行入口说明。
- 不改变 T-023 行为技能、路由、安装器或发布链路实现。

## Risk & Watchpoints

- 无运行时风险;这是权威位置和文档职责边界修正。
- 仍保留既有 contract debt warning,不属于本次修正。

## Verification

- `engine verify T-024`:3 pass,0 fail,0 skip。
- `bash scripts/check.sh`:由 T-024 AC-3 覆盖 PASS。

## Rollback

- 如需回滚,恢复 Doctor 中被删除的说明并移除 T-024 记录即可;不涉及代码运行路径。

## Next Step

- 后续新增项目级准则必须写入 `engine/SYSTEM.md`;Doctor 只在确有独立健康检查需求时更新,且不得作为准则权威或准则索引。

## Responsibility Boundary

- 本次由 AI 会话根据架构师纠偏执行。架构师对项目准则权威位置保留最终裁定权。
