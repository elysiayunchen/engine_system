# CHANGE-2026-07-17-02 — v6.5.0 发布与远端更新链

> Created: 2026-07-17 · Status: published and verified · Related: T-030 / D-026

## Goal

把已经通过 T-028/T-029 验收的 Engine System 6.5.0 发布到 `main` 与 `v6.5.0` tag，使下游 `engine check-update` 能看到远端 6.5.0，`engine update` 能拉到对应发布内容。

## Actual Changes

- 发布版本保持根 `VERSION`、`engine/VERSION`、`plugin/VERSION` 三处为 6.5.0。
- 补充 v6.5.0 CHANGELOG，包含全路径任务门禁、并行 workstream 分片、低 token 重锚和 done evidence 门。
- 将 T-028/T-029、D-024/D-025、验收 evidence、契约源/编译产物、plugin 镜像、installer 与测试一起提交。
- 发布提交已推送 `main`，`v6.5.0` tag 指向 `e33a3d8`；`release.yml` 已生成 tar/zip/offline 包、checksums 和 Release notes。
- 修复 CI/Release 的旧安装模拟：不再用 sed 绑定下载函数实现，统一调用正式 `install.sh --local plugin`。
- 将 plugin/AGENTS 收缩为薄引导器；fresh install + managed contract 后为 44 行，低于 45 行硬上限。
- `check.ps1` 在 GitHub Actions 中为每个失败项输出 `::error` annotation，避免无日志权限时只能看到笼统 exit 1。
- 修复 Windows checkout 行尾分叉：同时固定 `engine/bin/engine.ps1` 与 `plugin/bin/engine.ps1` 为 LF，避免相同 blob checkout 后产生 hash drift。
- CI/Release 本地安装步骤在失败时把日志尾部写入公开 annotation，便于无 Actions 日志权限时继续定位。
- 修复跨平台 manifest 假失败：编译器、sh/PowerShell installer、仓库 check 与安装测试统一按 LF 规范化文本后计算 SHA256；行尾变化不再误报，内容篡改仍硬失败。
- 保证项目内 CLI 可执行：两个 engine shell shim 的 Git mode 设为 100755，install.sh 复制后再显式 chmod；I6 与 CI 同时断言。
- 修复 Release migrate E2E 的工作目录：安装到 sandbox 后从 sandbox 根调用 CLI，与 CI/真实下游一致；失败输出公开 annotation。
- 消除 `set -o pipefail` 与 `doctor | head -1` 的假失败：先完整捕获 Doctor 输出再展示首行；最小迁移 fixture 的 Doctor 结果明确为 smoke warning，不替代前置完整 health check。

## Impact Scope

影响 Engine System 发布仓库、GitHub Release、远端 raw `VERSION` 和下游 installer/update 拉取路径。`.workbuddy/`、`engine2.zip` 等用户 scratch 文件不进入提交。

## Risk & Watchpoints

- GitHub Actions Release 需要远端 tag push 后完成；若 workflow 失败，main/tag 仍可见但 GitHub Release 附件需重新运行 workflow。
- 下游 `engine update` 走 `main` 的 install.sh；`engine --version`/`check-update` 走 raw `VERSION`。两条路径必须同时指向 v6.5.0。
- 外部真实项目迁移尚未在本仓库内模拟；发布后应选一个下游项目执行 `engine check-update`、`engine update --check-only` 和 `engine migrate`。

## Verification

| Check | Result | Evidence |
|---|---|---|
| 根/engine/plugin 版本一致 | PASS | T-030 AC-1 |
| 完整发布门禁 | PASS | T-030 AC-2 |
| 本地 v6.5.0 tag 存在 | PASS | T-030 AC-3 |
| 胶囊章节完整 | PASS | T-030 AC-4 |
| workflow 正式本地安装 + fresh AGENTS ≤45 | PASS（fresh=44 行） | T-030 AC-5 |
| Release sandbox migrate + idempotency | PASS（从安装目标根执行；Doctor smoke 不再触发 pipefail） | 本地复现首轮 workflow 后重跑 |
| 跨平台 LF/CRLF manifest 验证 | PASS（LF 包 + PowerShell Local） | T-030 AC-2 / 本地跨平台 fixture |
| 远端 main CI | PASS（Linux / Windows / install-migrate） | 最终 run 29597273388 @ `e33a3d8` |
| 远端 raw VERSION | PASS（6.5.0） | GitHub Contents API / `v6.5.0` |
| 远端 main install.sh | PASS（SHA256 与本地一致） | `2A811708030BBCE403534A27A008B52B968180B85A9A4DD7BC4BE7A19F87C38B` |
| GitHub Release | PASS（public，4 assets uploaded） | Release 355822586 / workflow 29599793000 |
| Release 归档下载与 checksums | PASS（tar.gz / zip / offline.tar.gz 全部 OK） | 本地公开下载后 `sha256sum -c` |

## Rollback

若已发布附件出现传输或元数据问题，重新运行 Release workflow 或替换同名附件并复核 checksums；若产品内容需要改动，发布新的 patch 版本，不再移动已公开且验收完成的 `v6.5.0`。不回退 main 上已验收代码，下游仍可固定旧 tag 安装。

## Next Step

发布完成。下一步在一个真实下游项目执行 `engine check-update`、`engine update --check-only` 与 `engine migrate`，观察首次任务采用及并行 workstream 手感；该试点另开一张可独立验收任务卡。

## Responsibility Boundary

- Agent 负责：发布前检查、提交、tag、push、远端可见性与 workflow 状态核对。
- 发布者/架构师负责：确认 GitHub Release 附件完成、选择真实下游试点并处理凭据/权限问题。
