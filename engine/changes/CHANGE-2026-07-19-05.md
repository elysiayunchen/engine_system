# CHANGE-2026-07-19-05 — T-033 域级 INVENTORY.md 功能索引 + 双向 FAIL 检查 + v6.8.0 发布

> Created: 2026-07-19 · Status: needs-verification · Related plan: T-033 · Decision: D-028

## Goal
把"agent 接管大项目靠现读代码理解项目"做成机制:每域新增 `engine/domains/<domain>/INVENTORY.md`(≤120 行 5 列总览)+ 子文件分级,只写语义层(功能→入口文件→API 契约名),符号定位交给 ast-grep 现生;Doctor 双向 FAIL 检查(code↔INVENTORY)+ API 唯一性检查(机制 C);migrator 写入空表头 stub;dogfood 铺设 engine-runtime + project-meta 两域;发版 v6.8.0。

## Actual Changes
- **契约源** `contract/src/20-file-templates.md` 加 FILE 14:INVENTORY.md 5 列模板(Feature/Entry file/Public API/Status/Last verified)+ 分级规则(总览 ≤120 行,子文件 ≤200 行)+ 与 ast-grep/ctags 边界声明(语义层人写,符号层机器现生)+ 双向 FAIL 检查 + 机制 C API 唯一性 + 维护时机 + migrator stub + 骨架文件内容。
- **behaviors** `task-run.md` 加任务 done 时 MUST 更新涉及域 INVENTORY 规则;`verify-writeback.md` step 7 加 INVENTORY 同步检查(读 Doctor 输出,FAIL 须解决)。
- **ENGINE_DOCTOR.md** dogfood + plugin 镜像加 #13(双向 FAIL)+ #14(API 唯一性);contract-version 6.7.0→6.8.0。
- **engine-doctor.{sh,ps1}** ×4 实现 `check_inventory_bidirectional`(INVENTORY→code test -f + code→INVENTORY done 任务 WRITE-SET 检查)+ `check_inventory_api_uniqueness`(全仓 Public API 唯一性);迁移宽限期读 contract-version(<6.8.0 WARN, >=6.8.0 FAIL)。
- **engine-migrate-contract.{sh,ps1}** ×4 加 #13/#14 到 doctor_tmp heredoc + skeleton/domains/INVENTORY.md 模板创建 + 每域 INVENTORY.md stub 创建(create-if-missing)。
- **skeleton** `engine/skeleton/domains/INVENTORY.md` + `plugin/engine/skeleton/domains/INVENTORY.md` 字节对称模板。
- **dogfood** `engine/domains/engine-runtime/INVENTORY.md`(38 feature 行)+ `engine/domains/project-meta/INVENTORY.md`(15 feature 行)覆盖所有 done 任务 WRITE-SET 具体路径。
- **budget** `contract/budget.json` max_lines 2530→2630(D-028/T-033 背书,纳入 INVENTORY 模板+分级+双向检查+API 唯一性+migrator stub+骨架)。
- **bug 修复** engine-doctor.sh 两处 bash 正则 `^<!--` 在 `[[ =~ ]]` 内触发 syntax error(`<` 是 redirection 操作符)→ 改为 `== "<!--"*` glob;`write_set="$(grep|head|sed|tr|sed)"` 在 `set -euo pipefail` 下 grep 无匹配返回 1 触发 ERR trap → 加 `|| true`。

## Impact Scope
引擎运行时 + 契约源 + 迁移器 + 骨架模板 + dogfood 域产物。不引入 AST 解析器(符号定位交 ast-grep 现生);不替代 federation.json 域路由;INVENTORY 不进 SessionStart 全文注入。

## Risk & Watchpoints
- code→INVENTORY 检查只看 done 任务 WRITE-SET 的具体路径(非 glob),不覆盖 federation.json 全路径映射——这是 dogfood 简化,完整映射是 federation.json 的职责。
- INVENTORY API 唯一性检查依赖人写 API 契约名,若 agent 在 done 时漏填 Public API 列则检查无效——task-run.md 行为规则强制 done 时 MUST 更新。
- 迁移宽限期 contract-version < 6.8.0 时降级 WARN,旧项目迁移后需手动补 INVENTORY 行才会触发 FAIL——这是设计意图(渐进收紧)。

## Verification
| Check | Result | Evidence |
|-------|--------|----------|
| AC-1 契约源 INVENTORY.md 模板 | pass | contract/src/20-file-templates.md FILE 14 含 5 列模板 + ≤120 行 |
| AC-2 ast-grep/ctags 边界声明 | pass | FILE 14 含边界表(语义层人写/符号层机器现生) |
| AC-3 task-run.md done MUST 更新 | pass | behaviors/task-run.md 含 INVENTORY + done MUST |
| AC-4 verify-writeback step 7 | pass | behaviors/verify-writeback.md 含 INVENTORY 同步检查 |
| AC-5 ENGINE_DOCTOR #13 双向 FAIL | pass | engine + plugin ENGINE_DOCTOR.md 含 #13 |
| AC-6 check_inventory_bidirectional ×4 | pass | engine-doctor.{sh,ps1} ×4 含函数 + contract-version 判定 |
| AC-6.1 check_inventory_api_uniqueness ×4 | pass | engine-doctor.{sh,ps1} ×4 含函数 |
| AC-7 dogfood INVENTORY ≤120 行 | pass | engine-runtime 48 行 + project-meta 20 行,Entry file 路径存在 |
| AC-8 skeleton 模板 ×2 | pass | engine + plugin skeleton/domains/INVENTORY.md 字节对称 |
| AC-9 plugin 镜像同步 + check.sh | pass | compile.sh 20 scripts + 57 manifest + check.sh CHECK PASSED |
| AC-10 VERSION ×3 = 6.8.0 | pending | release.sh 6.8.0 待跑 |
| AC-11 check.sh 全绿 + push | pending | release.sh 6.8.0 待跑 |

## Rollback
移除 contract/src/20-file-templates.md FILE 14 + behaviors INVENTORY 段 + ENGINE_DOCTOR.md #13/#14 + engine-doctor.{sh,ps1} 两个函数 + migrator #13/#14 + skeleton/domains/ + dogfood INVENTORY.md + budget.json 回 2530。VERSION 回 6.7.0。

## Next Step
`bash scripts/release.sh 6.8.0` → `git push && git push --tags` → GitHub Release workflow → 开干 T-034/T-035。

## Responsibility Boundary
- AI checked: 契约源 + behaviors + ENGINE_DOCTOR + doctor 脚本 + migrator + skeleton + dogfood INVENTORY + budget + bug 修复 + check.sh 全绿。
- Architect should decide: code→INVENTORY 检查是否扩展到 federation.json 全路径映射(当前 dogfood 简化只看 WRITE-SET 具体路径)。
