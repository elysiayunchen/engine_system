# CHANGE-2026-07-20-01 — T-034: AC 级 checkpoint 机制 + 任务粒度软门禁 + v6.9.0

> Created: 2026-07-20 | Status: verified | Related plan: none | Task: T-034 | Decision: D-028

## Goal
把"任务进行中细节压缩丢失"从任务级粒度细化到 AC 级:每个 AC verify 通过后写 `engine/evidence/T-NNN/checkpoint.md`(已完成 AC 列表 + 关键中间状态 + 下一 AC 指针),SessionStart 重锚从 checkpoint 而非任务卡头开始;任务粒度 WARN 升级为软门禁(超阈值须拆卡或声明 checkpoint_plan 才能置 active);任务卡间 depends-on 显式依赖字段 + 上游未 done 时下游阻塞门禁;WRITE-SET 静态预算软门禁(D-028 §10 机制 A);发版 v6.9.0。

## Actual Changes
- **契约源** `contract/src/20-file-templates.md`:追加 FILE 15 checkpoint.md 模板(已完成 AC 列表 + 关键中间状态 + 下一 AC 指针 + 维护规则注释)。
- **engine-verify** `engine/scripts/engine-verify.{sh,ps1}` + plugin 镜像(4 份):在每个 AC verify PASS 后追加写 `engine/evidence/T-NNN/checkpoint.md`。
- **SessionStart hook** `engine/scripts/engine-hook-session-start.{sh,ps1}` + plugin 镜像(4 份):在 active 任务卡注入之后,progress.md 注入之前,添加 checkpoint.md 优先注入块。
- **任务卡 README** `engine/tasks/README.md` + plugin skeleton 镜像:添加 `estimated_steps` / `checkpoint_plan` / `depends-on` 三字段定义 + 任务粒度软门禁规则 + 跨域拆卡协调规则。
- **ENGINE_DOCTOR** `engine/ENGINE_DOCTOR.md` + plugin 镜像:contract-version 6.8.0→6.9.0 + #15 规则(任务粒度软门禁)+ #27 规则(plugin 镜像)。
- **Doctor** `engine/scripts/engine-doctor.{sh,ps1}` + plugin 镜像(4 份):新增 `check_writeset_budget` / `Test-WriteSetBudget`(WRITE-SET 字节数 > 30KB 软门禁)+ `check_task_granularity` / `Test-TaskGranularity`(AC>12 / paths>15 / steps>20 软门禁)+ `check_depends_on` / `Test-DependsOn`(depends-on 上游未 done 阻塞);迁移宽限期 contract-version < 6.9.0 时 FAIL→WARN,tryout bypass 同 D-028 §9。
- **Migrator** `engine/scripts/engine-migrate-contract.{sh,ps1}` + plugin 镜像(4 份):新增 v6.9.0 heredoc 块,确保 `engine/skeleton/checkpoint.md` 模板存在(idempotent,只创建不覆盖)。
- **Skeleton** `engine/skeleton/checkpoint.md` + plugin 镜像:新增骨架文件(AC 级压缩恢复锚点模板)。
- **版本三致** `VERSION` / `engine/VERSION` / `plugin/VERSION` = 6.9.0;`CHANGELOG.md` 追加 v6.9.0 段;`contract/budget.json` max_lines 2630→2730。
- **Manifest** `plugin/manifest.json`:更新 7 个修改文件的 SHA256(engine-doctor.sh/.ps1, engine-verify.sh/.ps1, engine-hook-session-start.sh/.ps1, engine/ENGINE_DOCTOR.md)。

## Impact Scope
引擎运行时(engine-runtime 域)+ 项目运营记忆(project-meta 域)。任务卡机制升级:AC 级 checkpoint 注入 + 软门禁强制 + depends-on 阻塞。旧项目迁移宽限期:contract-version < 6.9.0 时所有软门禁 FAIL 降级为 WARN。

## Risk & Watchpoints
- 软门禁阈值(AC>12 / paths>15 / steps>20 / bytes>30KB)基于 P1 子代理审视报告 + T-031 实证(8 AC 是合理发布单元)。若实际项目频繁触发,可调高阈值或声明 `checkpoint_plan: tryout`。
- depends-on 是硬阻塞(FAIL 级),跨域拆卡时须确保上游卡先 done。
- checkpoint.md 与 progress.md 写入职责分离:verify 脚本只写 checkpoint.md(AC 级),agent 只写 progress.md(任务级),无并发写冲突。

## Verification
| Check | Result | Evidence |
|-------|--------|----------|
| AC-1 契约源 checkpoint.md 模板 | PASS | T-034/AC-1.json |
| AC-2 engine-verify 追加写 checkpoint.md (4 份) | PASS | T-034/AC-2.json |
| AC-3 SessionStart 优先注入 checkpoint.md (4 份) | PASS | T-034/AC-3.json |
| AC-4 README 三字段定义 | PASS | T-034/AC-4.json |
| AC-5 ENGINE_DOCTOR 软门禁规则 | PASS | T-034/AC-5.json |
| AC-5.1 check_writeset_budget (4 份) | PASS | T-034/AC-5.1.json |
| AC-6 check_task_granularity + check_depends_on (4 份) | PASS | T-034/AC-6.json |
| AC-7 README 跨域拆卡协调规则 | PASS | T-034/AC-7.json |
| AC-8 plugin 镜像 + manifest SHA256 + check.sh | PASS | T-034/AC-8.json |
| AC-9 版本三处一致 6.9.0 + CHANGELOG | PASS | T-034/AC-9.json |
| AC-10 check.sh 全绿 | PASS | T-034/AC-10.json |

## Next Step
- v6.9.0 release commit + tag push 后,开 T-035(死代码检测 + v6.10.0)。
- T-035 WRITE-SET 已含 29 paths(触发软门禁,已声明 `checkpoint_plan: tryout` 走 bypass)。
- 跨域拆卡协调:后续若拆 T-035 为 T-035a(linter 委托)+ T-035b(反向调用点扫描),须在 T-035b 头部加 `depends-on: T-035a`。
- Architect 决策项:软门禁阈值是否需调整(参见 Responsibility Boundary);depends-on 是否扩展为传递性依赖。

## Rollback
回滚所有 4 份对称脚本的 checkpoint.md 相关代码块,删除 `check_writeset_budget` / `check_task_granularity` / `check_depends_on` 函数及调度调用,删除 `engine/skeleton/checkpoint.md` + plugin 镜像,恢复 ENGINE_DOCTOR.md contract-version 6.9.0→6.8.0,恢复 VERSION 三处 6.9.0→6.8.0,恢复 budget.json max_lines 2730→2630,恢复 manifest 旧 SHA256 值。

## Responsibility Boundary
- AI checked: 契约源模板 + 4 份对称脚本 + Doctor 函数 + migrator heredoc + skeleton + 版本三致 + CHANGELOG + budget + manifest SHA256 + check.sh 全绿。
- Architect should decide: 软门禁阈值是否需调整;depends-on 是否扩展为传递性依赖(当前仅直接依赖);checkpoint.md 是否需要签名/校验机制(当前信任 verify 脚本写入)。
