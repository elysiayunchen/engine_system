# CHANGE-2026-07-20-03 — T-037: 加 Trae continuity 开发准则到 engine/SYSTEM.md

> Created: 2026-07-20 | Status: verified | Related plan: T-037 | Decision: none

## Goal
把"Trae 相关开发 agent 工具永远不主动中止对话,用 AskUserQuestion 延续对话"作为开发准则加到 `engine/SYSTEM.md`「项目开发准则」段,使所有未来 agent 会话(本仓 + 安装到外部项目的 agent)默认遵守,避免浪费用户的发起对话额度。

## Actual Changes
- **engine/SYSTEM.md**「项目开发准则」段加 `### Trae agent 工具对话延续准则` 子段(在「### 发布可用性优先」段后):
  - 准则文本:运行在 Trae 相关开发 agent 工具(TRAE IDE / TRAE Work / TRAE CLI / TRAE Plugin)时,NEVER 主动中止对话,必须用 `AskUserQuestion` 延续
  - 触发条件:agent 工具属于 Trae 家族(IDE/Work/CLI/Plugin 任一)+ 本轮任务已完成或遇到决策点
  - 工具指定:`AskUserQuestion`(1-4 个问题,每问题 2-4 个选项)
  - 双轨说明:与 `user_profile.md` 的 Trae agent tool continuity 准则双轨(机器自动注入 + 项目级 system 显式声明)

## Impact Scope
项目级开发准则(project-meta 域)。影响所有未来 agent 会话的行为约束:Trae 相关 agent 工具下,每轮任务结束必须用 AskUserQuestion 延续对话,不主动中止。不进 contract 源(准则是项目级,不是契约级机制);不进 runtime-law.md(L0 全局宪法);不进 AGENTS.md / CLAUDE.md(anchor 引导器,不承载规则正文);不进 plugin 镜像(SYSTEM.md 不在 plugin 镜像范围)。

## Risk & Watchpoints
- **准则非机器强制**:本准则依赖 agent 自觉遵守,无 hook 检测。若未来需要机器强制(如 hook 检测对话末尾是否有 AskUserQuestion 调用),需另开决策卡。
- **范围边界**:准则只在 Trae 相关 agent 工具下生效;非 Trae 工具(如 Claude Code 原生、Codex CLI、Cursor 等)不受约束。这是设计意图——Trae 用户的发起对话额度是 Trae 工具特有的限制。
- **撤销路径**:准则不进 rules.json protected_paths,未来若需撤销,直接 Edit engine/SYSTEM.md 删除该子段即可,不需要决策卡覆盖。
- **与 user_profile.md 重叠**:user_profile.md 已有跨项目 Trae agent tool continuity 准则(机器自动注入到 system-reminder)。本任务在项目级 system 显式声明,使准则对所有 agent 会话(含安装到外部项目的 agent)默认可见。两者文本对齐,无冲突。

## Verification
| Check | Result | Evidence |
|-------|--------|----------|
| AC-1 engine/SYSTEM.md 加 Trae continuity 子段(grep Trae + AskUserQuestion + TRAE IDE/Work/CLI/Plugin + 延续/continuity/不主动中止) | pass | T-037/AC-1.json (fp=e3b0c44298fc) |

## Rollback
删除 `engine/SYSTEM.md`「项目开发准则」段下「### Trae agent 工具对话延续准则」子段(11 行)。无其他文件改动需要回滚。

## Next Step
- T-036 恢复 active,全修 review 发现的 P1-P7 问题。
- T-037 准则生效后,所有未来 agent 会话(本仓 + 安装到外部项目的 agent)默认遵守:Trae 相关 agent 工具下 NEVER 主动中止对话。

## Responsibility Boundary
- AI checked: engine/SYSTEM.md 加准则子段 + AC-1 verify PASS + T-037.md/spec.md 任务卡 + CHANGE capsule。
- Architect should decide: 准则是否需升级为机器强制(hook 检测);是否需扩展到非 Trae 工具(如所有 agent 工具)。
