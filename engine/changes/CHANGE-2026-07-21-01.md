# CHANGE-2026-07-21-01 — T-036 AC-18 端到端测试完成

> Created: 2026-07-21 | Status: verified | Related plan: T-036 | Decision: D-029

## Goal
T-036 AC-18 端到端测试创建:tests/workstream/ 下 8 个测试文件(4 bash + 4 PowerShell),覆盖 worker 模式触发 / 双信号 OR 关系 / lock 残留恢复 / kill switch 四个场景。完成 T-036 全 18 AC。

## Actual Changes
- **新建 8 个测试文件** `tests/workstream/`:
  - test_worker_mode.{sh,ps1}: 7 断言 ×2 = 14。W1 live lock → 新会话降级 worker + .role=worker 标志 + lock 不动;W2 无 lock → 成为 coordinator + 创建 lock
  - test_double_signal.{sh,ps1}: 4 断言 ×2 = 8。D1 信号 2 拦截共享写;D2 无信号 = 协调者放行;D3 worker 写自己分片放行;D4 worker 写兄弟分片拦截
  - test_lock_recovery.{sh,ps1}: 15 断言 ×2 = 30。L1 stale lock 自动恢复 + tombstone;L2 assume-coordinator 拒绝 live lock;L3 --force 覆盖 + tombstone reason=forced-replaced;L4 无 lock 时清理 stale tombstone
  - test_kill_switch.{sh,ps1}: 15 断言 ×2 = 30。K1 环境变量跳过;K2 标志文件跳过;K3-K4 disable on/off;K5-K7 status 命令
  - 合计 82 断言全 PASS(bash 41 + PowerShell 41)
- **修复 test_double_signal.sh D2/D3 失败**:
  - D2:WRITE-SET 加入 engine/CONTEXT.md + engine/HANDOFF.md,让 block_scope 不拦截协调者写共享记忆(workers 由 is_shared_memory 独立拦截)
  - D3:payload file_path 用 `s-top-main/HANDOFF.md` 匹配 session_key=safe_id("s-top-main") 算法(原 `worker-top/` 不匹配)
- **修复 checkpoint.md 慢性膨胀**:T-036/evidence/checkpoint.md 86 行 → 18 行(只保留最新一次 PASS 记录)。根因是 engine-verify.sh 每次 AC PASS 都 append 不 dedup,T-038 将处理。SessionStart 注入从 412 行降至 341 行,通过 N1 ≤400 门禁。
- **更新 evidence**: AC-14/AC-16/AC-18 从 fail → pass(check.sh CHECK PASSED + 8 测试文件全绿)
- **更新 HANDOFF.md**:立即恢复点改为"T-036 全 18 AC PASS"

## Impact Scope
T-036 v6.11.0 多会话锁机制验收完成。影响范围:tests/workstream/ 下 8 个新测试文件 + T-036 evidence 3 个 JSON 更新 + checkpoint.md 修剪 + HANDOFF.md 立即恢复点更新。无契约源改动,无 hook 代码改动,无 plugin 镜像改动。

## Risk & Watchpoints
- **engine-verify.sh append 不 dedup**:每次跑 `engine verify T-036` 都会向 checkpoint.md append 18 行(每 AC PASS 一行)。多次跑会重新触发 N1 超标。T-038 将修复(dedup by AC id,保留最新)。
- **PowerShell 测试需要 pwsh.exe**:bash 跑 `pwsh` 命令需要 PATH 含 `/mnt/c/Program Files/PowerShell/7`(Windows)。CI 环境(Linux)需要安装 PowerShell。本机测试已通过 `pwsh -NoProfile -File` 验证 4 个 .ps1 文件。
- **D3 测试依赖 session_key 算法**:session_id="s-top" 无 agent_id 时,session_key=safe_id("s-top-main")="s-top-main"。如果未来 safe_id 算法改(如不再追加 `-main`),D3 测试需要同步更新。

## Verification
| Check | Result | Evidence |
|-------|--------|----------|
| AC-18 tests/workstream/ 下 8 个测试文件存在 + 全部跑过 | pass | T-036/AC-18.json (fp=4bb15a034d01) |
| bash 41 断言全 PASS(test_worker_mode 7 + test_double_signal 4 + test_lock_recovery 15 + test_kill_switch 15) | pass | 本地跑 `bash tests/workstream/test_*.sh` 全 exit=0 |
| PowerShell 41 断言全 PASS | pass | 本地跑 `pwsh -NoProfile -File tests/workstream/test_*.ps1` 全 exit=0 |
| check.sh CHECK PASSED(session injection 341 lines ≤ 400 N1) | pass | T-036/AC-14.json + AC-16.json (fp=e3b0c44298fc) |
| engine verify T-036 18 pass / 0 fail | pass | (运行时 PATH 含 pwsh.exe 后,18/18 PASS) |

## Rollback
删除 8 个测试文件:tests/workstream/test_worker_mode.{sh,ps1} + test_double_signal.{sh,ps1} + test_lock_recovery.{sh,ps1} + test_kill_switch.{sh,ps1}。恢复 T-036/evidence/checkpoint.md 到膨胀版本(86 行)。恢复 AC-14/AC-16/AC-18 evidence JSON 为 fail。恢复 HANDOFF.md 立即恢复点为 "AC-1~AC-17 全部 verify PASS,下一步 AC-18"。

## Next Step
- `git push origin main`(当前 19 commits ahead)
- T-036 任务卡状态从 active → done
- 开 T-038:engine-verify.sh checkpoint.md append 不 dedup 修复(慢性膨胀根因)
- 后续任务:T-034 checkpoint.md 长任务断点(弱依赖)/ T-035 死代码检测(已 done v6.10.0)

## Responsibility Boundary
- AI checked: 8 个测试文件创建 + 82 断言全 PASS + D2/D3 修复 + checkpoint.md 修剪 + evidence JSON 更新 + HANDOFF 更新。
- Architect should decide: T-038 任务卡起草(checkpoint.md dedup 修复)是否纳入 v6.11.1 hotfix 或 v6.12.0 常规发版。
