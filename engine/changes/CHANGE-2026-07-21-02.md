# CHANGE-2026-07-21-02 — T-038 v6.11.1 patch: D-029 实现层 5 处遗漏修复

> Created: 2026-07-21 | Status: verified | Related plan: T-038 | Decision: D-029

## Goal
修复 D-029 决策卡要求但 T-036 实现层未落地的 5 处遗漏(契约源完整,实现层 4 处未覆盖 + 1 处部分覆盖)。核心痛点:worker 模式下 agent 仍按 task-run.md 指引写共享 progress.md/checkpoint.md/INVENTORY.md,PreToolUse hook 的 is_shared_memory 未含这三类文件。

## Actual Changes
- **AC-1 is_shared_memory 扩展**:engine-hook-stop.{sh,ps1} + plugin 镜像,worker 模式下 PreToolUse 拦截 progress.md/checkpoint.md/INVENTORY.md 写共享版本
- **AC-2 worker 模式条件化指引**:task-run.md + handoff.md(engine + plugin 共 4 份)加 worker 边界指引(progress.md/checkpoint.md/INVENTORY.md worker 不写共享,写分片;HANDOFF 归档 coordinator-only)
- **AC-3 UUID fallback**:SessionStart hook(4 份) + engine bin 完全替换 anon-PID 为 UUID(timestamp 兜底,无 PID 复用风险)
- **AC-4 pre-commit worker 检测**:ENGINE_WORKER=1 时拒绝共享三件套 staged(2 份)
- **AC-5 s-/a- 前缀 + sessions/agents 目录**:engine workstream 命令(3 份 sh/ps1/cmd) + engine-context.sh/ps1 适配两级目录 find glob
- **AC-6 文档**:ENGINE_DOCTOR.md #18 worker 实现层检查 + AGENT_ADAPTERS.md s-/a- 前缀与 ENGINE_WORKER 说明,contract-version 6.11.1
- **AC-7 plugin 镜像**:diff -q 全对称,manifest SHA256 更新
- **AC-8 版本三处一致**:VERSION/engine/VERSION/plugin/VERSION = 6.11.1,CHANGELOG 含 v6.11.1
- **AC-9 check.sh 全绿**
- **AC-10 端到端测试**:test_worker_writes_shard.{sh,ps1} 各 6/6 通过(W1-W6: worker 拦截共享 + worker 放行分片 + coordinator 放行)
- **PS 5.1 BOM 修复**:engine-hook-stop.ps1(× 2 镜像)加 UTF-8 BOM,修复 PS 5.1 解析含中文注释的 .ps1 文件失败
- **D-029 Watchpoints**:补 T-038 patch 已落地标注 + 16 份 .ps1 BOM watchpoint

## Impact Scope
D-029 §8 审视结论真正落地到实现层。worker 模式下 progress.md/checkpoint.md/INVENTORY.md 抢写问题根治。影响范围:hook 脚本(4 份 sh + 4 份 ps1) + prompts(4 份) + pre-commit(2 份) + engine bin(4 份) + 文档(4 份) + 测试(2 份新建 + 1 份修改) + 版本文件(3 份) + D-029 + manifest + CHANGELOG。

## Risk & Watchpoints
- **16 份 .ps1 文件含非 ASCII 但缺 UTF-8 BOM**:PS 5.1 不自动检测 UTF-8 无 BOM,回退系统码页(中文 Windows GBK)导致含中文注释的 .ps1 解析失败。T-038 仅修复了 engine-hook-stop.ps1(× 2 镜像),其余 16 份留待未来 patch 批量修复。D-029 Watchpoints 已标注。
- **pwsh 不在 bash PATH**:Git Bash 环境 PATH 无 pwsh,AC-10 verify 命令改用 `pwsh || powershell.exe` 回退。测试文件内部也做 pwsh/powershell 检测。

## Verification
| Check | Result | Evidence |
|-------|--------|----------|
| engine verify T-038 10 pass / 0 fail | pass | T-038/AC-1~AC-10.json |
| check.sh CHECK PASSED (0 failure, 4 warning) | pass | T-038/AC-9.json (fp=1a4e49c44e2c) |
| test_worker_writes_shard.sh 6/6 | pass | T-038/AC-10.json (fp=82726c2af78a) |
| test_worker_writes_shard.ps1 6/6 | pass | 同上 |
| plugin 镜像 diff -q 全对称 | pass | T-038/AC-7.json (fp=f57581da80de) |

## Rollback
回滚所有 AC 改动:恢复 is_shared_memory 原始范围(不含 progress/checkpoint/INVENTORY)、移除 prompts worker 段落、恢复 anon-$$ 兜底、移除 pre-commit ENGINE_WORKER 检测、恢复扁平 workstream 目录、降版本到 6.11.0、删除 2 个测试文件。

## Next Step
- git push
- 后续任务:T-034 checkpoint.md 长任务断点 / T-035 已 done / 新任务待规划

## Responsibility Boundary
- AI checked: 10 AC 全部 verify PASS + check.sh 全绿 + plugin 镜像对称 + D-029 Watchpoints 标注。
- Architect should decide: 16 份 .ps1 BOM 批量修复是否开新任务卡。
