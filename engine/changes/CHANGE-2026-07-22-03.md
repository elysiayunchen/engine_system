# CHANGE-2026-07-22-03 — v6.11.4 仪式缩放指导 (Ceremony Scaling Guidance)

## Goal

在行为技能层明确区分"契约最小必要格式"和"项目可选规模化增强"，防止下游项目将自选仪式误认为引擎强制要求。

## Actual Changes

| 文件 | 变更 |
|------|------|
| `contract/src/behaviors/task-run.md` | 追加 "Ceremony scaling guidance / 仪式缩放指导 (v6.11.4)" 节：契约最小格式定义 + 4 类可选增强表 + agent 行为规则 + 反模式说明 |
| `engine/GLOSSARY.md` | Project Terms 区追加 "Scaling ceremony" 条目 |
| `engine/ENGINE_MAP.md` | revision 31→32；里程碑表追加 v6.11.4 行 |
| 编译产物 | `engine/prompts/behaviors/task-run.md`、`plugin/engine/prompts/behaviors/task-run.md`、`plugin/.claude/skills/engine-task-run/SKILL.md`、`plugin/manifest.json` (sha256 更新) |

## Impact Scope

- 行为层 prose 追加，不动契约规则（13/13 不变）、不动 hook 逻辑、不动模板
- 不消耗契约预算（behaviors 不计入 2940 行；实测 2910/2940 不变）
- 下游项目通过 `/engine-sync` 或 `engine update` 自动获得新行为内容
- PS1 对等性不受影响（behaviors 无 .ps1 孪生）

## Risk & Watchpoints

- 风险极低：纯文档增强，无机器行为变更
- 唯一注意点：新增的 "Agent behavior rule" 使用 MUST/SHOULD 措辞，语义上是对 agent 行为的指导而非 hook 强制——与现有 behavior 文件风格一致

## Verification

- behavior-skills 测试：34 PASS, 0 FAIL（mirror byte-equal + routing + local install）
- contract-compile 测试：8 PASS, 0 FAIL（幂等 + 预算 2910≤2940 + 规则 13≤13）
- dist-drift：11 PASS；hook-parity：31 PASS；task-card：44 PASS；fractal-memory：21 PASS；workstream：14 PASS；behavior-verify：24 PASS
- 总计：187 PASS, 0 FAIL

## Rollback

`git revert` 本次 commit 即可完全回滚。编译产物由 `contract/compile.sh` 幂等重建。

## Next Step

- 诺识团队拉取后，其 agent 在执行任务卡时会加载新的仪式缩放指导
- 如诺识仍有规模化需求（渐进式加重、FU 跟踪），需等契约预算做减法后评估（v7 方向）

## Responsibility Boundary

- 引擎侧：提供最小格式定义 + 可选增强菜单 + agent 行为规则
- 项目侧：自行决定采用哪些增强、按什么风险阈值分级
