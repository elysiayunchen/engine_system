# CHANGE-2026-07-23-05 — T-046 install.sh/ps1 manifest src list sync fix

> task: T-046 | version: v6.11.7 (伴随 T-045,不单独 bump) | decision: none (engine-runtime patch) | issue: GitHub Actions CI red — manifest differs from install.sh/ps1

## Goal

Fix GitHub Actions CI red (second root cause) — `check.ps1` L144-150 "manifest differs from install.sh" / "manifest differs from install.ps1" check failing since v6.7.0 (T-032). `plugin/manifest.json` had 61 src entries, but `install.sh` / `install.ps1` FILES arrays each had only 57 — 4 skeleton files added to manifest across T-032/T-033/T-034 were never synced to the installer scripts:

- `engine/skeleton/checkpoint.md` (T-034 v6.9.0 added to manifest)
- `engine/skeleton/progress.md` (T-032 v6.7.0 added to manifest)
- `engine/skeleton/domains/INVENTORY.md` (T-033 v6.8.0 added to manifest)
- `engine/skeleton/tasks/README.md` (T-033 v6.8.0 added to manifest)

Also fixed a latent bug in the installer case statements: `install.sh` L157 / `install.ps1` L140 used a blanket `engine/skeleton/*` pattern that remapped ALL skeleton paths to `engine/` root. This was correct for ENGINE_MAP/CONTEXT/HANDOFF (skeleton templates → project actual files) but WRONG for the 4 new files (their dest=src, they should stay in `engine/skeleton/`). Fixed to only remap the 3 known files.

## Actual Changes

- **install.sh** (FILES array): Added 4 skeleton entries (`engine/skeleton/checkpoint.md:engine/skeleton/checkpoint.md:false` etc.). Fixed L157 case statement: `engine/skeleton/*)` → `engine/skeleton/ENGINE_MAP.md|engine/skeleton/CONTEXT.md|engine/skeleton/HANDOFF.md)`.
- **install.ps1** (FILES array): Added 4 skeleton entries with backslash dest paths. Fixed L140 case statement: `$entry.src -like "engine/skeleton/*"` → `$entry.src -in @("engine/skeleton/ENGINE_MAP.md","engine/skeleton/CONTEXT.md","engine/skeleton/HANDOFF.md")`.
- **engine/tasks/T-046.md** (new): Task card with 5 AC, status done.
- **engine/tasks/T-046/progress.md** (new): §1+§4 only (small-task exemption).
- **engine/archive/tasks/T-046-progress.md** (new): Archived progress.md.
- **engine/evidence/T-046/** (new): AC-1..AC-5.json + checkpoint.md (5/5 PASS).
- **engine/domains/engine-runtime/INVENTORY.md**: T-046 row added.

## Impact Scope

- **Affected**: CI manifest diff check now PASSES on both Linux (check.sh → pwsh check.ps1) and Windows (check.ps1). With T-045's sessions-dir fix, CI should turn green.
- **Unaffected**: Existing 3 skeleton entries (ENGINE_MAP/CONTEXT/HANDOFF) — remapping behavior unchanged.
- **Unaffected**: manifest.json — no changes needed (already had all 61 entries with correct SHA256).

## Verification

- AC-1: install.sh has 4 skeleton entries → PASS (Select-String confirmed)
- AC-2: install.ps1 has 4 skeleton entries → PASS (Select-String confirmed)
- AC-3: install.sh case statement no longer has blanket `engine/skeleton/*)` → PASS
- AC-4: install.ps1 case statement no longer has blanket `engine/skeleton/*` → PASS
- AC-5: `bash scripts/check.sh` → CHECK PASSED (manifest matches install.sh + manifest matches install.ps1 both PASS)

## Rollback

Revert commit (`git revert`). Remove the 4 skeleton lines from each installer's FILES array and revert the case statement to the blanket `engine/skeleton/*` pattern.

## Responsibility Boundary

T-046 scope: install.sh + install.ps1 only. Does NOT modify manifest.json (already correct), VERSION/CHANGELOG (covered by T-045's v6.11.7 release), or any engine scripts. Self-exempt per small-task exemption (`estimated_steps=5 ≤ 10, checkpoint_plan=inline`).
