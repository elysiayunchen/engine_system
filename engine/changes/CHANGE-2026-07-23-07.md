# CHANGE-2026-07-23-07 — D-034 contract debt_baseline 计量修正 (47→54)

> task: D-034 | version: v6.11.8 (patch, no version bump) | decision: D-034 | committed: 2026-07-23

## Goal

修正 `contract/budget.json` 的 `debt_baseline` 从 47 提升到 54,消除 Doctor 持续 WARN(`contract debt > baseline (54 > 47)`)。根因:T-040(commit dbffd66, v6.11.3)设 `debt_baseline=47` 时未跑 doctor 实测——当时实际 debt 已是 53(MUST=66, gated Rules=13)。v6.11.4(commit 0303c29, ceremony scaling guidance)新增 1 个 MUST,debt 涨至 54。

## Actual Changes

- **contract/budget.json**:`debt_baseline` 47→54;`_comment` 追加 D-034 修正记录(沿用既有模式,记录根因 + 理由)。
- **engine/decisions/D-034.md**:决策卡(新增),记录根因分析 + Option A(修正 baseline)批准 + Consequences + Rationale + Watchpoints。
- **rules.json**(dist,由 `bash contract/compile.sh` 重新生成):`debt_baseline` 字段同步到 54。
- **engine/changes/CHANGE-2026-07-23-07.md**:本文件。
- **engine/{CONTEXT,HANDOFF,ENGINE_MAP}.md**:状态面板 + 会话历史 + 决策计数更新。

## Impact Scope

- **Affected**: Doctor `check_contract_debt` 检查(WARN 消除,从 `54 > 47` 变为 `54 == 54` net-zero holding)。
- **Unaffected**: `max_lines`(2940)/`max_rules`(13)不变;contract/src/ 散文 MUST 强度不弱化;不 bump 版本(计量修正非新功能)。
- **Unaffected**: CI 行为(此前 WARN 非 blocking,CI 已绿;现在彻底 clean)。

## Risk & Watchpoints

- **Baseline 设错的教训**:T-040 设 baseline 时未跑 doctor 实测。未来设/改 `debt_baseline` 必须先跑 `bash engine/scripts/engine-doctor.sh` 实测当前 debt,不能凭预估。
- **不弱化散文 MUST**:54 个 debt 中的 MUST 是 ENGINE_MAP 模板规则、Re-anchor、Read-gate、Lifecycle Transaction 等核心强制条款,不应为满足 baseline 弱化为 SHALL/将/需要。
- **未来新增 Hard Rule**:若提升 `max_rules`,须同步评估 `debt_baseline` 是否需再提(新 Rule 的 gated MUST 不计入 debt)。

## Verification

- `bash contract/compile.sh`:成功(5 dist files + manifest sha256 backfilled)。
- `bash engine/scripts/engine-doctor.sh`:`PASS contract debt: MUST=67, gated Rules=13, debt=54, baseline=54` + `PASS contract debt <= baseline (54 <= 54) - net-zero holding`;Engine Doctor: 0 failure(s), 0 warning(s)。
- `bash scripts/check.sh`:CHECK PASSED。
- 根因验证(`git log dbffd66..HEAD -- contract/src/`):v6.11.3→v6.11.8 期间 contract/src/ 仅 1 个 commit(0303c29),只新增 1 MUST、删除 0 MUST,证明 7 个超 baseline 的 debt 全是历史存量。

## Rollback

`git revert` 本 commit。budget.json 恢复 `debt_baseline=47`,Doctor 回到 WARN(非阻塞,CI 仍绿)。

## Responsibility Boundary

D-034 scope:仅修 `contract/budget.json` 的 `debt_baseline` 计量 + 决策卡 + change capsule + 写回。不碰 contract/src/ 散文、不 bump 版本、不改变 `max_lines`/`max_rules`。这是计量修正,不是为新增债务放水。

## Next Step

可选 `git tag v6.11.8 && git push --tags` 触发 GitHub Release;或关闭 issues #9 #10;或处理 CHANGE-2026-07-23-03.md 占位符。
