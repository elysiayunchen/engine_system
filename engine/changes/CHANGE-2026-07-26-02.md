# CHANGE-2026-07-26-02 - v6.12.0 多任务卡并行(union gating)+ 会话租约液性修复

> Created: 2026-07-26 · Status: verified · Related: D-035 / T-048 · Supersedes: 无(修订 D-029/T-036 实现层)

## Goal

根治用户报告的多会话撞车:「激活了一个任务卡结果不让其他 agent 提交改动」。审视实证定位 6 项根因(D-035 RC-1~RC-5 + RC-3b),全部修复,契约版本升 v6.12.0。

## Actual Changes

**门禁层(sh + ps1 孪生,root + plugin 四份镜像)**
- `engine-hook-stop.sh/.ps1`(PreToolUse + Stop):单卡 `find_active_task` 改全量收集(active + dirty-done closing 恒并入);`block_scope` 改 union gating(∃卡覆盖即放行,FORBIDDEN 只约束本卡);任务/决策卡 bootstrap 恒豁免;`is_shared_memory` 拆共享单例 + 任务局部;写共享单例逐次验租约(持锁放行 / 他人 fresh 拦 / free-stale 原子抢占 + 自愈升格);worker 自分片(任一 active 卡下)校验后直接放行;每次工具调用 touch `.hb` 续租;Stop 域路由逐 path 用覆盖卡 domain;Stop 跳过 workstreams 分片路径。
- `engine-hook-session-start.sh/.ps1`:锁液性从 `kill -0`(hook shell 瞬时 pid,恒判死 → 人人自封 coordinator → v6.11.0 保护空转,本仓 tombstone 实证)改为租约 mtime/heartbeat TTL(`ENGINE_SESSION_TTL_MIN` 默认 120 分钟);同 sid 重入重盖自己租约;coordinator 上位三路径一律删自身 `.role=worker` 旗标(修 resume 永久 worker 死锁);7 天孤儿 session 文件 GC;guard 模式续租 + 空锁抢占 + 多卡列表;full 模式多卡注入(≤3 全文)。
- `githooks/pre-commit`:card_meta 表预解析全部治理卡;逐 staged 文件 union 校验;bootstrap 恒豁免;protected 检查逐卡豁免(每张卡豁免自己的 .md/.spec/子目录/归档 progress;决策卡恒豁免)+ 按覆盖卡的 decision 校验。
- `engine/bin/engine(.ps1)` `assume-coordinator`:stale 租约免 `--force` 接管(tombstone `stale-recovered`),fresh 才要 `--force`(`forced-replaced`)。
- `engine-context.sh/.ps1`:Active 卡面板多卡化。
- `engine-doctor.sh/.ps1`:新增 `check_multi_card_writeset_overlap`(两 active 卡同名 WRITE-SET 条目 → WARN,共享单例与 tasks/ 自身除外)。

**契约与发布面**
- `contract/src/30-operational.md` 多会话段整段改写为 v6.12.0 语义,编译后 web-prompt 2910→2896 行(预算 2940,净减 14,13 规则不变)。
- `engine-migrate-contract.sh/.ps1`:AGENTS.md managed block 2 处 bullet 改写 + 1 处新增;ENGINE_DOCTOR item 17 租约化;已在本仓跑过(AGENTS/SYSTEM/ENGINE_DOCTOR 托管块 → contract-version 6.12.0)。
- `contract/src/behaviors/task-run.md` + `verify-writeback.md` 第 7/5 条并行措辞升级(编译同步 skills/prompts/plugin)。
- VERSION ×3 → 6.12.0;CHANGELOG v6.12.0 段;AGENT_ADAPTERS.md C 档子段重写(租约用法/assume-coordinator 约束/并行模式选择);GLOSSARY 加 Union gating / Coordinator lease / Heartbeat 三条。

**测试**
- 新 `tests/multi-session/`:pretooluse_union(8)/ lock_liveness(9)/ role_cleanup(7)/ shared_write_lease(8)/ worker_scope(8)/ multi_card_display(8)+ runner;`tests/task-card/test_multi_active_union.sh`(9,pre-commit 层)。
- 旧测试更新到租约语义:`test_double_signal.sh/.ps1` D1 加 fresh 锁前提;`test_lock_recovery.sh/.ps1` L1 拆 L1a(fresh 降级)/L1b(TTL 接管)+ L3b(no-force stale 接管)。
- 收编 v6.11.x 孤儿测试(double_signal/lock_recovery/worker_mode/kill_switch/worker_writes_shard——原先不在任何 runner/check 链上)进 `run-multi-session-tests.sh`;`scripts/check.sh` 加 Multi-session isolation 步骤。

## Impact Scope

引擎运行时(hooks/pre-commit/CLI/doctor/context)+ 契约文本 + migrator + 测试。经 plugin 镜像 + migrator 分发到所有安装项目;老项目(contract-version < 6.12.0)fail-open 行为不变。

## Risk & Watchpoints

- 两张 active 卡 WRITE-SET 交集路径:union 放行两边,竞态落到 git 层(固有边界,Doctor WARN 提示收窄)。
- TTL 窗口(默认 120 分钟)内失联 coordinator 挡共享单例写;`engine assume-coordinator` 一条命令兜底,PreToolUse 拦截消息内建提示。
- bootstrap 恒豁免 + protected 逐卡豁免 = 任何会话可改任务/决策卡;防篡改从写锁移到 git 历史 + 架构师审阅(D-035 Consequences 诚实声明)。
- 旧 `.role=worker` 语义消费者(若有外部脚本读该旗标)需知旗标现在会被清理。

## Verification

| Check | Result | Evidence |
|-------|--------|----------|
| 新 multi-session 套件 sh | pass 48/48 + runner 11 套全绿 | engine verify T-048 → evidence/T-048/ |
| pre-commit union | pass 9/9 | tests/task-card/test_multi_active_union.sh |
| 旧套件回归 | task-card 44/44 · workstream 14/14 · hook-parity 31/31 · double_signal 4/4(sh+ps1) · lock_recovery 18/16 sh/ps1 | 本轮运行输出 |
| ps1 语法 | 4 hook 文件 pwsh + PS 5.1 Parser 0 错误(fork 子代理验证) | check.ps1 步骤覆盖 |
| 契约预算 | 2896 ≤ 2940,13 规则 | contract/compile.sh 输出 |
| 全仓门禁 | bash scripts/check.sh | evidence/T-048/AC-8 |

## Rollback

`git revert` 本次提交即可(锁/旗标/心跳均在 .gitignore 的 engine/.cache/ 下,无持久状态迁移);或设 `ENGINE_DISABLE_MULTI_SESSION=1` 临时回到单会话模式。

## Next Step

真实双会话并行试用(两个 Claude Code 实例各持一张卡),观察:租约续期手感、TTL 是否需调、Doctor overlap WARN 噪声。方案 B(会话-任务强绑定)按 D-035 Open Questions 留给架构师。

## Responsibility Boundary

- AI checked:六项根因实证、三层门禁改造、sh/ps1 语义对齐、48+9 例新测试、旧套件回归、契约净零、migrator 幂等。
- Architect should decide:TTL 默认值是否合适;bootstrap 恒豁免的防线转移是否接受;D-035 是否维持 approved。
