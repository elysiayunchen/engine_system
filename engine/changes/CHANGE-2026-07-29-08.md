# CHANGE-2026-07-29-08 - v6.15.1 T-058 证据 + 文档完整性修复

> Created: 2026-07-29 · Status: verified · Related: T-059 · Supersedes: 无

## Goal

修复 T-058 子代理 5 维度复查发现的 3 个 CRITICAL + 3 个 MAJOR + 6 个 MINOR 证据/文档问题。功能层(BOM 添加 + 镜像一致 + 契约编译)已全 PASS,本次仅修证据层和文档层,使 T-058 的行为化验收证据可机械复现、文档与实际状态一致。

## Actual Changes

**A. CRITICAL 修复(3 处)**

- A-1 (C1/P-CRIT-2): 7/7 T-058 AC 证据文件 fingerprint 从描述性字符串重写为真实 64-hex SHA256:
  - AC-1: `sha256:24files-bom-verified` → `sha256:61559a1e...` (engine-migrate-contract.ps1)
  - AC-2: `sha256:compile.sh-synced` → `sha256:676a1506...` (engine-doctor.ps1)
  - AC-3: `sha256:62entries-verified` → `sha256:d4935e5d...` (plugin/manifest.json)
  - AC-4: `sha256:sh-files-no-bom` → `sha256:d3431608...` (contract/compile.sh)
  - AC-5: `sha256:no-dupes` → `sha256:d4935e5d...` (plugin/manifest.json)
  - AC-6: `sha256:warning-comment-added` → `sha256:61559a1e...` (engine-migrate-contract.ps1)
  - AC-7: `sha256:v6.15.0` → `sha256:74894f40...` (VERSION@6.15.0)
- A-2 (C2): T-058 AC-6 verify 命令从 `Select-String -Pattern 'FUNCTIONAL.*§.*MUST NOT'`(无匹配)改为 `grep -c 'FUNCTIONAL section signs'` = 1(匹配 L9)。
- A-3 (C3): .sh § 计数口径明确,4 份文档(T-058.md/T-058-progress.md/CHANGE-07/CHANGELOG)从"实测 100 处,非 113"改为"113 字符出现(grep -o|wc -l)/ 101 行(grep -c)"。

**B. MAJOR 修复(3 处)**

- B-1 (M1): check-version-consistency.ps1 BOM 文档修正(3 处:progress.md L28、CHANGE-07 L50、AC-1.json note):"纯 ASCII 无 BOM" → "实测含 BOM(EF BB BF)"。
- B-2 (M2): .ps1 文件计数 24→25(progress/checkpoint/CHANGE/CHANGELOG):WRITE-SET 24 + check-version-consistency.ps1 = 25 实测。
- B-3 (M3): 行号漂移 +8 修正(P4 加 7 行注释 + 1 空行导致):L304→L312, L355-360→L362-368, L375-394→L379-402。修正文档:T-058.md CONSTRAINTS、T-058-progress.md、CHANGE-07、CHANGELOG v6.15.0、AC-6.json note。历史 CHANGELOG v6.14.2 行号保留(当时准确)。

**C. MINOR 修复(6 处)**

- C-1: P4 注释行数 10→7(L9-15)
- C-2: L375-394 模板范围 → L379-402
- C-3: CHANGELOG v6.14.2 "重生成 CONTEXT.md" → "重生成 skeleton/progress.md"
- C-4: manifest HEAD hash stale(153a74b7→61559a1e)随 T-059 commit 提交
- C-5: checkpoint.md 计数 24→25
- C-6: AC-1 note 关于 check-version-consistency.ps1 的描述修正

**D. 版本 + 证据**

- D-1: 版本 bump 6.15.0→6.15.1(patch,evidence/docs fix)
- D-2: T-059 evidence(7 AC + checkpoint)生成,真实 SHA256 + 可执行 verify
- D-3: T-059 progress + change capsule 生成

## Impact Scope

- 仅证据文件(engine/evidence/T-058/**)+ 文档(T-058.md/progress/CHANGE/CHANGELOG/HANDOFF/CONTEXT)变更
- 不动任何功能代码(.ps1/.sh/.json dist/contract src)
- 不动 manifest(工作树 hash 修正已存在,仅随 commit 提交)
- 不影响 plugin 镜像(byte-identical 保持)
- 不影响 contract compile(dist 非 stale)

## Risk & Watchpoints

- **证据可复现性**:fingerprint 用真实文件 SHA256(sed 's/\r$//' | sha256sum),与 manifest 一致,可机械复现。
- **verify 可执行性**:所有 verify 命令为 bash/grep/diff/check.sh,可在 Git Bash 环境独立运行。
- **行号稳定性**:行号引用基于当前工作树,若未来 P4 注释再变更需同步更新。建议改用 grep -n 动态查找而非硬编码行号。
- **版本历史**:T-058 AC-7 verify 改为检查 CHANGELOG(非 VERSION),因 VERSION 会随后续版本 bump 变化,CHANGELOG 历史条目永久保留。

## Verification

- 7 个 T-058 AC 证据 fingerprint 全部为合法 64-hex SHA256
- 7 个 T-058 AC 证据 verify 全部为可执行命令
- AC-6 verify 实际可匹配(grep -c = 1)
- check-version-consistency.ps1 BOM 实测确认(EF BB BF)
- .sh § 计数 113 字符出现 / 101 行
- check.sh CHECK PASSED

## Rollback

`git revert <commit>` 即可。回退后 T-058 证据回到描述性字符串状态(功能不受影响,仅证据质量退化)。

## Responsibility Boundary

- T-059 scope: T-058 证据 + 文档完整性修复(3 CRITICAL + 3 MAJOR + 6 MINOR)
- 不含: T-056/T-057 证据修复(已推送,历史不动)
- 不含: 功能代码变更(BOM/镜像/contract dist 均不动)

## Next Step

push main + tag v6.15.1 触发 CI/Release。T-058 证据 + 文档完整性已闭环。
