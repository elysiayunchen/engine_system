# CHANGE-2026-07-30-01 — T-064 v6.17.3

## Goal
修复 CI/Doctor 报出的 3 个问题:(1) Windows Git Bash 下 awk 按字节计数导致 HANDOFF.md 长行误报;(2) check-version-consistency.sh 不可执行;(3) skeleton ENGINE_MAP.md 空模板导致 install dry-run 6 doctor FAIL。

## Actual Changes
- **engine-doctor.sh + plugin mirror**: check_long_line 改用 `perl -CSD` 按 UTF-8 字符计数,awk 作 fallback。修复 Windows Git Bash C/POSIX locale 下 `awk length()` 返回字节数的误报。
- **engine/checks/check-version-consistency.sh**: `git update-index --chmod=+x` 将 git 文件模式从 100644 改为 100755。修复 Linux CI `run_custom_checks` 的 `-x` 可执行性检查 WARN。
- **plugin/engine/skeleton/ENGINE_MAP.md**: 从空模板扩展为含 §0 profile 占位 + §1 预注册 6 核心文件(ENGINE_MAP/SYSTEM/CONTEXT/HANDOFF/ENGINE_DOCTOR/GLOSSARY)。修复 install + migrate 后 doctor `check_disk_to_registry` 对 6 个 `engine/*.md` 文件报 FAIL。
- **T-061 状态**: 从 active 改为 paused(T-061 实现已包含在本提交中,但 AC-8/9/10 测试和验证未完成)。
- **版本**: 6.17.2 → 6.17.3。

## Impact Scope
- `engine/scripts/engine-doctor.sh` + plugin mirror: check_long_line 函数
- `plugin/engine/skeleton/ENGINE_MAP.md`: install 新项目时 ENGINE_MAP.md 初始内容
- `engine/checks/check-version-consistency.sh`: git 文件模式(无内容变更)
- `plugin/manifest.json`: SHA256 更新

## Risk & Watchpoints
- **perl 依赖**: `perl -CSD` 需要 perl 安装。GitHub Actions runners(Ubuntu/Windows/macOS)和 Git Bash 均自带 perl。若用户系统无 perl,回退到 awk(可能仍有 Windows 误报,但不影响 Linux)。
- **skeleton ENGINE_MAP.md 预注册**: init 时 agent 需知道这些预注册条目已存在,不要重复添加。init prompt 应处理 this。
- **T-061 包含**: T-061 的 parse_ac_declarations 函数和 AC 格式解析改动已包含,但未独立验证。T-061 恢复后需跑 AC-8 测试。

## Verification
- 本地 `engine-doctor.sh`: 0 failure, 0 warning (T-061 overlap WARN 除外)
- AC-1: `grep -l 'perl -CSD -ne' engine/scripts/engine-doctor.sh plugin/engine/scripts/engine-doctor.sh | wc -l` = 2
- AC-2: `git ls-files -s engine/checks/check-version-consistency.sh | grep -c 100755` = 1
- AC-3: skeleton ENGINE_MAP.md 含 6 核心文件名
- AC-5: `diff engine/scripts/engine-doctor.sh plugin/engine/scripts/engine-doctor.sh | wc -l` = 0

## Rollback
- Revert this commit
- `git update-index --chmod=-x engine/checks/check-version-consistency.sh` 恢复 100644

## Next Step
- CI 验证:Linux doctor 0 WARN(除 .cache/sessions),Windows Git Bash doctor 不再报 HANDOFF.md long line,Install dry-run doctor 0 FAIL
- T-061 恢复:完成 AC-8(4 格式测试)+ AC-9/10(plugin 镜像 + CHANGELOG)

## Responsibility Boundary
T-064 负责 CI/Doctor 三项修复。T-061 的 AC 格式解析实现已包含但未独立验证,T-061 恢复后需补测试。
