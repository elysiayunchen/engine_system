# CHANGE-2026-07-30-02 — T-065 v6.17.4

## Goal
修 issue #21 — pre-commit hook 的 closing_paths 收集逻辑(L234-238)把所有 staged + status:done 的任务卡当作 "closing" 加入 governing_files,不区分「新 close(active→done)」与「修改已 done 卡(HEAD 已 done,本次只改内容)」。导致修改已 done 卡时该卡被误加入 governing,其 WRITE-SET 不覆盖的文件被 union check 拦截,只能 --no-verify 绕过。

## Actual Changes
- **engine/scripts/githooks/pre-commit + plugin mirror (L234-245)**: closing_paths 收集逻辑增加 HEAD 检查。staged 卡 status:done 后,额外检查 `git show HEAD:$_task_path` 是否也 status:done;HEAD 已 done 则跳过(不是 closing,不加入 governing)。与 issue #18 的 AC PASS 检查修复(L375-381)模式一致。
- **tests/workstream/test_precommit_done_card_governing.sh**: 新增 5 场景测试:active→done closing / 新卡首次 done closing / 已 done 修改跳过 / staging active 跳过 / 新 active 卡跳过。
- **版本**: 6.17.3 → 6.17.4。
- **plugin/manifest.json**: pre-commit + VERSION sha256 更新。

## Impact Scope
- `engine/scripts/githooks/pre-commit` + plugin mirror: closing_paths 收集块(L234-245)
- `tests/workstream/test_precommit_done_card_governing.sh`: 新测试文件
- `plugin/manifest.json`: 2 个 sha256 条目更新

## Risk & Watchpoints
- **行为变更**: 修改已 done 卡时,该卡不再加入 governing。这意味着 staged 的 done 卡修改 + 其他不相关文件可以一起提交(不再被 done 卡的 WRITE-SET 拦截)。这是预期行为:done 卡的内容修改不应治理其他文件。
- **与 issue #18 (#21 的姊妹修复)的对称性**: issue #18 修了 AC PASS 检查(L367+),issue #21 修了 WRITE-SET governing 收集(L234+)。两处现在都区分「新 close」与「修改已 done」。
- **HEAD 缺失(新卡)**: `git show HEAD:$_task_path` 返回空,grep 不匹配 done -> 不 skip -> 加入 governing(正确:新卡首次 done 是 closing)。

## Verification
- `tests/workstream/test_precommit_done_card_governing.sh`: 5 pass, 0 fail
- `tests/workstream/test_precommit_done_card_drift.sh`(issue #18 回归): 5 pass, 0 fail
- `diff engine/scripts/githooks/pre-commit plugin/engine/scripts/githooks/pre-commit`: IDENTICAL
- `scripts/check.sh`: CHECK PASSED

## Rollback
- Revert this commit

## Responsibility Boundary
T-065 负责 issue #21 的 closing_paths 收集逻辑修复。不涉及 AC PASS 检查(issue #18 已修)、WRITE-SET/FORBIDDEN/protected/dist-stale 门禁(独立代码路径)。
