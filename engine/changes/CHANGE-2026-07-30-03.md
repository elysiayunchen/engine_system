# CHANGE-2026-07-30-03 — T-066 v6.18.0

## Goal
实施 D-038a/b 两层防漂移——evidence schema 升级为多锚(output_fingerprint + code_fingerprint + write_set_snapshot + verified_against_commit + evidence_manifest_sha256 + write_provenance)+ 新增 engine drift-check 三步顺序校验(完整性自证 → WRITE-SET 二阶检测 → 代码指纹比对)+ evidence/drift-check 脚本纳入 protected_paths + pre-commit provenance 校验。堵 6 类漂移(时间/锚点/状态/evidence篡改/WRITE-SET二阶/EOL假DRIFT)。

## Actual Changes
- **engine/decisions/rules.json + plugin mirror**: `engine/evidence/**` + `engine/scripts/engine-drift-check.*` 纳入 protected_paths
- **engine-verify.{sh,ps1} + plugin mirror**: evidence JSON schema 升级为多锚——新增 `code_fingerprint`(`git ls-files -s` 取 index blob sha)、`write_set_snapshot`、`verified_against_commit`、`write_provenance`(writer/commit/timestamp/argv);循环结束写 `MANIFEST.json`(目录所有 .json + checkpoint.md 聚合 sha256,排除自身);收集 code_fingerprint 时前置 `git add` 检查(未 add 则 FAIL)
- **engine-drift-check.{sh,ps1} + plugin mirror**: 新建脚本,三步顺序校验(完整性自证 → WRITE-SET 二阶检测 → 代码指纹比对),任一 FAIL 仍输出后续摘要标 unverified;legacy evidence(无 MANIFEST + 无 write_provenance)→ WARN 跳过,信任级 T2
- **githooks/pre-commit + plugin mirror**: AC PASS 检查后追加 evidence provenance gate。双路径:(a) 机器写入 → writer=engine-verify + commit=HEAD + argv='engine verify <task>';(b) 手工编辑 → evidence JSON 须带 `evidence-manual-edit` 标注。pre-commit protected-path gate 加 `engine/evidence/T-NNN/**` self_exempt
- **engine-doctor.{sh,ps1} + plugin mirror**: 新增 `check_drift`/`Test-Drift` 函数,调用 drift-check 脚本
- **tests/workstream/test_drift_check.sh**: 5 场景端到端(clean PASS / MANIFEST 篡改 FAIL / 代码改 DRIFT / MANIFEST 缺失 tamper FAIL / provenance.commit 不匹配 FAIL)
- **tests/workstream/test_evidence_provenance.sh**: 6 场景 black-box(clean PASS / 缺 provenance FAIL / writer=manual FAIL / commit!=HEAD FAIL / manual-edit 标注 PASS / argv 不匹配 FAIL)
- **版本**: 6.17.4 → 6.18.0。CHANGELOG 新增 v6.18.0 段。

## Impact Scope
- 防漂移机制覆盖所有 done 任务的 evidence 目录(49 个 legacy + T-066 本身)
- pre-commit provenance gate 影响所有含 evidence 的提交
- drift-check 在 doctor/手动/SessionStart 三处触发(撤出 pre-commit)

## Risk & Watchpoints
- **legacy evidence 迁移期**: 49 个无 MANIFEST 的旧证据 → WARN 跳过(T2),不阻断。有 write_provenance 但 MANIFEST 缺失 → FAIL tamper
- **code_fingerprint 前置 git add 要求**: WRITE-SET 代码文件须 git add 进 index 才能计算 blob sha,未 add 则 verify FAIL。agent 须先 git add 再跑 verify
- **跨平台 EOL**: 使用 `git ls-files -s` 取 index blob sha(已 EOL 归一化),不用 `git hash-object`/`sha256sum`,避免 Windows CRLF 假 DRIFT

## Verification
- `engine verify T-066`: 12 pass, 0 fail, 0 skip
- `tests/workstream/test_drift_check.sh`: 5 pass, 0 fail
- `tests/workstream/test_evidence_provenance.sh`: 6 pass, 0 fail
- plugin 镜像 7 文件 byte-identical
- `scripts/check.sh`: CHECK PASSED

## Next Step
- 提交 D-038 决策卡 + T-066 任务卡及全部实现文件
- 推送后创建 v6.18.0 tag 触发 GitHub CI/Release workflow
- 监控 CI 确认 release 构件正常生成

## Rollback
- Revert this commit

## Responsibility Boundary
T-066 负责 D-038a/b 两层防漂移实施。不涉及 D-038c(状态视图)和 D-038d(信任分级注入)——留独立任务。
