# CHANGE-2026-07-30-04 — T-067 v6.19.0

## Goal
实施 D-038c/d 两层防漂移——CONTEXT.md 状态面板从「权威声明」降级为「派生视图」(engine context 输出新增 "Derived Status" 段,实时重算 git tag + engine/VERSION + 最近 done 卡 evidence 信任级)+ `engine context` 输出对每段声明打 T1/T2/T3 信任标签(T1=机器校验通过;T2=legacy-evidence/declared-only/stale;T3=待验证)+ Doctor 校验 derived/legacy 标注与一致性。堵声明漂移 + 决策漂移 + agent 误判。批量补 code_fingerprint 独立到 T-068。

## Actual Changes
- **engine-context.{sh,ps1} + plugin mirror**: 新增 `render_derived_status()`/`Render-DerivedStatus` 函数,实时计算最近 git tag、engine/VERSION 一致性、最近 done 卡 evidence 信任级(code_fingerprint 存在 + verified_against_commit=HEAD/ancestor → T1 structural;无 code_fingerprint → T2 legacy-evidence;vac not ancestor → T2 stale;tag/VERSION mismatch → T3),输出 "Derived Status" 段(machine-verified);CONTEXT.md 输出时按段注入 [T1]/[T2 legacy]/[T2 declared-only]/[T3 unverified] 信任标签
- **engine-doctor.{sh,ps1} + plugin mirror**: 新增 `check_derived_status()`/`Test-DerivedStatus` 函数,校验 (1) CONTEXT.md `<!-- legacy: status-panel -->` 标注存在(缺失→WARN);(2) 派生值(最近 git tag vs engine/VERSION)与静态声明一致(不匹配→WARN);(3) 静态面板是否引用当前版本(stale panel detection)。双写过渡期 WARN 不 FAIL
- **engine/CONTEXT.md**: "状态面板" 段加 `<!-- legacy: status-panel (double-write transition, v6.19.0~v6.20.0) -->` 标注 + 说明注释(engine context 输出时实时重算 "Derived Status" 段,本静态段保留并标 legacy)
- **tests/workstream/test_derived_status.sh**: 6 场景端到端(S1 derived 段渲染 + T1 / S2 信任标签 [T2 legacy]/[T2 declared-only]/[T3 unverified] 注入 / S3 doctor PASS on legacy annotation / S4 doctor WARN on missing legacy / S5 doctor WARN on tag/VERSION mismatch / S6 T2 legacy-evidence when no code_fingerprint)
- **版本**: 6.18.0 → 6.19.0。CHANGELOG 新增 v6.19.0 段。

## Impact Scope
- `engine context` 输出新增 "Derived Status" 段 + 信任标签注入(所有 agent 调用 engine context 都受影响)
- Doctor 新增 `check_derived_status` 校验(每次 doctor 运行都触发)
- CONTEXT.md 状态面板降级为 legacy(双写过渡期 v6.19.0~v6.20.0)
- 信任标签计入 D-019e SessionStart 预算(标签注入是输出层,不写入 CONTEXT.md 文件)

## Risk & Watchpoints
- **双写过渡期容忍**: Doctor 对 derived 不一致只 WARN 不 FAIL(迁移期容忍),v6.20.0 后须切硬 FAIL
- **verified_against_commit 校验范围**: HEAD 直接匹配 OR ancestor of HEAD(evidence 在任务提交前写入,vac 通常是 HEAD~1)。若任务已 rebase / squash,vac 可能脱离 ancestor 链 → T2 stale
- **T2 信任级通胀**: 当前 14+ 张 done 卡无 code_fingerprint(legacy evidence)→ 都标 T2 legacy-evidence。T-068 批量补 code_fingerprint 后提升 T1 占比
- **最近 done 卡选择**: 取 lexicographic highest T-NNN(with status:done)。若跳号(如 T-070 done 但 T-068/T-069 active),derived 显示 T-070 而非实际"最近完成"

## Verification
- `engine verify T-067`: 10 pass, 0 fail, 0 skip (AC-1~AC-10)
- `tests/workstream/test_derived_status.sh`: 9 pass, 0 fail (S1 双断言)
- plugin 镜像 4 文件 byte-identical(engine-context.sh/ps1 + engine-doctor.sh/ps1)
- `scripts/check.sh`: CHECK PASSED

## Next Step
- 提交 T-067 任务卡及全部实现文件
- 推送后创建 v6.19.0 tag 触发 GitHub CI/Release workflow
- 监控 CI 确认 release 构件正常生成
- 拆分子任务 T-068:对近期 done 卡(T-048~T-065)批量重跑 `engine verify` 补全 code_fingerprint,提升 T1 信任级占比

## Rollback
- Revert this commit

## Responsibility Boundary
T-067 负责 D-038c/d 两层防漂移实施(状态视图化 + 信任分级注入)。不涉及批量补 code_fingerprint(独立到 T-068)。不修改 D-038 决策卡本体(已在 T-066 时 approved)。
