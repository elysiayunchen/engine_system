# CHANGE-2026-08-01-01 — GitHub issue regression and README refresh

## Goal

Close the implementation gap behind GitHub issues #13, #14, #22, #23, #24, #26, and #27, and bring the root English/Chinese README up to the shipped v6.24.0 CLI.

## Actual changes

- Fixed the Windows review wrapper's `-Task` parameter contract and PowerShell `$task:` interpolation.
- Made `engine update` pass an internal `--skip-migrate`/`-SkipMigrate` flag to the installer so the wrapper owns contract migration exactly once.
- Added explicit `ENGINE_COMMIT_ROLE=implementer-candidate` handling to the pre-commit memory gate while retaining path, FORBIDDEN, protected-path, and shared-singleton checks.
- Made Bash and PowerShell update checks distinguish remote-newer, equal, and remote-older versions.
- Accepted legacy `verdict: PASS` evidence in Doctor and pre-commit; Doctor warns and recommends re-verification.
- Added Bash/PowerShell behavior fixtures for all four AC declaration formats and issue regressions.
- Updated README.md and README.zh.md with acceptance preflight, prove, review, gate, close, candidate commits, and current update semantics.

## Impact scope

Runtime scripts, CLI mirrors, installer migration scheduling, pre-commit policy, plugin manifest hashes, regression tests, and root documentation.

## Risk and watchpoints

- `ENGINE_COMMIT_ROLE=implementer-candidate` is intentionally explicit; without it the existing coordinator memory requirement remains.
- Remote-older versions are reported as version skew and never recommended for downgrade.
- Legacy evidence is accepted only when no contradictory `status` field exists.
- Existing unrelated T-069/T-079 dirty artifacts and repository-wide Doctor failures were preserved.

## Verification

- `engine verify T-080 --preflight` — 8/8 AC PASS; evidence: `engine/evidence/T-080/`.
- `bash tests/workstream/test_issue_regressions.sh` — PASS.
- `pwsh -NoProfile -File tests/workstream/test_issue_regressions.ps1` — PASS.
- `bash tests/behavior-verify/test_verify_block_ac_format.sh` — PASS.
- `pwsh -NoProfile -File tests/behavior-verify/test_verify_block_ac_format.ps1` — PASS.
- PowerShell parser checks for changed `.ps1` files — PASS.

## Rollback

Revert the T-080 runtime/docs/test files and manifest entry updates as one change. Do not remove unrelated pre-existing dirty files or evidence.

## Next step

Coordinator review and normal repository commit/merge. GitHub issue closure remains a deliberate maintainer action and is not automated here.

## Responsibility boundary

Codex implemented and verified this card's write-set. The coordinator owns shared-memory merge, final staging/commit, and any external issue updates.
