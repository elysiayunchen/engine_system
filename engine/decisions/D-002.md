# D-002 — S2 分形记忆：联邦表 + 域引擎 + 路由 read-gate
> status: approved
> date: 2026-07-03
> expiry: none
> scope: engine/domains/**, engine/ENGINE_MAP.md, engine/CONTEXT.md, engine/HANDOFF.md, engine/scripts/engine-hook-session-start.*, engine/scripts/engine-hook-stop.*, ENGINE_FILE_SYSTEM_v5.md, plugin/.claude/commands/engine-init.md, plugin/engine/scripts/engine-hook-session-start.*, plugin/engine/scripts/engine-hook-stop.*, tests/**, scripts/check.sh, scripts/check.ps1, engine/decisions/**, engine/tasks/**

选项: A 分形记忆完整切片(选定) / B 仅联邦表骨架推迟装配 / C 等 S1 实战检验后再做
理由: S1 已落地并全套验证通过(任务卡+决策台账+三层门禁+parity/task-card 测试全绿);S2 是路线图回答百万行(G3)的核心一步——工作集常数化(N1)依赖分形分区。本切片含联邦表+域引擎+路由read-gate+L2装配+汇总协议+检索配方,可独立发布、向后兼容(无域时回退 S1 行为)。
后果: 引入 engine/domains/ 新文件类型(federation.json + 域 CONTEXT/PITFALLS);ENGINE_MAP 升级联邦表段(rev 8);SessionStart 按任务卡 domain 装配 L2(≤300 行/域);Stop hook 加路由一致性校验(WRITE-SET 代码路径所属域 ⊆ 任务卡 domain 集合,越域=block);INIT 采访加「项目分几大块」。本仓库自托管铺设 2 域(engine-runtime + project-meta)补 D6 缺口。分发策略:路由/装配/汇总逻辑内联进 hook,不新增独立可执行,install 清单不变。
