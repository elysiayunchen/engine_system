# D-004 — S4 驾驶舱：engine verify 行为化验收 + status v2
> status: approved
> date: 2026-07-03
> expiry: none
> scope: engine/scripts/engine-verify.*, engine/bin/engine, engine/bin/engine.ps1, engine/bin/engine.cmd, plugin/engine/scripts/engine-verify.*, plugin/.claude/commands/engine-status.md, ENGINE_FILE_SYSTEM_v5.md, contract/src/ENGINE_FILE_SYSTEM.md, scripts/check.sh, tests/**, engine/ENGINE_MAP.md, engine/CONTEXT.md, engine/HANDOFF.md, engine/decisions/**, engine/tasks/**

选项: A engine verify + status v2(选定) / B 仅 verify / C 仅 status v2
理由: S3 已落地契约编译;S4 是路线图最后阶段,完成 G1 闭环——行为化验收(N3)让架构师判断行为而非代码,status v2 五段式让架构师日常只看驾驶舱即可决策。verify 是核心:AC 的 verify 命令从声明式变机器执行,PASS/FAIL+输出指纹入 evidence/,done 门 = verify 全绿 或 架构师明示豁免(豁免本身是决策)。
后果: 引入 engine/scripts/engine-verify.{sh,ps1}(行为化验收器)、engine/evidence/T-NNN/AC-N.json(验收证据,generated-cache 不登记 ENGINE_MAP);engine CLI 加 verify 子命令;/engine-status 升级 v2(加决策队列+证据段);契约加 Behavior Verification Rule。verify 执行任务卡声明的命令(架构师批准任务卡时即批准 verify),用户主动跑(非 hook 自动),失败不阻断会话(只写 evidence+非零 exit)。
