# D-005 — v6 体系完善：分发完整性 + Doctor 编译检查（批次1）
> status: approved
> date: 2026-07-03
> expiry: none
> scope: install.sh, install.ps1, plugin/manifest.json, engine/scripts/engine-doctor.*, plugin/engine/scripts/engine-doctor.*, engine/ENGINE_MAP.md, engine/CONTEXT.md, engine/HANDOFF.md, engine/decisions/**, engine/tasks/**, tests/**, scripts/check.sh, scripts/check.ps1

选项: A 分批完善(选定) / B 一次性全做 / C 只做分发
理由: S0-S4 核心阶段已完成,但有机制闭环硬缺口:① S4 的 engine-verify 未入分发清单(install/manifest),用户 install 后无 verify 脚本;② S3 的编译机制 Doctor 不检查,dist 被手改时 Doctor 静默;③ Doctor bundled 清单不含 engine-verify。本批次补齐分发完整性 + Doctor 编译幂等检查,让机制真正闭环。S3-b 源模块细分 + D6 狗粮缺口留后续批次。
后果: install.sh/ps1 + manifest.json 加 engine-verify.{sh,ps1};Doctor bundled 清单加 engine-verify;Doctor 加 contract 编译幂等检查(compile(src)==dist,无 contract/ 时 fail-open 跳过)。分发完整后用户 install 即得 verify 能力;Doctor 编译检查让 dist 手改被检出(补 S3 的 Doctor 缺口)。
