# D-009 — v6 体系完善批次4：契约债计数器（N4）
> status: approved
> date: 2026-07-03
> expiry: none
> scope: engine/scripts/engine-doctor.*, plugin/engine/scripts/engine-doctor.*, contract/budget.json, engine/ENGINE_MAP.md, engine/CONTEXT.md, engine/HANDOFF.md, engine/decisions/**, engine/tasks/**

选项: A 契约债计数器(选定) / B 不做
理由: N4 是设计文档核心度量——「契约债计数器让倒立金字塔本身变成可追踪下降的数字」。Doctor 报告总 MUST 数 + 门禁 Rule 数 + 契约债(差值),与基线比,推动散文 MUST 移进数据表。完成 N4 最后一环。
后果: Doctor 加 check_contract_debt(报告 MUST/Rule/debt + 基线趋势);budget.json 加 debt_baseline;契约债 = 总 MUST - 门禁 Rule(粗略代理,目标是递减)。fail-open(无 contract 时跳过)。
