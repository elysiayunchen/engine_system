# D-011 — v6 中优先缺口：N1 注入计数器 + L0 SessionStart 注入 + flaky
> status: approved
> date: 2026-07-03
> expiry: none
> scope: engine/scripts/engine-hook-session-start.*, plugin/engine/scripts/engine-hook-session-start.*, scripts/check.sh, tests/**, engine/ENGINE_MAP.md, engine/CONTEXT.md, engine/HANDOFF.md, engine/decisions/**, engine/tasks/**

选项: A 修 3 中优先(选定) / B 只修 N1 / C 只修 L0
理由: review 暴露 3 中优先缺口:① N1 注入行数≤400 未机器强制(无计数器);② L0 宪法(runtime-law.md)未注入 SessionStart;③ check.sh 首次 A1 flaky(eol=lf 后应稳定,需确认)。一次性修复。
后果: SessionStart 加 L0 宪法注入(runtime-law.md 前 40 行);check.sh 加注入行数≤400 检查(N1 机器强制);跑 check 多次确认 flaky 消除。
