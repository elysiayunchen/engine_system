# D-015 — v6 验收缺口修复：init.md 纳入编译 + 16a 回补 + 版本语义化 + 迁移调度
> status: approved
> date: 2026-07-04
> expiry: none
> scope: contract/**, ENGINE_FILE_SYSTEM_v5.md, runtime-law.md, rules.json, plugin/.claude/commands/engine-init.md, VERSION, plugin/VERSION, engine/VERSION, engine/bin/**, plugin/bin/**, engine/scripts/**, plugin/engine/scripts/**, engine/migrations/**, plugin/migrations/**, .gitattributes, scripts/check.sh, scripts/check.ps1, tests/**, engine/tasks/**, engine/decisions/**, engine/changes/**, engine/evidence/**, engine/domains/**, engine/ENGINE_MAP.md, engine/CONTEXT.md, engine/HANDOFF.md, docs/**

选项: A 全量修复(选定) / B 只修高优先 G1+G2 / C 只修外部 bug
理由: 2026-07-04 对 v6 方向设计文档验收发现两处高优先回归:① plugin/.claude/commands/engine-init.md 脱离契约编译闭环——横幅 5.7.0 vs dist 6.0、v6 四 Rule 缺失,正是设计 D5 诊断的"同一契约两份实现漂移"病灶复发(§5.5 第 4 产物未落地);② S2 采访问题 16a(项目分几大块→联邦表域划分来源)在 S3 拆分时从 contract/src 丢失,唯一真相源缺 S2 特性。另采纳外部项目 agent 报告的 4 个 bug:histexpand 击穿 MARK_START 赋值(因果链:双引号内 !- 触发历史展开→赋值丢弃→set -u unbound)、VERSION 需语义化(6.0→6.0.0 + 归一化比较防伪更新提示)、迁移创建的 rules.json 基线缺 protected_paths 键(与 pre-commit 读取契约不齐)、run_migrate 无版本化调度(不发现 migrations/v*.sh、不按序应用、不回写 VERSION)。架构师会话直接授权落实(2026-07-04)。
后果: compile 产出 4 dist(新增 engine-init.md = cli-preamble + 同一模块;doctor/tests 幂等检查同步扩展);contract/src 新增 cli-preamble.md(不计入行数预算,同 L0-runtime-law 先例);16a 回补 10-interview.md,budget max_lines 2439→2441(回补丢失特性,非新增散文,依预算决策背书条款提升);00-core 头与 VERSION 三处统一 6.0.0;check-update 与 session-start 提示改归一化比较(6.0≡6.0.0);migrate/sync 脚本 MARK 改单引号 + set +H;迁移 rules.json 基线改 {"rules":[],"protected_paths":[]};run_migrate 版本化调度(发现 v*.sh → sort -V → 仅应用新于本地版本的步 → 每步成功回写 VERSION,无待应用步时回退幂等契约修复);新增 tests/update-flow 套件接入 check.sh。注意:T-013 AC-1 钉死 "6.0" 属历史快照不改写,VERSION 断言由 T-014 接管;老项目(6.0)因归一化不再收到 6.0.0 伪更新提示,若需推送本批修复应发 6.0.1。
