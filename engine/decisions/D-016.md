# D-016 — Doctor 占位符检测修复:剔除代码块与内联代码后再匹配
> status: approved
> date: 2026-07-04
> expiry: none
> scope: engine/scripts/engine-doctor.sh, engine/scripts/engine-doctor.ps1, plugin/engine/scripts/engine-doctor.sh, plugin/engine/scripts/engine-doctor.ps1, tests/doctor-parity/**, scripts/check.sh, engine/tasks/**, engine/decisions/**, engine/changes/**, engine/evidence/**, engine/CONTEXT.md, engine/HANDOFF.md

选项: A 剔除代码块+内联代码后检测(选定) / B 收紧正则仅排除空 [] / C 改 CHANGE 胶囊转义 JSON
理由: D-015 的 CHANGE-2026-07-04-01.md 在 Actual Changes 段用内联代码 `{"rules":[]}` 记录迁移基线变更,触发既有 check_change_capsule_semantics 的 `\[.*\]` 子串匹配,误报 "still contains placeholders"。根因是正则无法区分"模板占位符 [TBD]/[items]"与"合法 JSON 数组 []/markdown 链接 [text](url)"。占位符只出现在正文叙述中,JSON [] 必在代码块(```...``` )或内联代码(`...` )里,故检测前先剔除这两类片段,即可精准命中真占位符且不误伤数据。选项 B 仅排除空 [],仍会误报 [a,b] 与链接;选项 C 改数据迁就正则,违反"数据为准、正则迁就数据"。架构师授权修复(2026-07-04)。
后果: engine-doctor.{sh,ps1} 双树(×2 平台 = 4 文件)同步改动——sh 侧 sed 剔除代码块+内联代码后 grep,ps1 侧 -replace 剔除后 -match,正则 `\[.*\]|TBD|TODO` 不变;新增 tests/doctor-parity 回归套件(D1 内联 JSON []/D2 代码块 JSON []/D3 真 [TBD]/D4 sh-ps1 一致)接入 check.sh;不 bump VERSION(这是 D-015 补丁,并入 PR #8);双树字节一致(check.ps1 drift SHA256 校验通过)。
