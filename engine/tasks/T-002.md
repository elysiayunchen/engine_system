# TASK CARD — T-002
> status: done | lane: L1 | decision: D-002 | plan: — | domain: engine-runtime, project-meta

GOAL: 落地 v6 S2 分形记忆——联邦表(path-glob→domain)+域引擎目录+路由 read-gate+L2 装配+汇总协议+检索配方,并对本仓库自托管铺设 2 域补 D6 缺口。

WRITE-SET: engine/**, plugin/engine/scripts/**, plugin/.claude/commands/engine-init.md, ENGINE_FILE_SYSTEM_v5.md, scripts/check.sh, scripts/check.ps1, tests/**
FORBIDDEN: archive/**, plugin/bin/**, engine/bin/**, install.sh, install.ps1, plugin/manifest.json, .gitattributes, .git/**

AC: AC-1 联邦表存在且 hook 能解析 path→domain,无匹配落 default_domain → verify: bash tests/fractal-memory/run-fractal-tests.sh
AC: AC-2 SessionStart 按 active 任务卡 domain 装配 L2(域 CONTEXT+PITFALLS),无域/无任务卡时回退 S1 行为 → verify: bash tests/fractal-memory/run-fractal-tests.sh
AC: AC-3 Stop hook 校验 WRITE-SET 代码路径所属域 ⊆ 任务卡 domain 集合,越域=block → verify: bash tests/fractal-memory/run-fractal-tests.sh
AC: AC-4 汇总协议:域 CONTEXT 首行摘要提升到 SessionStart 注入的域仪表盘 → verify: bash tests/fractal-memory/run-fractal-tests.sh
AC: AC-5 INIT 采访含「项目分几大块」一问 → verify: grep -c "几大块" plugin/.claude/commands/engine-init.md
AC: AC-6 本仓库铺设 2 域(engine-runtime + project-meta),federation.json 登记完整 → verify: bash tests/fractal-memory/run-fractal-tests.sh
AC: AC-7 双实现等价 + 既有 parity/task-card 回归全绿 → verify: bash scripts/check.sh

CONSTRAINTS: 向后兼容(无域/无任务卡回退 S1/v5.6 行为);双实现 sh/ps1 判定等价;不新增独立可执行脚本(路由逻辑内联 hook);不破坏 S0/S1 既有测试 (source: D-001/D-002 + N1/N5)
<!-- exempt: v6 开发期任务卡, verify 机制 S4 后落地 (source: D-010) -->
