# TASK CARD — T-003
> status: done | lane: L1 | decision: D-003 | plan: — | domain: engine-runtime, project-meta

GOAL: 落地 v6 S3 契约编译——contract/src → compile → dist 幂等校验 + 减法规则(行数基线),让契约从「agent 背诵」变成「机器编译产物」。

WRITE-SET: contract/**, ENGINE_FILE_SYSTEM_v5.md, scripts/check.sh, scripts/check.ps1, tests/**, engine/**, plugin/engine/scripts/**
FORBIDDEN: archive/**, plugin/bin/**, engine/bin/**, install.sh, install.ps1, plugin/manifest.json, .gitattributes, .git/**

AC: AC-1 contract/src/ENGINE_FILE_SYSTEM.md 存在(契约源) → verify: test -f contract/src/ENGINE_FILE_SYSTEM.md
AC: AC-2 compile 脚本产出 dist(横幅 + src) → verify: bash contract/compile.sh && git diff --quiet ENGINE_FILE_SYSTEM_v5.md
AC: AC-3 编译幂等:compile(src) == dist → verify: bash tests/contract-compile/run-compile-tests.sh
AC: AC-4 减法规则:src 行数 ≤ budget.max_lines → verify: bash tests/contract-compile/run-compile-tests.sh
AC: AC-5 check 接入编译校验 → verify: bash scripts/check.sh
AC: AC-6 dist 头部含编译横幅 → verify: head -1 ENGINE_FILE_SYSTEM_v5.md | grep -q "compiled from contract/src"
CONSTRAINTS: 向后兼容(dist 内容除横幅外不变);compile 脚本零依赖(纯 sh+ps1);不破坏 S0/S1/S2 既有测试;src 是 dist 副本(本切片不细分模块,留 S3-b) (source: D-003 + N4/N5)
<!-- exempt: v6 开发期任务卡, verify 机制 S4 后落地 (source: D-010) -->
