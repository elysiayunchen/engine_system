# TASK CARD — T-004
> status: done | lane: L1 | decision: D-004 | plan: — | domain: engine-runtime, project-meta

GOAL: 落地 v6 S4 驾驶舱——engine verify T-NNN 行为化验收(AC verify 命令机器执行,PASS/FAIL+指纹入 evidence) + /engine-status v2(决策队列+证据段),完成 G1 闭环。

WRITE-SET: engine/**, plugin/engine/scripts/**, plugin/.claude/commands/engine-status.md, engine/bin/**, contract/**, ENGINE_FILE_SYSTEM_v5.md, scripts/check.sh, scripts/check.ps1, tests/**
FORBIDDEN: archive/**, install.sh, install.ps1, plugin/manifest.json, .gitattributes, .git/**

AC: AC-1 engine-verify 脚本存在(sh+ps1) → verify: test -f engine/scripts/engine-verify.sh -a -f engine/scripts/engine-verify.ps1
AC: AC-2 verify pass:AC verify 命令 exit 0 → evidence status=pass → verify: bash tests/behavior-verify/run-verify-tests.sh
AC: AC-3 verify fail:AC verify 命令 exit 1 → evidence status=fail → verify: bash tests/behavior-verify/run-verify-tests.sh
AC: AC-4 evidence JSON 格式正确(含 ac/verify/status/exit/fingerprint) → verify: bash tests/behavior-verify/run-verify-tests.sh
AC: AC-5 engine CLI verify 子命令工作 → verify: bash tests/behavior-verify/run-verify-tests.sh
AC: AC-6 /engine-status v2 含决策队列+证据段 → verify: grep -c "等你拍板" plugin/.claude/commands/engine-status.md
AC: AC-7 契约加 Behavior Verification Rule → verify: grep -c "Behavior Verification Rule" contract/src/ENGINE_FILE_SYSTEM.md
AC: AC-8 既有测试回归全绿 → verify: bash scripts/check.sh
CONSTRAINTS: verify 脚本零依赖(纯 sh+ps1,无 jq);执行 verify 命令容错(失败不 exit 脚本,只写 evidence+非零 exit);evidence 是 generated-cache 不登记 ENGINE_MAP;向后兼容 (source: D-004 + N3)
