# TASK CARD — T-005
> status: done | lane: L1 | decision: D-005 | plan: — | domain: engine-runtime, project-meta

GOAL: v6 体系完善批次1——分发完整性(engine-verify 入 install/manifest/install.ps1) + Doctor 编译幂等检查 + Doctor bundled 清单补 engine-verify。

WRITE-SET: install.sh, install.ps1, plugin/manifest.json, engine/scripts/engine-doctor.*, plugin/engine/scripts/engine-doctor.*, engine/**, tests/**, scripts/check.sh, scripts/check.ps1
FORBIDDEN: archive/**, .git/**, contract/**, ENGINE_FILE_SYSTEM_v5.md

AC: AC-1 install.sh 分发 engine-verify.{sh,ps1} → verify: grep -c "engine-verify" install.sh
AC: AC-2 install.ps1 分发 engine-verify.{sh,ps1} → verify: grep -c "engine-verify" install.ps1
AC: AC-3 manifest.json 含 engine-verify → verify: grep -c "engine-verify" plugin/manifest.json
AC: AC-4 Doctor bundled 清单含 engine-verify → verify: bash engine/scripts/engine-doctor.sh 2>&1 | grep -c "engine-verify"
AC: AC-5 Doctor 检查 contract 编译幂等(无 contract 时 fail-open) → verify: bash engine/scripts/engine-doctor.sh 2>&1 | grep -c "contract compile\|编译"
AC: AC-6 既有测试回归全绿 → verify: bash scripts/check.sh
CONSTRAINTS: 不破坏既有分发;Doctor 编译检查 fail-open(无 contract/ 时跳过);双实现等价;install 清单加在 engine-migrate-contract 后 githooks/pre-commit 前 (source: D-005 + N5)
<!-- exempt: v6 开发期任务卡, verify 机制 S4 后落地 (source: D-010) -->
