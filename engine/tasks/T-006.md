# TASK CARD — T-006
> status: done | lane: L1 | decision: D-006 | plan: — | domain: engine-runtime, project-meta

GOAL: v6 体系完善批次2a——pre-commit 加 done 任务卡 fallback(无 active 时用最新 done 的 decision) + 安装到 .git/hooks/ + 测试。

WRITE-SET: engine/scripts/githooks/pre-commit, plugin/engine/scripts/githooks/pre-commit, tests/**, engine/**, scripts/check.sh
FORBIDDEN: archive/**, .git/**, install.sh, install.ps1, plugin/manifest.json, .gitattributes, contract/**

AC: AC-1 pre-commit 加 done fallback → verify: grep -c "fallback" engine/scripts/githooks/pre-commit
AC: AC-2 .git/hooks/pre-commit 已安装 → verify: test -f .git/hooks/pre-commit
AC: AC-3 done fallback 场景测试通过(无 active + done 任务卡 decision 覆盖 → 通过) → verify: bash tests/task-card/run-task-tests.sh
AC: AC-4 done fallback scope 不覆盖 → 拦截 → verify: bash tests/task-card/run-task-tests.sh
AC: AC-5 既有测试回归全绿 → verify: bash scripts/check.sh
CONSTRAINTS: fallback 仍校验 scope(不覆盖则拦);安装不覆盖已有 hook(保护用户自定义);pre-commit 是 POSIX sh 无 ps1 孪生 (source: D-006 + N2)
<!-- exempt: v6 开发期任务卡, verify 机制 S4 后落地 (source: D-010) -->
