# TASK CARD — T-007
> status: done | lane: L1 | decision: D-007 | plan: — | domain: engine-runtime, project-meta

GOAL: v6 体系完善批次3——S3-b 源模块细分:contract/src/ 拆为 4 模块(00-core/10-interview/20-file-templates/30-operational),compile 拼接多模块 → dist,幂等校验。

WRITE-SET: contract/**, ENGINE_FILE_SYSTEM_v5.md, tests/**, scripts/check.sh, engine/**
FORBIDDEN: archive/**, .git/**, install.sh, install.ps1, plugin/manifest.json, .gitattributes, plugin/engine/scripts/**

AC: AC-1 contract/src/ 拆为 4 模块(00-core/10-interview/20-file-templates/30-operational) → verify: ls contract/src/*.md | wc -l
AC: AC-2 compile 拼接多模块 → dist → verify: bash contract/compile.sh && git diff --quiet ENGINE_FILE_SYSTEM_v5.md
AC: AC-3 编译幂等:compile(多模块) == dist → verify: bash tests/contract-compile/run-compile-tests.sh
AC: AC-4 既有测试回归全绿 → verify: bash scripts/check.sh
CONSTRAINTS: dist 内容零变化(幂等保证);compile 脚本零依赖;模块顺序固定(00/10/20/30) (source: D-007 + N4)
<!-- exempt: v6 开发期任务卡, verify 机制 S4 后落地 (source: D-010) -->
