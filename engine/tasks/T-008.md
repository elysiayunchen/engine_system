# TASK CARD — T-008
> status: done | lane: L1 | decision: D-008 | plan: — | domain: project-meta

GOAL: v6 体系完善批次2b——铺本仓库根锚点(AGENTS.md 正本 + CLAUDE.md import 引用),Session Protocol 浓缩 v6 六条强制流程,补齐 D6 根锚点缺口。

WRITE-SET: AGENTS.md, CLAUDE.md, engine/**
FORBIDDEN: archive/**, .git/**, install.sh, install.ps1, plugin/manifest.json, .gitattributes, contract/**, ENGINE_FILE_SYSTEM_v5.md, plugin/**

AC: AC-1 AGENTS.md 存在 → verify: test -f AGENTS.md
AC: AC-2 CLAUDE.md 存在且 import AGENTS.md → verify: grep -c "AGENTS.md" CLAUDE.md
AC: AC-3 AGENTS.md 含 Session Protocol → verify: grep -c "Session Protocol" AGENTS.md
AC: AC-4 AGENTS.md 指向 ENGINE_MAP + 契约 → verify: grep -c "ENGINE_MAP\|ENGINE_FILE_SYSTEM" AGENTS.md
AC: AC-5 既有测试回归全绿 → verify: bash scripts/check.sh
CONSTRAINTS: 薄引导器(权威规则在引擎文件不在锚点);CLAUDE.md import AGENTS.md;不覆盖用户全局 CLAUDE.md(项目级补充) (source: D-008 + D6)
<!-- exempt: v6 开发期任务卡, verify 机制 S4 后落地 (source: D-010) -->
