# TASK CARD — T-009
> status: done | lane: L1 | decision: D-009 | plan: — | domain: engine-runtime, project-meta

GOAL: v6 体系完善批次4——契约债计数器(N4):Doctor 报告总 MUST + 门禁 Rule + 契约债(差值) + 基线趋势,推动散文 MUST 移进数据表。

WRITE-SET: engine/scripts/engine-doctor.*, plugin/engine/scripts/engine-doctor.*, contract/budget.json, engine/**, tests/**
FORBIDDEN: archive/**, .git/**, install.sh, install.ps1, plugin/manifest.json, .gitattributes, contract/src/**, ENGINE_FILE_SYSTEM_v5.md

AC: AC-1 Doctor 加 check_contract_debt → verify: bash engine/scripts/engine-doctor.sh 2>&1 | grep -c "contract debt"
AC: AC-2 budget.json 含 debt_baseline → verify: grep -c "debt_baseline" contract/budget.json
AC: AC-3 契约债 ≤ 基线 → verify: bash engine/scripts/engine-doctor.sh 2>&1 | grep -c "debt.*baseline\|baseline.*debt"
AC: AC-4 既有测试回归全绿 → verify: bash scripts/check.sh
CONSTRAINTS: 契约债 = 总 MUST - 门禁 Rule(粗略代理);fail-open(无 contract 时跳过);双实现等价 (source: D-009 + N4)
<!-- exempt: v6 开发期任务卡, verify 机制 S4 后落地 (source: D-010) -->
