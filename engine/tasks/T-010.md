# TASK CARD — T-010
> status: done | lane: L1 | decision: D-010 | plan: — | domain: engine-runtime, project-meta

GOAL: v6 review 缺口修复——3 高优先:dist 行尾根因 + N3 done-gate 机器强制 + dist 多产物(runtime-law.md + rules.json)。

WRITE-SET: .gitattributes, contract/**, ENGINE_FILE_SYSTEM_v5.md, runtime-law.md, rules.json, engine/scripts/engine-doctor.*, plugin/engine/scripts/engine-doctor.*, tests/**, engine/**
FORBIDDEN: archive/**, .git/**, install.sh, install.ps1, plugin/manifest.json

AC: AC-1 .gitattributes 钉 dist/src eol=lf → verify: grep -c "ENGINE_FILE_SYSTEM_v5.md" .gitattributes
AC: AC-2 dist 行尾不再噪音(compile 后 git status 干净) → verify: bash contract/compile.sh && git diff --quiet ENGINE_FILE_SYSTEM_v5.md
AC: AC-3 Doctor done-gate:done 任务卡无 evidence 无 exempt → fail → verify: bash tests/task-card/run-task-tests.sh
AC: AC-4 compile 产出 runtime-law.md → verify: test -f runtime-law.md
AC: AC-5 compile 产出 rules.json → verify: test -f rules.json
AC: AC-6 既有测试回归全绿 → verify: bash scripts/check.sh
CONSTRAINTS: 不破坏既有;双实现等价;exempt 豁免=任务卡标记 (source: D-010 + N3 + §5.5)
