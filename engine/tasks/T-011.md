# TASK CARD — T-011
> status: done | lane: L1 | decision: D-011 | plan: — | domain: engine-runtime, project-meta

GOAL: v6 中优先缺口修复——N1 注入行数计数器(≤400 机器强制) + L0 宪法 SessionStart 注入(runtime-law.md) + flaky 确认。

WRITE-SET: engine/scripts/engine-hook-session-start.*, plugin/engine/scripts/engine-hook-session-start.*, scripts/check.sh, tests/**, engine/**
FORBIDDEN: archive/**, .git/**, install.sh, install.ps1, plugin/manifest.json, .gitattributes, contract/**, ENGINE_FILE_SYSTEM_v5.md

AC: AC-1 SessionStart 注入 L0 宪法(runtime-law.md) → verify: CLAUDE_PROJECT_DIR="$PWD" bash plugin/engine/scripts/engine-hook-session-start.sh 2>/dev/null | grep -c "L0 宪法"
AC: AC-2 check.sh 加注入行数≤400 检查 → verify: bash scripts/check.sh 2>&1 | grep -c "injection.*400\|N1"
AC: AC-3 注入行数≤400 → verify: bash scripts/check.sh
AC: AC-4 check 多次稳定(无 flaky) → verify: for i in 1 2 3; do bash scripts/check.sh 2>&1 | grep -c "CHECK PASSED"; done
CONSTRAINTS: L0 注入 fail-open(无 runtime-law.md 跳过);双实现等价;不破坏既有 (source: D-011 + N1 + §5.1)
