# TASK CARD — T-013
> status: done | lane: L1 | decision: D-014 | plan: — | domain: engine-runtime

GOAL: 引擎自动检测版本更新 + 老项目完整更迭到 v6 所有机制 + 远程拉取自动更新闭环。

WRITE-SET: VERSION, plugin/VERSION, install.sh, install.ps1, plugin/bin/engine, plugin/bin/engine.ps1, plugin/engine/scripts/engine-check-update.sh, plugin/engine/scripts/engine-check-update.ps1, plugin/engine/scripts/engine-migrate-contract.sh, plugin/engine/scripts/engine-migrate-contract.ps1, plugin/migrations/**, plugin/engine/scripts/engine-hook-session-start.sh, plugin/engine/scripts/engine-hook-session-start.ps1, plugin/.claude/commands/engine-sync.md, plugin/manifest.json, engine/scripts/**, plugin/AGENTS.md, ENGINE_FILE_SYSTEM_v5.md, plugin/engine/ENGINE_DOCTOR.md, scripts/check.sh, scripts/check.ps1, engine/tasks/T-013.md, engine/decisions/D-014.md, engine/changes/**
FORBIDDEN: archive/**, .git/**, contract/src/**, contract/compile.sh, contract/compile.ps1, contract/budget.json, engine/tasks/T-001..T-012.md, engine/decisions/D-001..D-013.md

AC: AC-1 仓库根 VERSION + plugin/VERSION 存在且为 6.0 → verify: test "$(cat VERSION)" = "6.0" && test "$(cat plugin/VERSION)" = "6.0"
AC: AC-2 engine check-update 子命令存在且返回合规 exit code → verify: bash plugin/bin/engine check-update; ec=$?; [ $ec -eq 0 -o $ec -eq 7 -o $ec -eq 8 ]
AC: AC-3 migrator 创建 v6 数据层目录 → verify: tmp=$(mktemp -d); mkdir -p "$tmp/engine"; echo "# stub" > "$tmp/engine/ENGINE_MAP.md"; ENGINE_DIR="$tmp/engine" bash plugin/engine/scripts/engine-migrate-contract.sh "$tmp" >/dev/null 2>&1; test -d "$tmp/engine/tasks" && test -d "$tmp/engine/decisions" && test -d "$tmp/engine/domains" && test -d "$tmp/engine/changes" && test -d "$tmp/engine/evidence"; rm -rf "$tmp"
AC: AC-4 migrator 写入 federation.json → verify: tmp=$(mktemp -d); mkdir -p "$tmp/engine"; echo "# stub" > "$tmp/engine/ENGINE_MAP.md"; bash plugin/engine/scripts/engine-migrate-contract.sh "$tmp" >/dev/null 2>&1; test -f "$tmp/engine/domains/federation.json"; rm -rf "$tmp"
AC: AC-5 migrator contract-version 标记 6.0 → verify: tmp=$(mktemp -d); mkdir -p "$tmp/engine"; echo "# stub" > "$tmp/engine/ENGINE_MAP.md"; bash plugin/engine/scripts/engine-migrate-contract.sh "$tmp" >/dev/null 2>&1; grep -q "contract-version: 6.0" "$tmp/AGENTS.md"; rm -rf "$tmp"
AC: AC-6 engine update --check-only 选项 → verify: bash plugin/bin/engine update --check-only 2>&1 | grep -qi "check\|version\|update"
AC: AC-7 session-start 生成更新检测缓存 → verify: bash plugin/engine/scripts/engine-hook-session-start.sh 2>/dev/null >/dev/null; test -f engine/.cache/update-check.json || true
AC: AC-8 双树镜像 parity 一致 → verify: bash scripts/check.sh 2>&1 | grep -i "parity" | grep -iq pass
AC: AC-9 Doctor 含 VERSION check → verify: grep -qi "version" engine/scripts/engine-doctor.sh
AC: AC-10 check.sh 全绿 → verify: bash scripts/check.sh
CONSTRAINTS: fail-open(网络错不阻塞 session);幂等(migrator 可重跑);版本号 6.0 (source: D-013/D-014);不破坏现有用户 engine/*.md 记忆;双树同步 plugin/ 与 engine/scripts/
<!-- evidence: engine/evidence/T-013/ (10 AC pass, verify 2026-07-03); capsule: engine/changes/CHANGE-2026-07-03-14.md; decision: D-014 -->
