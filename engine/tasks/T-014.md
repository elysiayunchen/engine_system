# TASK CARD — T-014
> status: done | lane: main | decision: D-015 | plan: — | domain: engine-runtime,project-meta,root

GOAL: 落实 v6 验收缺口(G1 init.md 纳入编译 + G2 16a 回补 + G6 文档收尾)与外部报告 4 bug(histexpand / VERSION 语义化 / rules.json 契约对齐 / migrations 版本化调度)。
WRITE-SET: contract/**, ENGINE_FILE_SYSTEM_v5.md, runtime-law.md, rules.json, plugin/.claude/commands/engine-init.md, VERSION, plugin/VERSION, engine/VERSION, engine/bin/**, plugin/bin/**, engine/scripts/**, plugin/engine/scripts/**, engine/migrations/**, plugin/migrations/**, .gitattributes, scripts/check.sh, scripts/check.ps1, tests/**, engine/tasks/**, engine/decisions/**, engine/changes/**, engine/evidence/**, engine/domains/**, engine/ENGINE_MAP.md, engine/CONTEXT.md, engine/HANDOFF.md, docs/**
FORBIDDEN: archive/**, .git/**
AC: AC-1 init.md 由编译产出且含 v6 四 Rule → verify: test "$(grep -cE 'Rule \(v6 S[0-9]' plugin/.claude/commands/engine-init.md)" -ge 4
AC: AC-2 dist 与 init.md 版本横幅一致 6.0.0 → verify: grep -q "^# Version: 6.0.0 " ENGINE_FILE_SYSTEM_v5.md && grep -q "^# Version: 6.0.0 " plugin/.claude/commands/engine-init.md
AC: AC-3 16a 采访问题在唯一真相源与两个 dist → verify: grep -q "16a\." contract/src/10-interview.md && grep -q "16a\." ENGINE_FILE_SYSTEM_v5.md && grep -q "16a\." plugin/.claude/commands/engine-init.md
AC: AC-4 VERSION 语义化 6.0.0 三处一致 → verify: test "$(cat VERSION)" = "6.0.0" && test "$(cat plugin/VERSION)" = "6.0.0" && test "$(cat engine/VERSION)" = "6.0.0"
AC: AC-5 迁移 rules.json 基线含 protected_paths → verify: tmp=$(mktemp -d); mkdir -p "$tmp/engine"; echo "# stub" > "$tmp/engine/ENGINE_MAP.md"; bash plugin/engine/scripts/engine-migrate-contract.sh "$tmp" >/dev/null 2>&1 && grep -q protected_paths "$tmp/engine/decisions/rules.json" && rm -rf "$tmp"
AC: AC-6 契约编译幂等(4 dist 含 init.md) → verify: bash tests/contract-compile/run-compile-tests.sh
AC: AC-7 update-flow 套件(归一化/调度/回写/histexpand 防御) → verify: bash tests/update-flow/run-update-tests.sh
AC: AC-8 check.sh 全绿 → verify: bash scripts/check.sh
CONSTRAINTS: 双树镜像 engine/ ⇄ plugin/ 字节一致(drift check);sh/ps1 判定一致;fail-open(网络错不阻塞);不改写历史任务卡与证据 (source: D-015)
<!-- evidence: engine/evidence/T-014/ (8 AC pass, verify 2026-07-04); capsule: engine/changes/CHANGE-2026-07-04-01.md; decision: D-015 -->
