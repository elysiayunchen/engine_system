# TASK CARD — T-015
> status: done | lane: main | decision: D-016 | plan: — | domain: engine-runtime

GOAL: 修复 Doctor check_change_capsule_semantics 占位符正则误报——D-015 的 CHANGE 胶囊内联代码 `{"rules":[]}` 中的 JSON [] 被 `\[.*\]` 当作占位符。改为剔除代码块(```...``` )与内联代码(`...` )后再检测。
WRITE-SET: engine/scripts/engine-doctor.sh, engine/scripts/engine-doctor.ps1, plugin/engine/scripts/engine-doctor.sh, plugin/engine/scripts/engine-doctor.ps1, tests/doctor-parity/**, scripts/check.sh, engine/tasks/**, engine/decisions/**, engine/changes/**, engine/evidence/**, engine/CONTEXT.md, engine/HANDOFF.md
FORBIDDEN: archive/**, .git/**, contract/**, ENGINE_FILE_SYSTEM_v5.md, plugin/.claude/commands/engine-init.md, VERSION, plugin/VERSION, engine/VERSION, install.sh, install.ps1, plugin/manifest.json, .gitattributes
AC: AC-1 双树 doctor 同步修复且字节一致 → verify: [ "$(sha256sum engine/scripts/engine-doctor.sh | cut -d' ' -f1)" = "$(sha256sum plugin/engine/scripts/engine-doctor.sh | cut -d' ' -f1)" ] && [ "$(sha256sum engine/scripts/engine-doctor.ps1 | cut -d' ' -f1)" = "$(sha256sum plugin/engine/scripts/engine-doctor.ps1 | cut -d' ' -f1)" ]
AC: AC-2 真实 CHANGE 胶囊(含内联 JSON [])无误报 → verify: test "$(bash engine/scripts/engine-doctor.sh . 2>&1 | grep -c 'CHANGE-2026-07-04-01.md still contains placeholders')" -eq 0
AC: AC-3 [TBD] 占位符仍检出 + 回归套件全绿 → verify: bash tests/doctor-parity/run-doctor-tests.sh
AC: AC-4 check.sh 全绿 → verify: bash scripts/check.sh
CONSTRAINTS: 双树镜像 engine/ ⇄ plugin/ 字节一致(drift check);sh/ps1 判定一致;不 bump VERSION(D-015 补丁,并入 PR #8);不改契约 dist/src (source: D-016)
<!-- evidence: engine/evidence/T-015/ (4 AC pass, verify 2026-07-04); capsule: engine/changes/CHANGE-2026-07-04-02.md; decision: D-016 -->
