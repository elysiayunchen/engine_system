# TASK CARD — T-016
> status: done | lane: main | decision: D-017 | plan: — | domain: engine-runtime,root

GOAL: GitHub Release 自动化 workflow——tag push 触发，从 change capsules 生成 release notes，打包 plugin/ 为 tarball + zip，生成 SHA256 checksums，挂到 GitHub Release。消除手动 `gh release create`。
WRITE-SET: .github/workflows/**, scripts/release.sh, engine/tasks/T-016.md, engine/decisions/**, engine/changes/**, engine/evidence/**, engine/CONTEXT.md, engine/HANDOFF.md
FORBIDDEN: archive/**, .git/**, contract/src/**, plugin/engine/scripts/**, plugin/.claude/**
AC: AC-1 release workflow 文件存在且语法正确 → verify: test -f .github/workflows/release.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml', encoding='utf-8'))"
AC: AC-2 workflow 在 tag push (v*) 时触发 → verify: grep -q "tags:" .github/workflows/release.yml && grep -q "v\*" .github/workflows/release.yml
AC: AC-3 release notes 从 change capsules 生成 → verify: grep -q "CHANGE-\*.md" .github/workflows/release.yml || grep -q "changelog" .github/workflows/release.yml
AC: AC-4 产物含 plugin/ tarball + checksums → verify: grep -q "tar" .github/workflows/release.yml && grep -q "sha256" .github/workflows/release.yml
AC: AC-5 check.sh 全绿 → verify: bash scripts/check.sh
CONSTRAINTS: 不修改 CI workflow (ci.yml);release.sh 可被 workflow 调用也可独立运行;checksums 文件格式兼容 sha256sum 验证 (source: D-017)
<!-- evidence: engine/evidence/T-016/ (5 AC pass, verify 2026-07-05); capsule: engine/changes/CHANGE-2026-07-05-02.md; decision: D-017 -->
