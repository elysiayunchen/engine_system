# TASK CARD — T-017
> status: done | lane: main | decision: D-017 | plan: — | domain: engine-runtime,root

GOAL: 版本锁定安装（install.sh --version TAG 支持安装特定版本）+ 完整性校验（manifest.json 加 sha256 字段，installer 下载后校验）。优先从 GitHub Release artifact 下载，fallback 到 raw content。
WRITE-SET: install.sh, install.ps1, plugin/manifest.json, scripts/check.sh, scripts/check.ps1, engine/tasks/T-017.md, engine/decisions/**, engine/changes/**, engine/evidence/**, engine/CONTEXT.md, engine/HANDOFF.md
FORBIDDEN: archive/**, .git/**, contract/src/**, plugin/engine/scripts/**, plugin/.claude/**, plugin/bin/**
AC: AC-1 install.sh 支持 --version 参数 → verify: bash install.sh --help 2>&1 | grep -qi version || grep -q "\-\-version" install.sh
AC: AC-2 指定版本时从 release download URL 下载 → verify: grep -q "releases/download" install.sh
AC: AC-3 manifest.json 含 sha256 字段 → verify: python3 -c "import json; d=json.load(open('plugin/manifest.json')); assert any('sha256' in e for e in d.get('files',d.get('entries',[])))"
AC: AC-4 installer 下载后校验 sha256 → verify: grep -q "sha256" install.sh && grep -q "checksum\|sha256sum\|shasum" install.sh
AC: AC-5 fallback 到 raw content（release 不可用时） → verify: grep -q "raw.githubusercontent\|fallback" install.sh
AC: AC-6 install.ps1 支持 -Version 参数 → verify: grep -q "\-Version\|Version" install.ps1 | head -1; grep -q "releases/download" install.ps1
AC: AC-7 check.sh 全绿 → verify: bash scripts/check.sh
CONSTRAINTS: 不改变现有默认行为（无参数时仍从 main 拉取）;sh/ps1 校验逻辑一致;sha256 值由 T-016 release workflow 生成 (source: D-017)
<!-- evidence: engine/evidence/T-017/ (7 AC pass, verify 2026-07-05); capsule: engine/changes/CHANGE-2026-07-05-03.md; decision: D-017 -->
