# TASK CARD — T-018
> status: active | lane: main | decision: D-017 | plan: — | domain: engine-runtime,root

GOAL: 离线安装包——release workflow 产出 `engine-system-vX.Y.Z-offline.tar.gz`（含 plugin/ 完整树 + install 脚本），install.sh 支持 `--local PATH` 模式从本地 tarball 安装（不联网）。
WRITE-SET: install.sh, install.ps1, scripts/release.sh, .github/workflows/release.yml, engine/tasks/T-018.md, engine/decisions/**, engine/changes/**, engine/evidence/**, engine/CONTEXT.md, engine/HANDOFF.md
FORBIDDEN: archive/**, .git/**, contract/src/**, plugin/engine/scripts/**, plugin/.claude/**
AC: AC-1 release workflow 产出 offline tarball artifact → verify: grep -q "offline" .github/workflows/release.yml && grep -q "tar" .github/workflows/release.yml
AC: AC-2 tarball 含完整 plugin/ 树 → verify: grep -q "plugin" .github/workflows/release.yml
AC: AC-3 install.sh 支持 --local 参数 → verify: grep -q "\-\-local" install.sh
AC: AC-4 --local 模式不联网 → verify: grep -q "local\|offline" install.sh && ! grep -q "curl\|wget" install.sh | head -1; true
AC: AC-5 install.ps1 支持 -Local 参数 → verify: grep -q "\-Local\|Local" install.ps1 | head -1; true
AC: AC-6 check.sh 全绿 → verify: bash scripts/check.sh
CONSTRAINTS: tarball 命名格式 engine-system-vX.Y.Z-offline.tar.gz;离线安装结果与在线安装一致(同文件集);tarball 大小合理(<5MB);依赖 T-016 release workflow (source: D-017)
