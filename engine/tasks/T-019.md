# TASK CARD — T-019

> status: done | lane: main | decision: D-017 | plan: — | domain: engine-runtime,root

GOAL: init prompt 增加 agent 中立分发形态(engine/prompts/init.md 正本 + plugin 镜像 + manifest/install 分发)+ `engine init` 终端子命令(指引 + --print)+ bin 副本纳入 compile 同步(D-018a/b/e)。

WRITE-SET: contract/src/agent-preamble.md, contract/compile.sh, contract/compile.ps1, engine/bin/**, plugin/bin/**, engine/prompts/**, plugin/engine/prompts/**, plugin/engine/scripts/**, plugin/manifest.json, install.sh, install.ps1, engine/tasks/**, engine/decisions/**, engine/changes/**, engine/evidence/**, engine/CONTEXT.md, engine/HANDOFF.md, engine/ENGINE_MAP.md

FORBIDDEN: contract/src/[0-9]*.md, contract/src/L0-runtime-law.md, contract/src/cli-preamble.md, archive/**, .git/**

AC: AC-1 compile 产出 agent 中立 dist 且 plugin 镜像零漂移 → verify: test -f engine/prompts/init.md && git diff --no-index --quiet engine/prompts/init.md plugin/engine/prompts/init.md
AC: AC-2 dist 含 agent 中立前言与完整契约正文 → verify: grep -q "AGENT-NEUTRAL EXECUTION CONTEXT" engine/prompts/init.md && grep -q "ENGINE FILE SYSTEM" engine/prompts/init.md
AC: AC-3 dist 不含 Claude Code 专属前言 → verify: grep -c "CLAUDE CODE EXECUTION CONTEXT" engine/prompts/init.md | grep -qx 0
AC: AC-4 engine init --print 输出契约正文可供管道 → verify: bash engine/bin/engine init --print | grep -q "ENGINE FILE SYSTEM"
AC: AC-5 engine init 默认指引提示 prompt 路径 → verify: bash engine/bin/engine init | grep -q "engine/prompts/init.md"
AC: AC-6 bash 与 ps1 孪生的 help 均纳入 init → verify: grep -q "engine init" engine/bin/engine && grep -q "engine init" engine/bin/engine.ps1
AC: AC-7 bin 纳入 compile 同步且当前零漂移 → verify: grep -q "plugin/bin" contract/compile.sh && grep -q "plugin/bin" contract/compile.ps1 && git diff --no-index --quiet engine/bin/engine plugin/bin/engine
AC: AC-8 分发三件套(manifest+install.sh+install.ps1)均含 prompt dist → verify: grep -q "engine/prompts/init.md" plugin/manifest.json && grep -q "engine/prompts/init.md" install.sh && grep -q "engine/prompts/init.md" install.ps1
AC: AC-9 全仓健康门全绿 → verify: bash scripts/check.sh

CONSTRAINTS: 契约正文(contract/src/[0-9]*.md 与 L0)一字不改,本卡只改分发形态;engine init 只打印零写入,缺 prompt 文件时指引 engine update,ENGINE_MAP 已存在时提示已初始化但不中断;agent-preamble 须与 cli-preamble 结构对齐且不含任何 Claude Code 专属工具名 (source: D-018)

<!-- decision: D-018(细化设计,proposed);门禁引用 D-017(approved,scope 覆盖本卡全部受保护路径) -->
