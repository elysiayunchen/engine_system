# TASK CARD — T-020

> status: paused | lane: main | decision: D-017 | plan: — | domain: engine-runtime,root

GOAL: agent 检测器孪生脚本(engine-detect-agents.{sh,ps1})+ `engine init` 与 installer 收尾指引按检测结果个性化(D-018c)。

WRITE-SET: engine/scripts/engine-detect-agents.sh, engine/scripts/engine-detect-agents.ps1, contract/compile.sh, contract/compile.ps1, engine/bin/**, plugin/bin/**, plugin/engine/scripts/**, plugin/manifest.json, install.sh, install.ps1, engine/tasks/**, engine/changes/**, engine/evidence/**, engine/CONTEXT.md, engine/HANDOFF.md, engine/ENGINE_MAP.md

FORBIDDEN: contract/src/[0-9]*.md, contract/src/L0-runtime-law.md, contract/src/cli-preamble.md, contract/src/agent-preamble.md, archive/**, .git/**

AC: AC-1 检测器孪生存在且纳入 compile 同步清单 → verify: test -f engine/scripts/engine-detect-agents.sh && test -f engine/scripts/engine-detect-agents.ps1 && grep -q "engine-detect-agents" contract/compile.sh
AC: AC-2 检测器在本仓输出 claude-code(存在 .claude/)→ verify: bash engine/scripts/engine-detect-agents.sh | grep -qx "claude-code"
AC: AC-3 engine init 指引消费检测结果 → verify: bash engine/bin/engine init | grep -q "claude"
AC: AC-4 分发三件套均含检测器 → verify: grep -q "engine-detect-agents" plugin/manifest.json && grep -q "engine-detect-agents" install.sh && grep -q "engine-detect-agents" install.ps1
AC: AC-5 全仓健康门全绿 → verify: bash scripts/check.sh

CONSTRAINTS: 检测输出契约 = 一行一个 agent 标识(小写-连字符),无匹配输出为空且 exit 0;sh/ps1 孪生语义一致;installer 在铺完文件后才调用检测器(此时脚本已落盘) (source: D-018)

<!-- decision: D-018c;依赖 T-019(init 命令先存在);门禁引用 D-017 -->
