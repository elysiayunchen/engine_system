# TASK CARD — T-021

> status: paused | lane: main | decision: D-017 | plan: — | domain: root

GOAL: 快速安装骨架:plugin/engine/skeleton/ 三个自描述空模板(ENGINE_MAP/CONTEXT/HANDOFF)经既有 FILES/protect 机制分发,装完即见结构并被引导跑 engine init(D-018d)。

WRITE-SET: plugin/engine/skeleton/**, plugin/manifest.json, install.sh, install.ps1, engine/tasks/**, engine/changes/**, engine/evidence/**, engine/CONTEXT.md, engine/HANDOFF.md, engine/ENGINE_MAP.md

FORBIDDEN: contract/**, engine/bin/**, plugin/bin/**, archive/**, .git/**

AC: AC-1 三模板存在且 ENGINE_MAP 骨架自述指向 init → verify: grep -q "engine init" plugin/engine/skeleton/ENGINE_MAP.md && test -f plugin/engine/skeleton/CONTEXT.md && test -f plugin/engine/skeleton/HANDOFF.md
AC: AC-2 manifest 含三条骨架映射 → verify: grep -q "skeleton/ENGINE_MAP.md" plugin/manifest.json && grep -q "skeleton/CONTEXT.md" plugin/manifest.json && grep -q "skeleton/HANDOFF.md" plugin/manifest.json
AC: AC-3 两个 installer 均含三条骨架条目 → verify: grep -q "skeleton/ENGINE_MAP.md" install.sh && grep -q "skeleton/ENGINE_MAP.md" install.ps1
AC: AC-4 全仓健康门全绿 → verify: bash scripts/check.sh

CONSTRAINTS: 骨架 dest 为 engine/{ENGINE_MAP,CONTEXT,HANDOFF}.md 且 protect=false(fresh 缺失才铺、update 永不覆盖用户记忆,复用既有 skip 规则,不新增安装器逻辑分支);骨架内容为空模板 + 一句「跑 engine init 或 /engine-init 填充」,不得包含任何伪造的项目状态 (source: D-018)

<!-- decision: D-018d;门禁引用 D-017 -->
