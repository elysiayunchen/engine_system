# TASK CARD — T-022

> status: active | lane: main | decision: D-019 | plan: — | domain: engine-runtime,project-meta,root

GOAL: D-019a 防护前置批(P0):① 编译产物漂移检测全覆盖(含此前零检测的根部 runtime-law.md 与 rules.json、engine/prompts/init.md 幂等、plugin 镜像与 bin 副本)+ tests/dist-drift 套件;② protected_paths 补 contract/src/** 与 runtime-law.md(含 migrator 基线);③ manifest sha256 去空转(compile 回填 + install 硬校验 + .gitattributes eol 钉齐 + check.sh 哈希门 + tests/install-flow);④ engine-verify 孪生修复(ps1 param 启动 bug + 真 Git Bash 语义 + sh rc 陈旧值 bug)+ tests/behavior-verify 双侧夹具。实施计划:.superpowers/sdd/t022-plan.md(scratch,不入库)。

WRITE-SET: contract/compile.sh, contract/compile.ps1, scripts/check.sh, scripts/check.ps1, scripts/release.sh, .github/**, install.sh, install.ps1, plugin/manifest.json, .gitattributes, .gitignore, .claude/**, CLAUDE.md, AGENTS.md, engine/scripts/**, plugin/engine/scripts/**, engine/bin/**, plugin/bin/**, plugin/CLAUDE.md, plugin/AGENTS.md, plugin/.claude/**, plugin/engine/**, tests/**, ENGINE_FILE_SYSTEM_v5.md, runtime-law.md, rules.json, engine/prompts/**, engine/decisions/**, engine/domains/**, engine/migrations/**, engine/tasks/**, engine/changes/**, engine/evidence/**, engine/CONTEXT.md, engine/HANDOFF.md, engine/ENGINE_MAP.md, .superpowers/**

FORBIDDEN: contract/src/**, archive/**, .git/**, VERSION, engine/VERSION, plugin/VERSION

AC: AC-1 dist 漂移套件全绿(幂等/镜像/篡改负用例) → verify: bash tests/dist-drift/run-dist-drift-tests.sh
AC: AC-2 protected_paths 含 contract/src 与 runtime-law → verify: grep -q 'contract/src/\*\*' engine/decisions/rules.json && grep -q 'runtime-law.md' engine/decisions/rules.json
AC: AC-3 manifest 40 条全量真哈希零占位 → verify: ! grep -q placeholder plugin/manifest.json && test "$(grep -c '"sha256"' plugin/manifest.json)" = "$(grep -c '"src"' plugin/manifest.json)"
AC: AC-4 install 校验硬失败与兼容路径可证 → verify: bash tests/install-flow/run-install-tests.sh
AC: AC-5 verify 孪生双侧夹具全绿(含先fail后pass顺序用例) → verify: bash tests/behavior-verify/run-verify-tests.sh
AC: AC-6 仓库健康门禁全绿 → verify: bash scripts/check.sh

CONSTRAINTS: engine/scripts 与 engine/bin 为正本,plugin 镜像只经 bash contract/compile.sh 同步;不改契约正文;不 bump VERSION;测试套件遵循既有 bash runner + set -u + 计数器惯例;ps1 侧断言走 PS_BIN 探测(hook-parity 范式),探测不到 SKIP。
