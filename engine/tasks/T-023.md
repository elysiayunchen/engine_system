# TASK CARD — T-023

> status: done | lane: main | decision: D-019 | plan: — | domain: engine-runtime,project-meta,root

GOAL: D-019 P1 行为技能层 + 行为路由表:从 contract/src/behaviors 单源生成 Claude Code skills 与 agent-neutral prompts,经 manifest/install 分发到其他项目;同时把“发布可用性优先”写入引擎开发准则。

WRITE-SET: contract/src/behaviors/**, contract/compile.sh, contract/compile.ps1, engine/prompts/behaviors/**, plugin/.claude/skills/**, plugin/engine/prompts/behaviors/**, engine/domains/routing.json, plugin/engine/domains/routing.json, engine/domains/federation.json, plugin/manifest.json, install.sh, install.ps1, scripts/check.sh, scripts/check.ps1, tests/**, engine/SYSTEM.md, engine/ENGINE_DOCTOR.md, engine/tasks/**, engine/changes/**, engine/evidence/**, engine/CONTEXT.md, engine/HANDOFF.md, engine/ENGINE_MAP.md

FORBIDDEN: contract/src/[0-9]*.md, contract/src/L0-runtime-law.md, contract/src/cli-preamble.md, contract/src/agent-preamble.md, archive/**, .git/**, VERSION, engine/VERSION, plugin/VERSION

AC: AC-1 五个行为技能源与 Claude Code skill dist 存在 → verify: test -f contract/src/behaviors/task-run.md && test -f plugin/.claude/skills/engine-task-run/SKILL.md && test -f plugin/.claude/skills/engine-handoff/SKILL.md
AC: AC-2 agent-neutral 行为 prompts 与 routing.json 存在 → verify: test -f engine/prompts/behaviors/task-run.md && test -f plugin/engine/prompts/behaviors/task-run.md && grep -q '"engine-task-run"' engine/domains/routing.json
AC: AC-3 分发清单与两个 installer 覆盖 skills/prompts/routing → verify: grep -q '.claude/skills/engine-task-run/SKILL.md' plugin/manifest.json && grep -q 'engine/prompts/behaviors/task-run.md' install.sh && grep -q 'engine/domains/routing.json' install.ps1
AC: AC-4 行为技能专项测试全绿 → verify: bash tests/behavior-skills/run-behavior-skills-tests.sh
AC: AC-5 全仓健康门全绿 → verify: bash scripts/check.sh

CONSTRAINTS: 技能是引擎的编译输出和体验增强,不是新的最高权威层;每条 MUST 必须绑定已有/本轮机器查点或标注为自觉项;所有新运行时产物必须通过 plugin/manifest.json + install.sh + install.ps1 分发,并能安装到隔离目录中的非本仓项目。
