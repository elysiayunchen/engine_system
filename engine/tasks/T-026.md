# T-026: Doctor 补字节上限 + 单行宽度上限检查

> status: done
> exempt: manual verification (AC-1 to AC-7 passed via study_engine fixture + source repo self-check + check.sh; evidence is terminal logs, not JSON)
> lane: main
> decision: D-022
> domain: engine-runtime

## GOAL

Doctor 现有行数上限(budget_cap)无法检测"单行膨胀"型异常:诺识项目 CONTEXT.md 仅 145 行但 317KB,状态面板表格单元格被空格填充到 19600 字符/行,分隔线 57735 字符,Doctor 报 0 failure。补字节上限 + 单行宽度上限两个检查,使这类静默膨胀能被检测和报告。

## WRITE-SET

- engine/scripts/engine-doctor.sh
- engine/scripts/engine-doctor.ps1

## FORBIDDEN

- contract/** (不碰契约)
- engine/decisions/** (不碰决策台账,除新建 D-022)
- engine/tasks/** (不碰其他任务卡)
- install.sh, install.ps1 (不碰安装逻辑)
- plugin/manifest.json (不碰分发清单,compile 自动同步)
- engine/migrations/** (不碰迁移 step)
- engine/ENGINE_MAP.md, engine/SYSTEM.md, engine/ENGINE_DOCTOR.md (不碰管理层文件)

## AC

| # | Criterion | Verify |
|---|-----------|--------|
| 1 | engine-doctor.sh 对超字节上限的引擎文件报 WARN | 在诺识项目跑 `bash engine/scripts/engine-doctor.sh` 看 CONTEXT.md 是否报字节超限 |
| 2 | engine-doctor.ps1 对超字节上限的引擎文件报 WARN | 在诺识项目跑 `pwsh engine/scripts/engine-doctor.ps1` 看 CONTEXT.md 是否报字节超限 |
| 3 | engine-doctor.sh 对超行宽上限(>500 字符)的引擎文件报 WARN | 同 AC-1,看是否报行宽超限 |
| 4 | engine-doctor.ps1 对超行宽上限的引擎文件报 WARN | 同 AC-2,看是否报行宽超限 |
| 5 | 双实现 parity:.sh 和 .ps1 对同一文件报相同数量的 WARN | 对比两实现输出 |
| 6 | 源仓库自身不误报(自身引擎文件都在预算内) | 在源仓库跑 Doctor,确认 0 新增 WARN |
| 7 | scripts/check.sh 全绿 | `bash scripts/check.sh` |
