# T-027: Installer pre-commit hook 智能覆盖

> status: done
> exempt: manual verification (check.sh passed + dual-implementation parity confirmed)
> lane: main
> decision: D-023
> domain: engine-runtime

## GOAL

修复 installer 的 keep-existing 逻辑导致 .git/hooks/pre-commit 永远不更新。详见 D-023。

## WRITE-SET

- install.ps1
- install.sh

## FORBIDDEN

- contract/** (不碰契约)
- engine/decisions/** (不碰决策台账,除新建 D-023)
- engine/tasks/** (不碰其他任务卡)
- engine/migrations/** (不碰迁移 step)

## AC

| # | Criterion | Verify |
|---|-----------|--------|
| 1 | install.ps1: 引擎管理 hook 内容不同时覆盖 | 诺识项目 engine update 后 .git/hooks/pre-commit 从 32 行更新到 136 行 |
| 2 | install.sh: parity(diff 比较) | 源仓库 check.sh 全绿 |
| 3 | 用户自定义 hook 保留 | 逻辑分支:无 Engine System 标记 → keep |
