# T-028: Installer 下载失败聚合 + --local SHA256 校验

> status: done
> lane: main
> decision: D-024
> domain: root,project-meta

## GOAL

修复发布端两个安装缺陷:网络模式下载失败可假成功、--local 模式跳过 manifest SHA256 校验。详见 D-024。

## WRITE-SET

- install.sh
- install.ps1
- engine/tasks/T-028.md
- engine/decisions/D-024.md
- engine/evidence/T-028/**
- engine/CONTEXT.md
- engine/HANDOFF.md
- engine/ENGINE_MAP.md
- engine/changes/CHANGE-2026-07-14-04.md

## FORBIDDEN

- contract/** (不碰契约)
- plugin/** (安装器不在 plugin 镜像内,manifest 本卡不改)
- engine/scripts/** (不碰引擎运行时脚本)
- engine/migrations/** (不碰迁移 step)

## AC

AC: AC-1 --local 安装后跑 manifest 校验且全绿 | verify: rm -rf /tmp/t028-ac1 /tmp/t028-ac1.out && mkdir -p /tmp/t028-ac1/home && cd /tmp/t028-ac1 && git init -q . && HOME=/tmp/t028-ac1/home bash "$OLDPWD/install.sh" --local "$OLDPWD/plugin" > /tmp/t028-ac1.out 2>&1 && grep -q 'verified (SHA256)' /tmp/t028-ac1.out
AC: AC-2 --local 篡改文件时校验硬失败 | verify: rm -rf /tmp/t028-ac2 && cp -r plugin /tmp/t028-ac2-plugin && echo tampered >> /tmp/t028-ac2-plugin/engine/scripts/engine-doctor.sh && mkdir -p /tmp/t028-ac2 && cd /tmp/t028-ac2 && git init -q . && ! bash "$OLDPWD/install.sh" --local /tmp/t028-ac2-plugin >/dev/null 2>&1
AC: AC-3 runtime-law.md 下载失败不再静默 | verify: grep -n 'runtime-law' install.sh | head -20 && ! grep -E 'runtime-law.*\|\| true' install.sh
AC: AC-4 sh/ps1 parity——ps1 含同等 local 校验逻辑 | verify: grep -q 'Verify-Checksums' install.ps1 && grep -qE 'LocalDir.*manifest|manifest.*LocalDir' install.ps1
AC: AC-5 全量门禁通过 | verify: bash scripts/check.sh

## 2026-07-17 验收记录

- `engine verify T-028`：AC-2、AC-3、AC-4 PASS；AC-1 FAIL；AC-5 在外层 240 秒超时前未完成。
- 证据已写入 `engine/evidence/T-028/AC-1.json` 至 `AC-4.json`。
- 暂停原因：先处理 D-025/T-029 的长会话门禁与并行写冲突；恢复时从 AC-1 的本地 manifest 校验失败继续。
- 2026-07-17 二次诊断：安装实际 exit 0 且输出 `56 file(s) verified (SHA256)`；旧 AC 的 `grep -q` 管道在匹配后提前关闭，`pipefail` 将上游 SIGPIPE 误判为失败。AC-1 已改为先落日志再 grep。
- 2026-07-17 三次诊断：manifest 的 57 项校验已完成，失败发生在随后安装 CLI 到宿主 `~/.local/bin`，被工作区沙箱拒绝。AC-1 增加隔离 HOME，避免验收污染宿主目录。
- 2026-07-17 最终验收：`engine verify T-028` 5/5 PASS；AC-5 完整发布门禁 PASS，证据见 `engine/evidence/T-028/`。
