# T-029: 长会话门禁 + 并行 agent 记忆分片

> status: done | lane: main | decision: D-025 | plan: none | domain: engine-runtime,project-meta,root
GOAL: 让任务范围约束覆盖引擎文件，并让并行 worker 写各自分片、只由协调者汇总共享 CONTEXT/HANDOFF。
WRITE-SET: VERSION, engine/VERSION, plugin/VERSION, contract/**, engine/SYSTEM.md, engine/ENGINE_DOCTOR.md, engine/AGENT_ADAPTERS.md, engine/scripts/**, engine/bin/**, engine/prompts/**, engine/domains/**, engine/workstreams/**, plugin/**, tests/**, scripts/**, .claude/settings.json, .claude/commands/engine-sync.md, install.sh, install.ps1, AGENTS.md, ENGINE_FILE_SYSTEM_v5.md, runtime-law.md, rules.json, engine/tasks/README.md, engine/tasks/T-028.md, engine/tasks/T-029.md, engine/decisions/D-025.md, engine/changes/CHANGE-2026-07-17-01.md, engine/evidence/T-028/**, engine/evidence/T-029/**, engine/CONTEXT.md, engine/HANDOFF.md, engine/ENGINE_MAP.md
FORBIDDEN: .git/**, archive/**
CONSTRAINTS: sh/ps1 + source/plugin 语义孪生；通用能力不得只靠 Claude hook；不自建 agent 进程/队列；发布端必须通过隔离安装验证。 (source: D-025, engine/SYSTEM.md)

## AC

AC: AC-1 单行与 section-list 两种任务卡格式均被解析；v6.5 无 active 卡拒绝普通写入，engine 越界/FORBIDDEN 硬拦，done 逐 AC 要 PASS evidence；旧标记保持兼容 | verify: bash tests/task-card/run-task-tests.sh
AC: AC-2 PreToolUse 按 session/agent 记录路径，子 agent 抢写共享记忆被拦，只允许写自己的 workstream 分片 | verify: bash tests/hook-parity/run-parity.sh
AC: AC-3 `engine workstream T-029 <agent-id>` 在独立目录生成 worker 上下文骨架，且 `engine context` 能展示未汇总分片 | verify: bash tests/workstream/run-workstream-tests.sh
AC: AC-4 Claude 设置含写前校验且周期重锚不超过 5 行、不重复 L0/完整写集；非 Claude 环境仍有 pre-commit 全路径范围检查 | verify: grep -q 'PreToolUse' plugin/.claude/settings.json && grep -q 'UserPromptSubmit' plugin/.claude/settings.json && grep -q 'WRITE-SET' plugin/engine/scripts/githooks/pre-commit && guard_out=$(printf '{}' | CLAUDE_PROJECT_DIR="$PWD" bash plugin/engine/scripts/engine-hook-session-start.sh --guard) && test "$(printf '%s\n' "$guard_out" | wc -l)" -le 5 && ! printf '%s\n' "$guard_out" | grep -q 'runtime-law\|^WRITE-SET:'
AC: AC-5 发布镜像、manifest、installer 隔离安装与全量门禁通过 | verify: bash scripts/check.sh

## 2026-07-17 验收记录

- `engine verify T-029`：5/5 PASS；task-card 44/44、hook parity 31/31、workstream 12/12，完整发布门禁 PASS。
- UserPromptSubmit guard 实测 sh/PowerShell 均为 4 行，不重复 runtime-law 或完整 WRITE-SET；Doctor 将 24 张 done 卡聚合为 1 行成功摘要。
- 证据：`engine/evidence/T-029/AC-1.json` 至 `AC-5.json`。
