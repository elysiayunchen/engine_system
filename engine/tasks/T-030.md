# T-030: v6.5.0 发布与远端可更新验证

> status: done | lane: release | decision: D-026 | plan: none | domain: engine-runtime,project-meta,root

GOAL: 将已验收的 Engine System v6.5.0 提交到 main、创建 v6.5.0 发布 tag，并验证下游可从远端 VERSION/分支拉取。
WRITE-SET: **
FORBIDDEN: .git/**, archive/**, .workbuddy/**, engine2.zip
AC: AC-1 版本与发布说明一致 | verify: test "$(tr -d '[:space:]' < VERSION)" = "6.5.0" && test "$(tr -d '[:space:]' < engine/VERSION)" = "6.5.0" && test "$(tr -d '[:space:]' < plugin/VERSION)" = "6.5.0" && grep -q '^## v6.5.0' CHANGELOG.md
AC: AC-2 发布前完整健康检查 | verify: bash scripts/check.sh
AC: AC-3 发布 tag 与当前版本存在 | verify: git tag -l v6.5.0 | grep -qx v6.5.0
AC: AC-4 发布胶囊具备架构师可读的目标、影响、风险、验证、回滚、下一步和责任边界 | verify: for h in Goal 'Actual Changes' 'Impact Scope' 'Risk & Watchpoints' Verification Rollback 'Next Step' 'Responsibility Boundary'; do grep -q "^## $h" engine/changes/CHANGE-2026-07-17-02.md || exit 1; done
AC: AC-5 CI/Release 使用正式 --local 安装入口，fresh install 后 AGENTS 不超过 45 行，Release migrate 在安装目标根执行 | verify: grep -q -- '--local' .github/workflows/ci.yml && grep -q -- '--local' .github/workflows/release.yml && grep -q 'I6 fresh install AGENTS' tests/install-flow/run-install-tests.sh && grep -Fq 'cd "$SANDBOX" && bash engine/bin/engine migrate' .github/workflows/release.yml
CONSTRAINTS: 只发布已通过 T-028/T-029 验收的 v6.5.0；不纳入用户 scratch 文件；tag 必须指向 main CI 全绿的最终发布提交。 (source: user request, D-026)

## 2026-07-17 验收记录

- `engine verify T-030`：5/5 PASS；完整健康检查 fingerprint `47c747757d0c`。
- 最终发布提交 `e33a3d820732013d08d6533f11bee0f2ae383c55` 的 main CI 29597273388：Linux、Windows、install-migrate 全部 PASS。
- `v6.5.0` Release workflow 29599793000 PASS；GitHub Release 355822586 已公开，非 draft / 非 prerelease。
- Release 附件：checksums、tar.gz、zip、offline.tar.gz 共 4 项 uploaded；实际下载三个归档后 `sha256sum -c` 全部 OK。
- 远端 VERSION=6.5.0；main install.sh SHA256 与本地一致（`2A811708030BBCE403534A27A008B52B968180B85A9A4DD7BC4BE7A19F87C38B`）。
- 用户 scratch `.workbuddy/`、`engine2.zip` 未纳入任何提交。
