# T-035: verify 死代码检测(linter 委托 + 反向调用点扫描 + done 门项)+ v6.10.0 发布

> status: active | lane: contract | decision: D-028 | plan: none | domain: engine-runtime,project-meta,root
> checkpoint_plan: tryout (v6.10.0 任务,WRITE-SET 含 29 paths 是设计内完整范围,拆卡会破坏 verify 脚本 4 份对称性)

GOAL: 把"agent 跑出从来没用上的死代码"做成机制:verify 调用原生 linter(shellcheck + PSScriptAnalyzer 优先,自研 grep 仅 fallback),反向调用点扫描(查 WRITE-SET 删改标识符在全仓的残留引用),未被引用输出 `evidence/T-NNN/DEAD-CODE.json`;WARN 升级为 done 门项(warn_count > 0 时 done 须架构师显式豁免);发版 v6.10.0。
WRITE-SET: contract/src/30-operational.md, contract/src/behaviors/verify-writeback.md, contract/compile.sh, contract/compile.ps1, ENGINE_FILE_SYSTEM_v5.md, plugin/.claude/skills/engine-verify-writeback/SKILL.md, engine/prompts/behaviors/verify-writeback.md, plugin/engine/prompts/behaviors/verify-writeback.md, engine/scripts/engine-verify.sh, engine/scripts/engine-verify.ps1, plugin/engine/scripts/engine-verify.sh, plugin/engine/scripts/engine-verify.ps1, engine/scripts/engine-doctor.sh, engine/scripts/engine-doctor.ps1, plugin/engine/scripts/engine-doctor.sh, plugin/engine/scripts/engine-doctor.ps1, engine/ENGINE_DOCTOR.md, plugin/engine/ENGINE_DOCTOR.md, engine/scripts/engine-migrate-contract.sh, engine/scripts/engine-migrate-contract.ps1, plugin/engine/scripts/engine-migrate-contract.sh, plugin/engine/scripts/engine-migrate-contract.ps1, engine/AGENT_ADAPTERS.md, plugin/engine/AGENT_ADAPTERS.md, engine/ENGINE_MAP.md, engine/CONTEXT.md, engine/HANDOFF.md, engine/VERSION, plugin/VERSION, VERSION, plugin/manifest.json, engine/decisions/D-028.md, engine/tasks/T-035.md, engine/tasks/T-035.spec.md, engine/changes/CHANGE-2026-07-19-05.md, engine/evidence/T-035/**, CHANGELOG.md, AGENTS.md, .github/**
FORBIDDEN: .git/**, archive/**, .workbuddy/**, engine/migrations/**
AC: AC-1 契约源 30-operational.md 含死代码检测规则(linter 委托 + 反向调用点扫描 + DONE 门项) | verify: grep -q 'shellcheck\|PSScriptAnalyzer' contract/src/30-operational.md && grep -q '反向.*调用点\|reverse.*call.*site\|删改.*残留' contract/src/30-operational.md && grep -q 'DEAD-CODE' contract/src/30-operational.md
AC: AC-2 engine-verify.sh + .ps1(4 份)入口自检 shellcheck/PSScriptAnalyzer 可用性(不可用降级 grep fallback;ps1 端含 `Install-Module -Scope CurrentUser -Force -AllowClobber` 兜底),失败时降级到自研 grep fallback | verify: grep -q 'shellcheck' engine/scripts/engine-verify.sh && grep -q 'PSScriptAnalyzer\|Invoke-ScriptAnalyzer' engine/scripts/engine-verify.ps1 && grep -q 'shellcheck' plugin/engine/scripts/engine-verify.sh && grep -q 'PSScriptAnalyzer\|Invoke-ScriptAnalyzer' plugin/engine/scripts/engine-verify.ps1 && grep -q 'command -v shellcheck' engine/scripts/engine-verify.sh && grep -q 'command -v shellcheck' plugin/engine/scripts/engine-verify.sh && grep -q 'Get-Module -ListAvailable PSScriptAnalyzer' engine/scripts/engine-verify.ps1 && grep -q 'Get-Module -ListAvailable PSScriptAnalyzer' plugin/engine/scripts/engine-verify.ps1 && bash engine/scripts/engine-verify.sh T-035 >/dev/null 2>&1; ! grep -q '"linter"[[:space:]]*:[[:space:]]*"grep-fallback"' engine/evidence/T-035/DEAD-CODE.json 2>/dev/null
AC: AC-2.1 engine-verify.sh + .ps1(4 份)调用 jscpd 扫描 WRITE-SET 触及的 .sh / .ps1 / .md 文件,输出 evidence/T-NNN/COPY-PASTE.json,命中即计 warn_count(走现有 warn_count→done 门,不新增门级);jscpd 不可用降级 skip + WARN | verify: grep -q 'jscpd' engine/scripts/engine-verify.sh && grep -q 'jscpd' engine/scripts/engine-verify.ps1 && grep -q 'jscpd' plugin/engine/scripts/engine-verify.sh && grep -q 'jscpd' plugin/engine/scripts/engine-verify.ps1 && diff -q engine/scripts/engine-verify.sh plugin/engine/scripts/engine-verify.sh && diff -q engine/scripts/engine-verify.ps1 plugin/engine/scripts/engine-verify.ps1
AC: AC-3 engine-verify.sh + .ps1(4 份)实现反向调用点扫描(WRITE-SET 删改标识符 → 全仓 grep 残留引用) | verify: grep -q 'reverse_call_site\|reverse-call-site\|反向调用点' engine/scripts/engine-verify.sh && grep -q 'reverse_call_site\|reverse-call-site\|反向调用点\|ReverseCallSite' engine/scripts/engine-verify.ps1
AC: AC-4 engine-verify.sh + .ps1(4 份)输出 evidence/T-NNN/DEAD-CODE.json | verify: grep -q 'DEAD-CODE.json' engine/scripts/engine-verify.sh && grep -q 'DEAD-CODE.json' engine/scripts/engine-verify.ps1
AC: AC-5 ENGINE_DOCTOR.md 含 WARN 升级为 done 门项规则(warn_count > 0 时 done 须架构师显式豁免) | verify: grep -q 'warn_count\|WARN.*done\|WARN.*豁免\|WARN.*exempt' engine/ENGINE_DOCTOR.md
AC: AC-6 engine-doctor.sh + .ps1(4 份)实现 check_warn_done_gate(FAIL 级,warn_count > 0 且无豁免标记 = FAIL;contract-version < 6.10.0 时降级 WARN 与 D-028 §9 一致;DEAD-CODE.json 顶层 `exempt_all: true` 视为全部 entry 已豁免,保留 per-entry `exempt` 细粒度) | verify: grep -q 'check_warn_done_gate\|warn_done_gate\|WarnDoneGate' engine/scripts/engine-doctor.sh && grep -q 'check_warn_done_gate\|warn_done_gate\|WarnDoneGate' engine/scripts/engine-doctor.ps1 && grep -q 'check_warn_done_gate\|warn_done_gate\|WarnDoneGate' plugin/engine/scripts/engine-doctor.sh && grep -q 'check_warn_done_gate\|warn_done_gate\|WarnDoneGate' plugin/engine/scripts/engine-doctor.ps1 && grep -q 'contract-version' engine/scripts/engine-doctor.sh && grep -q 'contract-version' engine/scripts/engine-doctor.ps1 && grep -q 'contract-version' plugin/engine/scripts/engine-doctor.sh && grep -q 'contract-version' plugin/engine/scripts/engine-doctor.ps1 && grep -q 'exempt_all' engine/scripts/engine-doctor.sh && grep -q 'exempt_all' engine/scripts/engine-doctor.ps1 && grep -q 'exempt_all' plugin/engine/scripts/engine-doctor.sh && grep -q 'exempt_all' plugin/engine/scripts/engine-doctor.ps1
AC: AC-7 behaviors/verify-writeback.md 含死代码检测步骤 + 豁免标记规则 | verify: grep -q 'DEAD-CODE\|死代码' engine/prompts/behaviors/verify-writeback.md && grep -q '豁免\|exempt' engine/prompts/behaviors/verify-writeback.md
AC: AC-8 plugin 镜像同步且 manifest SHA256 已更新 | verify: bash scripts/check.sh && diff -q engine/scripts/engine-verify.sh plugin/engine/scripts/engine-verify.sh && diff -q engine/scripts/engine-verify.ps1 plugin/engine/scripts/engine-verify.ps1
AC: AC-9 版本三处一致 = 6.10.0 且 CHANGELOG 含 v6.10.0 | verify: test "$(tr -d '[:space:]' < VERSION)" = "6.10.0" && test "$(tr -d '[:space:]' < engine/VERSION)" = "6.10.0" && test "$(tr -d '[:space:]' < plugin/VERSION)" = "6.10.0" && grep -q '^## v6.10.0' CHANGELOG.md
AC: AC-10 完整发布门禁全绿 | verify: bash scripts/check.sh
CONSTRAINTS: 不自研 AST 解析器(委托 shellcheck/PSScriptAnalyzer);不硬失败所有 WARN(只让 warn_count>0 时 done 须豁免);不覆盖软死代码(冗余复刻,靠 INVENTORY 兜底);不引入跨语言别名表(镜像对死代码靠 linter 自身识别);不破坏 v6.9 既有门禁。warn_count > 0 done 门须读 contract-version 实现迁移宽限期,与 D-028 §9 一致;DEAD-CODE.json 顶层 `exempt_all` 批量豁免须实现,与 D-028 §9 一致;linter 自检须实现,与 D-028 Open Question #4 评估结论一致;jscpd 委托须实现,与 D-028 §10 机制 B 一致。 (source: 用户要求 + P4 子代理审视报告 + D-028 approved §9/§10/Open Question #4)
