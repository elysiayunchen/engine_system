# T-036: 多会话锁 + worker 自动降级 + PreToolUse 双信号 + v6.11.0 发布

> status: active | lane: contract | decision: D-029 | plan: none | domain: engine-runtime,project-meta,root
> estimated_steps: 26 | checkpoint_plan: tryout (v6.11.0 任务,18 个 AC + 32 paths,拆卡会破坏 hook 4 份对称性)
> depends-on: none (D-028 v6.10.0 T-035 已 done,但 T-036 不依赖 D-028 路线)

GOAL: 把"多 Claude Code 实例并行抢写引擎记忆三件套"做成机制:SessionStart hook 复用 Claude Code payload 已传入的 `session_id`,用 atomic 独占 lock file(`engine/.cache/session.lock`)分配协调者/worker 角色,第一个会话获协调者(写共享三件套),后续会话降级 worker(写 `engine/workstreams/<task>/<session-id>/` 隔离分片);PreToolUse 拦截扩展为双信号(`agent_id` 非空 **或** `.cache/sessions/<key>.role=worker`);新增 `engine assume-coordinator [--force]` 与 `engine merge-workstream <session-id>` 两个命令作为逃生通道;发版 v6.11.0。
WRITE-SET: contract/src/30-operational.md, contract/budget.json, contract/compile.sh, contract/compile.ps1, ENGINE_FILE_SYSTEM_v5.md, rules.json, runtime-law.md, plugin/.claude/commands/engine-init.md, plugin/.claude/skills/engine-task-run/SKILL.md, plugin/.claude/skills/engine-handoff/SKILL.md, engine/prompts/init.md, engine/prompts/behaviors/task-run.md, engine/prompts/behaviors/handoff.md, plugin/engine/prompts/init.md, plugin/engine/prompts/behaviors/task-run.md, plugin/engine/prompts/behaviors/handoff.md, engine/scripts/engine-hook-session-start.sh, engine/scripts/engine-hook-session-start.ps1, plugin/engine/scripts/engine-hook-session-start.sh, plugin/engine/scripts/engine-hook-session-start.ps1, engine/scripts/engine-hook-stop.sh, engine/scripts/engine-hook-stop.ps1, plugin/engine/scripts/engine-hook-stop.sh, plugin/engine/scripts/engine-hook-stop.ps1, engine/scripts/engine-context.sh, engine/scripts/engine-context.ps1, plugin/engine/scripts/engine-context.sh, plugin/engine/scripts/engine-context.ps1, engine/scripts/engine-doctor.sh, engine/scripts/engine-doctor.ps1, plugin/engine/scripts/engine-doctor.sh, plugin/engine/scripts/engine-doctor.ps1, engine/scripts/engine-migrate-contract.sh, engine/scripts/engine-migrate-contract.ps1, plugin/engine/scripts/engine-migrate-contract.sh, plugin/engine/scripts/engine-migrate-contract.ps1, engine/scripts/githooks/pre-commit, plugin/engine/scripts/githooks/pre-commit, engine/ENGINE_DOCTOR.md, plugin/engine/ENGINE_DOCTOR.md, engine/AGENT_ADAPTERS.md, plugin/engine/AGENT_ADAPTERS.md, engine/bin/engine, engine/bin/engine.ps1, engine/bin/engine.cmd, plugin/engine/bin/engine, plugin/engine/bin/engine.ps1, plugin/engine/bin/engine.cmd, engine/ENGINE_MAP.md, engine/CONTEXT.md, engine/HANDOFF.md, engine/VERSION, plugin/VERSION, VERSION, plugin/manifest.json, engine/decisions/D-029.md, engine/tasks/T-036.md, engine/tasks/T-036.spec.md, engine/tasks/T-036/progress.md, engine/changes/CHANGE-2026-07-20-03.md, engine/evidence/T-036/**, tests/workstream/**, tests/hook-parity/**, CHANGELOG.md, AGENTS.md, .github/**
FORBIDDEN: .git/**, archive/**, .workbuddy/**, engine/migrations/**
AC: AC-1 契约源 30-operational.md 含多会话隔离规则(atomic lock + pid+StartTime 双信号 + 协调者/worker 角色分配 + fail-open/closed 边界 + D-028 三文件写入边界) | verify: grep -q 'session.lock\|atomic\|noclobber\|FileStream.*Exclusive' contract/src/30-operational.md && grep -q 'role.*coordinator\|role.*worker' contract/src/30-operational.md && grep -q 'agent_id.*role\|双信号\|double.signal' contract/src/30-operational.md && grep -q 'fail-open\|fail-closed' contract/src/30-operational.md
AC: AC-2 SessionStart hook (4 份) 实现 lock file 检测 + 协调者/worker 角色分配 + 注入角色提示(≤3 行) | verify: grep -q 'session.lock\|session_lock\|SessionLock' engine/scripts/engine-hook-session-start.sh && grep -q 'session.lock\|session_lock\|SessionLock' engine/scripts/engine-hook-session-start.ps1 && grep -q 'session.lock\|session_lock\|SessionLock' plugin/engine/scripts/engine-hook-session-start.sh && grep -q 'session.lock\|session_lock\|SessionLock' plugin/engine/scripts/engine-hook-session-start.ps1 && grep -q 'coordinator\|协调者' engine/scripts/engine-hook-session-start.sh && grep -q 'coordinator\|协调者' engine/scripts/engine-hook-session-start.ps1 && grep -q 'worker\|降级.*worker' engine/scripts/engine-hook-session-start.sh && grep -q 'worker\|降级.*worker' engine/scripts/engine-hook-session-start.ps1
AC: AC-3 Stop hook (4 份) 实现 .meta 文件写入(角色+时间戳) + 协调者退出释放 lock + tombstone 文件机制 | verify: grep -q '\.meta\|meta_file\|MetaFile' engine/scripts/engine-hook-stop.sh && grep -q '\.meta\|meta_file\|MetaFile' engine/scripts/engine-hook-stop.ps1 && grep -q '\.meta\|meta_file\|MetaFile' plugin/engine/scripts/engine-hook-stop.sh && grep -q '\.meta\|meta_file\|MetaFile' plugin/engine/scripts/engine-hook-stop.ps1 && grep -q 'rm.*session.lock\|Remove.*session.lock\|release.*lock' engine/scripts/engine-hook-stop.sh && grep -q 'rm.*session.lock\|Remove.*session.lock\|release.*lock' engine/scripts/engine-hook-stop.ps1 && grep -q 'rm.*session.lock\|Remove.*session.lock\|release.*lock' plugin/engine/scripts/engine-hook-stop.sh && grep -q 'rm.*session.lock\|Remove.*session.lock\|release.*lock' plugin/engine/scripts/engine-hook-stop.ps1
AC: AC-4 Stop hook (4 份) PreToolUse 拦截扩展为双信号(agent_id 非空 OR .role=worker 文件存在,OR 关系非 AND) | verify: grep -q 'role.*worker\|\.role' engine/scripts/engine-hook-stop.sh && grep -q 'role.*worker\|\.role' engine/scripts/engine-hook-stop.ps1 && grep -q 'role.*worker\|\.role' plugin/engine/scripts/engine-hook-stop.sh && grep -q 'role.*worker\|\.role' plugin/engine/scripts/engine-hook-stop.ps1 && grep -E 'agent_id.*role|role.*agent_id|\|\|.*role|\|\|.*agent_id' engine/scripts/engine-hook-stop.sh | head -1 && grep -E 'agent_id.*role|role.*agent_id|\|\|.*role|\|\|.*agent_id' engine/scripts/engine-hook-stop.ps1 | head -1
AC: AC-5 engine-context.sh + .ps1 (4 份) 实现 Active Sessions 面板(插入 Active Task 与 Parallel Workstreams 之间,≤5 行/会话,每行 ≤61 chars;数据源:lock file 读协调者 + .cache/sessions/*.meta 读 workers) | verify: grep -q 'Active Sessions\|active_session\|ActiveSession' engine/scripts/engine-context.sh && grep -q 'Active Sessions\|active_session\|ActiveSession' engine/scripts/engine-context.ps1 && grep -q 'Active Sessions\|active_session\|ActiveSession' plugin/engine/scripts/engine-context.sh && grep -q 'Active Sessions\|active_session\|ActiveSession' plugin/engine/scripts/engine-context.ps1 && grep -q 'session.lock\|SessionLock' engine/scripts/engine-context.sh && grep -q 'session.lock\|SessionLock' engine/scripts/engine-context.ps1 && grep -q '\.meta\|MetaFile' engine/scripts/engine-context.sh && grep -q '\.meta\|MetaFile' engine/scripts/engine-context.ps1
AC: AC-6 engine-doctor.sh + .ps1 (4 份) 实现 check_multi_session_isolation(FAIL 级,contract-version ≥ 6.11.0 fail-closed,< 6.11.0 fail-open WARN;SessionStart hook 异常时输出 WARN) | verify: grep -q 'check_multi_session_isolation\|multi_session_isolation\|MultiSessionIsolation' engine/scripts/engine-doctor.sh && grep -q 'check_multi_session_isolation\|multi_session_isolation\|MultiSessionIsolation' engine/scripts/engine-doctor.ps1 && grep -q 'check_multi_session_isolation\|multi_session_isolation\|MultiSessionIsolation' plugin/engine/scripts/engine-doctor.sh && grep -q 'check_multi_session_isolation\|multi_session_isolation\|MultiSessionIsolation' plugin/engine/scripts/engine-doctor.ps1
AC: AC-7 engine-doctor.sh + .ps1 (4 份) 实现 check_workstream_orphan(WARN 级,workstream 分片无对应 .meta 文件) | verify: grep -q 'check_workstream_orphan\|workstream_orphan\|WorkstreamOrphan' engine/scripts/engine-doctor.sh && grep -q 'check_workstream_orphan\|workstream_orphan\|WorkstreamOrphan' engine/scripts/engine-doctor.ps1 && grep -q 'check_workstream_orphan\|workstream_orphan\|WorkstreamOrphan' plugin/engine/scripts/engine-doctor.sh && grep -q 'check_workstream_orphan\|workstream_orphan\|WorkstreamOrphan' plugin/engine/scripts/engine-doctor.ps1
AC: AC-8 engine workstream 命令(3 份 sh/ps1/cmd)加 --kind=subagent|session 参数(默认 subagent 向后兼容) | verify: grep -q '\-\-kind\|--kind' engine/bin/engine && grep -q '\-\-kind\|--kind' engine/bin/engine.ps1 && grep -q '\-\-kind\|--kind' engine/bin/engine.cmd && grep -q 'subagent\|session' engine/bin/engine
AC: AC-9 engine assume-coordinator [--force] 命令实现(强制接管 lock + 写 tombstone 通知) | verify: grep -q 'assume-coordinator\|assume_coordinator\|AssumeCoordinator' engine/bin/engine && grep -q 'assume-coordinator\|assume_coordinator\|AssumeCoordinator' engine/bin/engine.ps1 && grep -q 'assume-coordinator\|assume_coordinator\|AssumeCoordinator' engine/bin/engine.cmd && grep -q 'tombstone' engine/bin/engine && grep -q 'tombstone' engine/bin/engine.ps1 && grep -q 'tombstone' engine/bin/engine.cmd && grep -q 'assume-coordinator\|assume_coordinator\|AssumeCoordinator' plugin/engine/bin/engine && grep -q 'assume-coordinator\|assume_coordinator\|AssumeCoordinator' plugin/engine/bin/engine.ps1
AC: AC-10 engine merge-workstream <session-id> 命令实现(显式 merge worker 分片到共享记忆,提示协调者合并步骤) | verify: grep -q 'merge-workstream\|merge_workstream\|MergeWorkstream' engine/bin/engine && grep -q 'merge-workstream\|merge_workstream\|MergeWorkstream' engine/bin/engine.ps1 && grep -q 'merge-workstream\|merge_workstream\|MergeWorkstream' engine/bin/engine.cmd
AC: AC-11 ENGINE_DOCTOR.md + plugin 镜像加 #16 多会话隔离检查 + #17 workstream 孤儿检查,contract-version 6.10.0→6.11.0 | verify: grep -q 'multi.session\|多会话' engine/ENGINE_DOCTOR.md && grep -q 'multi.session\|多会话' plugin/engine/ENGINE_DOCTOR.md && grep -q 'workstream.*orphan\|workstream.*孤儿' engine/ENGINE_DOCTOR.md && grep -q 'workstream.*orphan\|workstream.*孤儿' plugin/engine/ENGINE_DOCTOR.md && grep -q 'contract-version: 6.11.0\|contract-version:6.11.0' engine/ENGINE_DOCTOR.md
AC: AC-12 AGENT_ADAPTERS.md + plugin 镜像加 C 档扩展子段(多会话锁 + 何时用 --force) | verify: grep -q '多会话\|multi.session\|multi-session' engine/AGENT_ADAPTERS.md && grep -q '多会话\|multi.session\|multi-session' plugin/engine/AGENT_ADAPTERS.md && grep -q 'assume-coordinator\|assume.coordinator\|--force' engine/AGENT_ADAPTERS.md && grep -q 'assume-coordinator\|assume.coordinator\|--force' plugin/engine/AGENT_ADAPTERS.md
AC: AC-13 migrator (4 份) 加 #N managed block item 12(多会话场景协议:lock file + worker 自动降级 + assume-coordinator + merge-workstream) | verify: grep -q 'multi.session\|多会话\|session.lock' engine/scripts/engine-migrate-contract.sh && grep -q 'multi.session\|多会话\|session.lock' engine/scripts/engine-migrate-contract.ps1 && grep -q 'multi.session\|多会话\|session.lock' plugin/engine/scripts/engine-migrate-contract.sh && grep -q 'multi.session\|多会话\|session.lock' plugin/engine/scripts/engine-migrate-contract.ps1
AC: AC-14 plugin 镜像同步且 manifest SHA256 已更新 | verify: bash scripts/check.sh && diff -q engine/scripts/engine-hook-session-start.sh plugin/engine/scripts/engine-hook-session-start.sh && diff -q engine/scripts/engine-hook-stop.sh plugin/engine/scripts/engine-hook-stop.sh && diff -q engine/scripts/engine-context.sh plugin/engine/scripts/engine-context.sh && diff -q engine/scripts/engine-doctor.sh plugin/engine/scripts/engine-doctor.sh
AC: AC-15 版本三处一致 = 6.11.0 且 CHANGELOG 含 v6.11.0 | verify: test "$(tr -d '[:space:]' < VERSION)" = "6.11.0" && test "$(tr -d '[:space:]' < engine/VERSION)" = "6.11.0" && test "$(tr -d '[:space:]' < plugin/VERSION)" = "6.11.0" && grep -q '^## v6.11.0' CHANGELOG.md
AC: AC-16 完整发布门禁全绿 | verify: bash scripts/check.sh
AC: AC-17 kill switch:环境变量 ENGINE_DISABLE_MULTI_SESSION=1 时,SessionStart hook 跳过 lock 检测,所有会话降级为单会话模式(等同现状,等同 fail-open);同时 engine disable-multi-session 命令切换全局 disable 标志文件 engine/.cache/multi-session.disabled | verify: grep -q 'ENGINE_DISABLE_MULTI_SESSION\|DISABLE_MULTI_SESSION\|disable.multi.session\|disable_multi_session' engine/scripts/engine-hook-session-start.sh && grep -q 'ENGINE_DISABLE_MULTI_SESSION\|DISABLE_MULTI_SESSION\|disable.multi.session\|disable_multi_session' engine/scripts/engine-hook-session-start.ps1 && grep -q 'disable-multi-session\|disable_multi_session\|DisableMultiSession' engine/bin/engine && grep -q 'disable-multi-session\|disable_multi_session\|DisableMultiSession' engine/bin/engine.ps1
AC: AC-18 端到端测试:tests/workstream/ 下测试用例覆盖(1) worker 模式触发逻辑(2) 双信号拦截 OR 关系(3) lock 残留自动恢复(4) kill switch 生效 | verify: test -f tests/workstream/test_worker_mode.sh && test -f tests/workstream/test_double_signal.sh && test -f tests/workstream/test_lock_recovery.sh && test -f tests/workstream/test_kill_switch.sh && bash tests/workstream/test_worker_mode.sh && bash tests/workstream/test_kill_switch.sh
CONSTRAINTS: 不撤销 v6.5 workstream 机制(只扩展触发点 + 新增角色判定机制);不强制每会话用 git worktree(作为可选工作流保留);不引入跨进程分布式锁服务(违反 CLI-LEAN);不让协调者批量代理所有 worker 提交(避免单点阻塞);不引入自动 merge worker 分片(merge 仍是显式步骤);新项目 fail-closed 老项目 fail-open 与 D-028 §9 一致;D-028 三文件写入边界明确(progress.md/checkpoint.md worker 写分片,INVENTORY.md worker 不写 merge 时协调者补);Windows PowerShell 5.1 兼容(用户 memory 强调非 PS7+);atomic 操作用 noclobber(POSIX)/FileStream Exclusive(Windows),不依赖外部命令;session_id 复用 Claude Code payload 不重新生成。 (source: 用户要求 + 4 路子代理审视报告 + D-029 approved §3/§4/§8/§Consequences)
