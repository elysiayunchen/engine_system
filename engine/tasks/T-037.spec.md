# SPEC TWIN — T-037: 加 Trae continuity 开发准则到 engine/SYSTEM.md
> 与 engine/tasks/T-037.md 并列共生 | status follows T-037

## 验收标准 (Acceptance Criteria)

| ID | 标准(用户可见行为或系统不变量) | 验证方式 | 状态 | Evidence |
|----|----------------------------------|----------|------|----------|
| AC-1 | `engine/SYSTEM.md`「项目开发准则」段加 Trae continuity 子段:标题 `### Trae agent 工具对话延续准则` + 准则文本(运行在 Trae 相关开发 agent 工具时,NEVER 主动中止对话,必须用 AskUserQuestion 延续)+ 触发条件(列出 TRAE IDE / TRAE Work / TRAE CLI / TRAE Plugin 四种工具)+ 工具指定(AskUserQuestion) | grep 'Trae' + grep 'AskUserQuestion' + grep 'TRAE IDE\|TRAE Work\|TRAE CLI\|TRAE Plugin' + grep '延续\|continuity\|不.*主动.*中止' | [x] ✅ | T-037/AC-1.json |

## Verification Notes

- Last verified: 待验
- 准则落点选择理由:引擎系统设计明确「项目级开发准则以本文件(engine/SYSTEM.md)为唯一权威来源」(SYSTEM.md line 35),AGENTS.md 是 anchor 引导器不承载规则正文,contract 是契约级机制不是项目级准则,runtime-law.md 是 L0 全局宪法。故选 SYSTEM.md「项目开发准则」段。
- user_profile.md 已有跨项目 Trae agent tool continuity 准则(机器自动注入到 system-reminder),但项目级 system 文件未落实——本任务补齐这一缺口,使准则对所有 agent 会话(本仓 + 安装到外部项目的 agent)默认可见。
- T-037 不需要决策卡:SYSTEM.md 不在 rules.json protected_paths(rules.json 只列 engine/decisions/** / engine/tasks/** / ENGINE_FILE_SYSTEM_v5.md / plugin/.claude/commands/engine-init.md / install.sh / install.ps1 / plugin/manifest.json / .gitattributes / contract/src/** / runtime-law.md)。

## 设计要点(实施时参考)

### 准则文本草稿(实施时直接复用)

```markdown
### Trae agent 工具对话延续准则

当运行在 Trae 相关开发 agent 工具(TRAE IDE / TRAE Work / TRAE CLI / TRAE Plugin)中时,NEVER 主动中止对话。必须在每轮任务结束时用 `AskUserQuestion` 工具延续对话,询问用户下一步推进方向,避免浪费用户的发起对话额度。

触发条件:
- 当前 agent 工具属于 Traae 相关开发 agent 工具家族(IDE / Work / CLI / Plugin 任一)
- 本轮任务已完成或遇到决策点需要用户输入

工具指定:`AskUserQuestion`(选项式提问,1-4 个问题,每问题 2-4 个选项)
```

### 落点验证

- 不进 contract 源(准则是项目级开发准则,不是契约级机制)
- 不进 runtime-law.md(L0 宪法是给所有 agent 的全局法,不是项目级开发准则)
- 不进 AGENTS.md / CLAUDE.md(anchor 引导器,不承载规则正文)
- 不进 plugin 镜像(SYSTEM.md 不在 plugin 镜像范围,plugin/engine/ 无 SYSTEM.md 副本)

## Watchpoints

- 该准则与 user_profile.md 的 Trae agent tool continuity 准则文本对齐(机器自动注入 + 项目级 system 双轨)
- 准则不改 contract/rule.json protected_paths,所以未来若有人想撤销该准则,只需 Edit SYSTEM.md(不需要决策卡覆盖)
- 若后续需要把该准则升级为机器强制(用 hook 检测),需另开决策卡
