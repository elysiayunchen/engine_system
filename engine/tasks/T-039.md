# T-039: checkpoint.md append→dedup 修复 + v6.11.2 patch

> status: done | lane: contract | decision: none (bug fix) | plan: none | domain: engine-runtime,project-meta,root
> estimated_steps: 8 | checkpoint_plan: inline (patch 任务,7 个 AC,改动集中)
> depends-on: T-036 (v6.11.0 done,T-036 checkpoint.md 111 行是最严重案例)

GOAL: 修复 `engine-verify.{sh,ps1}` 的 checkpoint.md append-without-dedup bug。当前实现每次 AC verify PASS 都向 `engine/evidence/T-NNN/checkpoint.md` 末尾追加一行,不检查该 AC-N 是否已有 PASS 条目。后果:T-036 checkpoint.md 111 行(18 ACs × 6 轮 verify),T-034 60 行,T-035 51 行——均超出契约 ≤4KB ~100 行预算,SessionStart 注入时逼近 N1=400 行上限。根因:契约源 FILE 15 明确写了"追加写,不覆盖历史行",这是设计错误(不是实现 bug)——verify 是唯一写入者,同 AC-N 重复 PASS 无信息价值,只有时间戳更新有意义。修复:改为 dedup 写(同 AC-N replace 最新时间戳行,新 AC-N append),清理现有膨胀文件,更新契约源,加端到端测试,发版 v6.11.2。
WRITE-SET: contract/src/20-file-templates.md, contract/compile.sh, contract/compile.ps1, ENGINE_FILE_SYSTEM_v5.md, engine/scripts/engine-verify.sh, engine/scripts/engine-verify.ps1, plugin/engine/scripts/engine-verify.sh, plugin/engine/scripts/engine-verify.ps1, engine/skeleton/checkpoint.md, plugin/engine/skeleton/checkpoint.md, engine/evidence/T-034/checkpoint.md, engine/evidence/T-035/checkpoint.md, engine/evidence/T-036/checkpoint.md, engine/evidence/T-037/checkpoint.md, engine/evidence/T-038/checkpoint.md, engine/evidence/T-038/DEAD-CODE.json, engine/tasks/T-038/progress.md, engine/archive/tasks/T-038-progress.md, engine/archive/tasks/T-039-progress.md, tests/workstream/test_checkpoint_dedup.sh, tests/workstream/test_checkpoint_dedup.ps1, VERSION, engine/VERSION, plugin/VERSION, CHANGELOG.md, plugin/manifest.json, engine/tasks/T-039.md, engine/tasks/T-039.spec.md, engine/tasks/T-039/progress.md, engine/changes/CHANGE-2026-07-22-01.md, engine/evidence/T-039/**, engine/domains/engine-runtime/INVENTORY.md, engine/prompts/init.md, plugin/.claude/commands/engine-init.md, plugin/engine/prompts/init.md, engine/CONTEXT.md, engine/HANDOFF.md, engine/ENGINE_MAP.md, AGENTS.md
FORBIDDEN: .git/**, archive/**, .workbuddy/**, engine/migrations/**, contract/src/behaviors/**, engine/scripts/engine-doctor.*, engine/scripts/engine-hook-*, engine/scripts/engine-context.*, engine/scripts/engine-migrate-contract.*, engine/bin/**, engine/ENGINE_DOCTOR.md, engine/AGENT_ADAPTERS.md
AC: AC-1 contract/src/20-file-templates.md FILE 15 改"追加写"→"dedup 写":(1) line ~1656 "末尾追加一行"→"替换同 AC-N 已有行(更新时间戳),无则追加"(2) line ~1681 骨架注释"追加写一行（不覆盖历史）"→"dedup 写一行(同 AC-N 替换,新 AC-N 追加)"(3) line ~1690 维护规则"追加写,不覆盖历史行"→"dedup 写:同 AC-N 替换最新时间戳,新 AC-N 追加";compile.sh 运行后 ENGINE_FILE_SYSTEM_v5.md 同步更新 | verify: grep -q 'dedup' contract/src/20-file-templates.md && ! grep -q 'verify 脚本追加写,不覆盖历史行' contract/src/20-file-templates.md && grep -q 'dedup' ENGINE_FILE_SYSTEM_v5.md
AC: AC-2 engine-verify.sh + .ps1 (4 份) 实现 dedup:verify PASS 前 grep 同 AC-N 行(`^- \[x\] $ac_id `),存在则 grep -v 移除旧行后 append 新行(sh) / Where-Object 过滤后 Set-Content 再 Add-Content(ps1),不存在则直接 append;header 创建逻辑不变 | verify: grep -q 'grep.*-v.*AC\|dedup\|replace.*checkpoint\|filter.*checkpoint' engine/scripts/engine-verify.sh && grep -q 'grep.*-v.*AC\|dedup\|replace.*checkpoint\|filter.*checkpoint' plugin/engine/scripts/engine-verify.sh && grep -q 'Where-Object.*AC\|dedup\|filter.*checkpoint\|notmatch.*AC' engine/scripts/engine-verify.ps1 && grep -q 'Where-Object.*AC\|dedup\|filter.*checkpoint\|notmatch.*AC' plugin/engine/scripts/engine-verify.ps1
AC: AC-3 engine/skeleton/checkpoint.md + plugin 镜像(2 份)注释更新:"追加写"→"dedup 写" | verify: grep -q 'dedup\|去重\|替换' engine/skeleton/checkpoint.md && grep -q 'dedup\|去重\|替换' plugin/engine/skeleton/checkpoint.md && ! grep -q '追加写.*不覆盖历史\|不覆盖历史行' engine/skeleton/checkpoint.md && ! grep -q '追加写.*不覆盖历史\|不覆盖历史行' plugin/engine/skeleton/checkpoint.md
AC: AC-4 现有 checkpoint.md 文件清理:T-034/T-035/T-036/T-037/T-038 各保留每个 AC-N 最新一行(时间戳最大),header 不变;清理后行数 = 该任务 AC 数 + header 行数(约 4 行) | verify: test $(grep -c '^- \[x\] AC-' engine/evidence/T-036/checkpoint.md) -le 18 && test $(grep -c '^- \[x\] AC-' engine/evidence/T-034/checkpoint.md) -le 11 && test $(grep -c '^- \[x\] AC-' engine/evidence/T-035/checkpoint.md) -le 11 && test $(grep -c '^- \[x\] AC-' engine/evidence/T-037/checkpoint.md) -le 1 && test $(grep -c '^- \[x\] AC-' engine/evidence/T-038/checkpoint.md) -le 10
AC: AC-5 端到端测试:tests/workstream/test_checkpoint_dedup.{sh,ps1} 验证(1) 首次 verify PASS 创建 checkpoint.md + 1 行(2) 同 AC 再次 verify PASS 后 checkpoint.md 仍只有 1 行该 AC(3) 不同 AC verify PASS 后追加新行;sh + ps1 双版本均跑过 | verify: test -f tests/workstream/test_checkpoint_dedup.sh && test -f tests/workstream/test_checkpoint_dedup.ps1 && bash tests/workstream/test_checkpoint_dedup.sh && (pwsh -NoProfile -File tests/workstream/test_checkpoint_dedup.ps1 2>/dev/null || powershell.exe -NoProfile -File tests/workstream/test_checkpoint_dedup.ps1)
AC: AC-6 版本三处一致 = 6.11.2 + CHANGELOG 含 v6.11.2 + plugin 镜像 diff -q 4 份 verify 脚本对称 | verify: test "$(tr -d '[:space:]' < VERSION)" = "6.11.2" && test "$(tr -d '[:space:]' < engine/VERSION)" = "6.11.2" && test "$(tr -d '[:space:]' < plugin/VERSION)" = "6.11.2" && grep -q '^## v6.11.2' CHANGELOG.md && diff -q engine/scripts/engine-verify.sh plugin/engine/scripts/engine-verify.sh && diff -q engine/scripts/engine-verify.ps1 plugin/engine/scripts/engine-verify.ps1
AC: AC-7 完整发布门禁全绿 | verify: bash scripts/check.sh
CONSTRAINTS: 不改 Doctor checkpoint 预算检查(≤4KB ~100 行——dedup 后自然不超,Doctor 检查仍有效);不改 migrator managed block(checkpoint.md skeleton 创建逻辑不变,只是 verify 写入语义改变);不改 behaviors 源(task-run.md/handoff.md 不涉及 checkpoint.md 写入);不改 engine-verify.ps1 的 dead code detection 段(只改 checkpoint append 段);Windows PowerShell 5.1 兼容(Where-Object + Set-Content 在 PS 5.1 可用);cleanup 用脚本不手改(可复现);dedup 只对 `^- \[x\] AC-N ` 行生效,header 与其他行不动。 (source: T-036 验收期间发现 checkpoint.md 111 行 bug + HANDOFF.md 立即恢复点标注)
