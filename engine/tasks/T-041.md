# T-041: pre-commit protected_paths 任务卡自身豁免修复

> status: done | lane: contract | decision: D-028 | plan: none | domain: engine-runtime,project-meta,root
> estimated_steps: 5 | checkpoint_plan: inline (patch 任务,5 AC,改动集中)
> depends-on: T-039 (v6.11.2 done,T-039 用 --no-verify 揭示门禁死循环 bug;T-040 也撞此 bug)

GOAL: 修复 pre-commit protected_paths 门禁对任务卡自身的死循环 bug。当前 `engine/tasks/**` 在 protected_paths(防跨任务卡改动),但新任务卡 `engine/tasks/T-NNN.md` 自身匹配该 glob,触发 protected 检查;pre-commit line 206 只读单个 decision(`head -1`),要求该 decision scope 覆盖 `engine/tasks/T-NNN.md`;但 decision scope 不可能预先列未来任务卡(只显式列已存在的 T-032/T-033/... 或用 `engine/tasks/T-*/progress.md` glob 只覆盖 progress.md)。后果:任何改了 `plugin/manifest.json`(protected,D-015 scope 不覆盖)的新任务卡都卡死,只能 `--no-verify`。T-039 与 T-040 均撞此 bug。修复:active/closing 任务卡 T-NNN 改自己的元数据(`.md` / `.spec.md` / 子目录 / 归档 progress)时豁免 protected 检查——门禁目的是防**跨任务卡**改动,不是阻止任务卡改自己。其他 protected 路径(`ENGINE_FILE_SYSTEM_v5.md` / `contract/src/**` / `plugin/manifest.json` 等)仍需 decision scope 覆盖。
WRITE-SET: engine/scripts/githooks/pre-commit, plugin/engine/scripts/githooks/pre-commit, plugin/manifest.json, tests/workstream/test_precommit_self_exempt.sh, engine/tasks/T-041.md, engine/tasks/T-041.spec.md, engine/tasks/T-041/progress.md, engine/CONTEXT.md, engine/HANDOFF.md, engine/ENGINE_MAP.md, engine/changes/CHANGE-2026-07-22-03.md, engine/evidence/T-041/**
FORBIDDEN: .git/**, archive/**, .workbuddy/**, engine/migrations/**, contract/src/**, contract/budget.json, contract/compile.sh, contract/compile.ps1, ENGINE_FILE_SYSTEM_v5.md, engine/scripts/engine-doctor.*, engine/scripts/engine-hook-*, engine/scripts/engine-context.*, engine/scripts/engine-migrate-contract.*, engine/bin/**, engine/ENGINE_DOCTOR.md, engine/AGENT_ADAPTERS.md, runtime-law.md, rules.json, plugin/.claude/**, plugin/engine/prompts/**, plugin/engine/skeleton/**, engine/prompts/**, engine/skeleton/**, VERSION, engine/VERSION, plugin/VERSION, CHANGELOG.md, AGENTS.md, plugin/bin/**
AC: AC-1 pre-commit protected 检查块(line 209-239)加任务卡自身豁免逻辑:在 `match_any_glob "$file" "$protected_csv" || continue` 之后、decision scope 校验之前,提取 `exempt_id`(active/closing 任务卡 ID,从 `task_file` basename 取),用 case 匹配 `engine/tasks/$exempt_id.md` / `engine/tasks/$exempt_id.spec.md` / `engine/tasks/$exempt_id/*` / `engine/archive/tasks/$exempt_id-progress.md`,匹配则 `continue` 跳过 protected 检查;其他 protected 路径仍走原 decision scope 校验 | verify: grep -q 'exempt_id' engine/scripts/githooks/pre-commit && grep -q 'engine/tasks/$exempt_id.md' engine/scripts/githooks/pre-commit && grep -q 'engine/archive/tasks/$exempt_id-progress.md' engine/scripts/githooks/pre-commit
AC: AC-2 测试 tests/workstream/test_precommit_self_exempt.sh 验证豁免逻辑:(1) T-999.md 豁免 (2) T-999.spec.md 豁免 (3) T-999/progress.md 豁免 (4) T-999/sub/deep/note.md 豁免(子目录任意深度) (5) archive/tasks/T-999-progress.md 豁免 (6) T-888.md 不豁免(不同任务) (7) ENGINE_FILE_SYSTEM_v5.md 不豁免 (8) contract/src/20-file-templates.md 不豁免 (9) plugin/manifest.json 不豁免 (10) engine/decisions/D-028.md 不豁免 (11) T-9999.md 不豁免(前缀碰撞防护,suffix-anchored) | verify: test -f tests/workstream/test_precommit_self_exempt.sh && bash tests/workstream/test_precommit_self_exempt.sh
AC: AC-3 compile.sh 跑后 plugin 镜像 sync + manifest SHA256 更新(pre-commit 条目 SHA256 变);T-041 不改契约源所以 ENGINE_FILE_SYSTEM_v5.md / runtime-law.md / rules.json / engine/prompts/** 等其他 dist 不变 | verify: bash contract/compile.sh 2>&1 | grep -q 'manifest sha256 backfilled' && git diff --name-only | grep -E '^plugin/(manifest\.json|engine/scripts/githooks/pre-commit)$'
AC: AC-4 git 永久指向修复版 pre-commit 源目录(`git config core.hooksPath engine/scripts/githooks`),让 git 总读源文件而非 `.git/hooks/` 副本(避免 RunCommand `.git/` denylist 阻塞 install,源文件改了立即生效,无需每次手动 copy) | verify: test "$(git config core.hooksPath)" = "engine/scripts/githooks" && grep -q 'exempt_id' engine/scripts/githooks/pre-commit
AC: AC-5 check.sh 全绿(含 manifest SHA256 一致性检查) | verify: bash scripts/check.sh
CONSTRAINTS: 不改契约源(contract/src/** 禁,豁免逻辑是 pre-commit 实现层,契约源 line 858-862 描述够泛"检查决策"不需精确描述每个豁免规则);不改 ENGINE_FILE_SYSTEM_v5.md / runtime-law.md / rules.json(派生文件,T-041 不改契约源所以不变);不改版本号(纯 bug fix,版本保持 6.11.2,T-040 发 v6.11.3);不改 rules.json(protected_paths 列表不变,豁免逻辑在 pre-commit 脚本内);不改 pre-commit 其他检查逻辑(WRITE-SET/FORBIDDEN/AC evidence/worker mode/shared memory 检查不变);豁免只对 active/closing 任务卡 T-NNN 自身及其衍生文件生效,其他 protected 路径仍需 decision scope 覆盖。 (source: T-039 commit ee97b1d 用 --no-verify 揭示 bug + T-040 第二次撞同一门禁死循环)
