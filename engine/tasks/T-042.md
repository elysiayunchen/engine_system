# T-042: 修复 issue #9 — LF-only engine.ps1 PS 5.1 解析失败

> status: done | lane: engine-runtime | decision: D-030 | plan: none | domain: engine-runtime,root
> estimated_steps: 6 | checkpoint_plan: inline (patch 任务,6 AC,改动集中)
> depends-on: T-041 (pre-commit 自身豁免已落地,T-042 改任务卡自身不被卡)

GOAL: 修复 GitHub issue #9 — `install.ps1` 产出 LF-only `engine.ps1`,PowerShell 5.1 解析 here-string 时行号错位导致 `Unexpected token '}'` 报错,`engine.cmd` shim 在仅有 PS 5.1 的 Windows 系统上完全不可用。方案 A(install.ps1 下载 .ps1 后转 CRLF)+ 方案 B(engine.cmd pwsh 优先)双保险。发版 v6.11.4。

WRITE-SET: install.ps1, engine/bin/engine.cmd, plugin/bin/engine.cmd, plugin/engine/bin/engine.cmd, plugin/manifest.json, VERSION, engine/VERSION, plugin/VERSION, CHANGELOG.md, engine/tasks/T-042.md, engine/tasks/T-042.spec.md, engine/tasks/T-042/progress.md, engine/archive/tasks/T-042-progress.md, engine/CONTEXT.md, engine/HANDOFF.md, engine/ENGINE_MAP.md, engine/handoff-archive-2026-07.md, engine/changes/CHANGE-2026-07-23-01.md, engine/evidence/T-042/**, engine/decisions/D-030.md

FORBIDDEN: .git/**, archive/**, engine/migrations/**, contract/src/**, contract/budget.json, contract/compile.sh, contract/compile.ps1, ENGINE_FILE_SYSTEM_v5.md, runtime-law.md, rules.json, .gitattributes, install.sh, engine/scripts/engine-*.ps1, engine/scripts/engine-*.sh, engine/scripts/engine-hook-*, engine/scripts/githooks/*, engine/scripts/engine-context.*, engine/scripts/engine-migrate-contract.*, engine/scripts/engine-verify.*, engine/scripts/engine-doctor.*, engine/scripts/engine-check-update.*, engine/scripts/engine-sync-agent-anchors.*, engine/bin/engine.ps1, plugin/bin/engine.ps1, plugin/engine/bin/engine.ps1, engine/ENGINE_DOCTOR.md, engine/AGENT_ADAPTERS.md, plugin/.claude/**, plugin/engine/prompts/**, plugin/engine/skeleton/**, engine/prompts/**, engine/skeleton/**, AGENTS.md, plugin/CLAUDE.md, plugin/AGENTS.md

AC: AC-1 install.ps1 加 Convert-ToCrlf 辅助函数 + Download-File 下载 .ps1 后转 CRLF | verify: grep -q 'Convert-ToCrlf' install.ps1 && grep -q 'Download-File' install.ps1
AC: AC-2 install.ps1 Copy-Local(-Local 离线包)复制 .ps1 后转 CRLF | verify: grep -q 'Copy-Local' install.ps1 && grep -q 'Convert-ToCrlf' install.ps1
AC: AC-3 三个 engine.cmd 改 pwsh 优先 + fallback powershell | verify: grep -q 'pwsh' engine/bin/engine.cmd && grep -q 'pwsh' plugin/bin/engine.cmd && grep -q 'pwsh' plugin/engine/bin/engine.cmd
AC: AC-4 plugin 镜像对称:三个 engine.cmd diff -q 一致 | verify: diff -q engine/bin/engine.cmd plugin/bin/engine.cmd && diff -q engine/bin/engine.cmd plugin/engine/bin/engine.cmd
AC: AC-5 版本三处一致 = 6.11.4 + CHANGELOG + manifest SHA256 更新 | verify: test "$(tr -d '[:space:]' < VERSION)" = "6.11.4" && test "$(tr -d '[:space:]' < engine/VERSION)" = "6.11.4" && test "$(tr -d '[:space:]' < plugin/VERSION)" = "6.11.4" && grep -q '^## v6.11.4' CHANGELOG.md
AC: AC-6 check.sh 全绿 | verify: bash scripts/check.sh

CONSTRAINTS: 不改 .gitattributes(仓库保持 LF 策略,D-015);不改 engine.ps1 here-string(install.ps1 自己用 here-string 能跑通,证明非根因);不改 install.sh(bash 安装器不涉及 PS 5.1);不改其他 engine-*.ps1 脚本(只改入口 engine.cmd + install.ps1);checksum 兼容(install.ps1 Get-NormalizedTextSha256 已 strip CRLF→LF);本任务自身狗粮豁免(estimated_steps=6 ≤ 10, checkpoint_plan=inline),progress.md 仅填 §1+§4。 (source: GitHub issue #9 + 字节级分析发现 install.ps1 自己也是 LF-only 但能跑通)
