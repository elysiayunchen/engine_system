# T-043: 修复 P038 — parse_task_patterns 支持 YAML frontmatter 多行 write-set

> status: done | lane: engine-runtime | decision: D-033 | plan: none | domain: engine-runtime,root
> estimated_steps: 6 | checkpoint_plan: inline (patch 任务,6 AC,改动集中)
> depends-on: none (P038 是独立 parser bug,不依赖 P037 决策)

GOAL: 修复 GitHub issue #10 的 P038 — pre-commit `parse_task_patterns`(L35-56)的两个解析分支(inline grep L37-41 + awk markdown L42-55)都不支持 YAML frontmatter 多行 `write-set:` 格式,导致用该格式的任务卡被误报「no readable WRITE-SET」拦截非相关 commit。扩展 awk 分支同时支持 markdown `## field` section + YAML frontmatter `field:` 多行列表 + 修复 case 敏感性 + frontmatter 边界检测。

WRITE-SET: engine/scripts/githooks/pre-commit, plugin/engine/scripts/githooks/pre-commit, tests/workstream/test_precommit_yaml_frontmatter.sh, engine/decisions/D-033.md, engine/tasks/T-043.md, engine/tasks/T-043.spec.md, engine/tasks/T-043/progress.md, engine/archive/tasks/T-043-progress.md, engine/CONTEXT.md, engine/HANDOFF.md, engine/handoff-archive-2026-07.md, engine/ENGINE_MAP.md, engine/changes/CHANGE-2026-07-23-02.md, engine/evidence/T-043/**, engine/domains/engine-runtime/INVENTORY.md, VERSION, engine/VERSION, plugin/VERSION, CHANGELOG.md, plugin/manifest.json

FORBIDDEN: .git/**, archive/**, engine/migrations/**, contract/src/**, contract/budget.json, contract/compile.sh, contract/compile.ps1, ENGINE_FILE_SYSTEM_v5.md, runtime-law.md, rules.json, .gitattributes, install.sh, install.ps1, engine/bin/**, engine/ENGINE_DOCTOR.md, engine/AGENT_ADAPTERS.md, plugin/.claude/**, plugin/engine/prompts/**, plugin/engine/skeleton/**, engine/prompts/**, engine/skeleton/**, AGENTS.md, plugin/CLAUDE.md, plugin/AGENTS.md

AC: AC-1 parse_task_patterns awk 分支扩展:匹配 YAML frontmatter `^field:$` 字段头(大小写不敏感)+ 后续缩进 `- ` 列表项(直到非缩进行或 `---` 闭合),与现有 markdown `## field` section 分支并存 | verify: grep -q 'in_frontmatter' engine/scripts/githooks/pre-commit
AC: AC-2 case 敏感性修复:awk 内 tolower 比较,大写 `WRITE-SET` 调用能匹配小写 `write-set:` frontmatter 字段 | verify: grep -q 'tolower' engine/scripts/githooks/pre-commit
AC: AC-3 frontmatter 边界检测:awk 限定 YAML frontmatter 字段匹配在 `^---$` 之间,避免 markdown body 误匹配 | verify: grep -q 'in_frontmatter_block\|frontmatter_block' engine/scripts/githooks/pre-commit
AC: AC-4 plugin 镜像 pre-commit diff -q 一致 | verify: diff -q engine/scripts/githooks/pre-commit plugin/engine/scripts/githooks/pre-commit
AC: AC-5 测试 tests/workstream/test_precommit_yaml_frontmatter.sh 覆盖:(1) YAML frontmatter 多行 write-set 卡 (2) markdown `## WRITE-SET` section 卡 (3) inline 单行 `WRITE-SET: a, b` 卡 (4) 大小写混合 `write-set:` vs `WRITE-SET` 调用 (5) frontmatter body 含 `field:` 行不误匹配 | verify: test -f tests/workstream/test_precommit_yaml_frontmatter.sh && bash tests/workstream/test_precommit_yaml_frontmatter.sh
AC: AC-6 check.sh 全绿 | verify: bash scripts/check.sh

CONSTRAINTS: 不改 P037 fallback(L89-94,留 T-044/D-032);不改 protected_paths 检查(L193-260);不改 worker mode 检查(L174-181);不改 memory-written 检查(L183-191);不改 done evidence 检查(L141-168);不改 contract/src(契约源不涉及,parse_task_patterns 是 pre-commit 实现层);本任务自身狗粮豁免(estimated_steps=6 ≤ 10, checkpoint_plan=inline),progress.md 仅填 §1+§4。 (source: GitHub issue #10 + reporter awk 补丁方向 + 我补充的 case 敏感性 + frontmatter 边界检测细节)
