# T-045: 修复 CI 红灯 — doctor multi-session isolation 在 CI 环境误报 FAIL

> status: done | lane: engine-runtime | decision: none | plan: none | domain: engine-runtime,root
> estimated_steps: 5 | checkpoint_plan: inline (patch 任务,5 AC,改动集中)
> depends-on: none

GOAL: 修复 GitHub Actions CI 自 v6.11.0 起持续红灯(10+ 次)——doctor 的 `check_multi_session_isolation`(engine-doctor.sh L1279-1287 / .ps1 L1311-1319)在 cv>=6.11.0 时硬 FAIL "`.cache/sessions` dir missing"。但 `.cache/sessions` 是 SessionStart hook 的运行时产物,CI 环境非交互式 agent 会话,SessionStart 永不运行,该目录永不创建。检测 `CI=true` 环境变量(de facto 标准,GitHub/GitLab/CircleCI/Jenkins 都设)时降为 WARN,不 block CI。

WRITE-SET: engine/scripts/engine-doctor.sh, engine/scripts/engine-doctor.ps1, plugin/engine/scripts/engine-doctor.sh, plugin/engine/scripts/engine-doctor.ps1, tests/workstream/test_doctor_ci_sessions.sh, engine/tasks/T-045.md, engine/tasks/T-045/progress.md, engine/archive/tasks/T-045-progress.md, engine/CONTEXT.md, engine/HANDOFF.md, engine/handoff-archive-2026-07.md, engine/ENGINE_MAP.md, engine/changes/CHANGE-2026-07-23-04.md, engine/evidence/T-045/**, engine/domains/engine-runtime/INVENTORY.md, VERSION, engine/VERSION, plugin/VERSION, CHANGELOG.md, plugin/manifest.json

FORBIDDEN: .git/**, archive/**, engine/migrations/**, contract/src/**, contract/budget.json, contract/compile.sh, contract/compile.ps1, ENGINE_FILE_SYSTEM_v5.md, runtime-law.md, rules.json, .gitattributes, install.sh, install.ps1, engine/bin/**, engine/ENGINE_DOCTOR.md, engine/AGENT_ADAPTERS.md, plugin/.claude/**, plugin/engine/prompts/**, plugin/engine/skeleton/**, engine/prompts/**, engine/skeleton/**, AGENTS.md, plugin/CLAUDE.md, plugin/AGENTS.md, engine/scripts/githooks/pre-commit, engine/scripts/engine-migrate-contract.sh, engine/scripts/engine-migrate-contract.ps1

AC: AC-1 engine-doctor.sh `check_multi_session_isolation`:检测 `CI=true`(或 `GITHUB_ACTIONS=true`)时 sessions dir missing 从 FAIL 降为 WARN,输出 human 提示 "CI environment: SessionStart hook not expected to run, sessions dir absence is normal" | verify: grep -q 'CI' engine/scripts/engine-doctor.sh && grep -q 'GITHUB_ACTIONS' engine/scripts/engine-doctor.sh
AC: AC-2 engine-doctor.ps1 `Test-MultiSessionIsolation` 同样改 | verify: grep -q 'CI' engine/scripts/engine-doctor.ps1 && grep -q 'GITHUB_ACTIONS' engine/scripts/engine-doctor.ps1
AC: AC-3 plugin 镜像 doctor sh+ps1 diff 一致 | verify: diff -q engine/scripts/engine-doctor.sh plugin/engine/scripts/engine-doctor.sh && diff -q engine/scripts/engine-doctor.ps1 plugin/engine/scripts/engine-doctor.ps1
AC: AC-4 测试 tests/workstream/test_doctor_ci_sessions.sh 验证 3 场景:(1) CI=true + 无 sessions dir → WARN 不 FAIL (2) 无 CI 变量 + 无 sessions dir → FAIL(行为不变,交互式场景仍硬门禁) (3) CI=true + 有 sessions dir → PASS(不误报) | verify: test -f tests/workstream/test_doctor_ci_sessions.sh && bash tests/workstream/test_doctor_ci_sessions.sh
AC: AC-5 check.sh 全绿 | verify: bash scripts/check.sh

CONSTRAINTS: 不改 ENGINE_DOCTOR.md managed block 描述(CI 降级是实现细节,描述说的是"交互式会话要求 sessions dir",CI 非交互式由脚本逻辑处理);不改 migrator(不改 managed block);不改 pre-commit;不改其他 doctor 检查;不改 contract/src;本任务自身狗粮豁免(estimated_steps=5 ≤ 10, checkpoint_plan=inline),progress.md 仅填 §1+§4。 (source: GitHub Actions CI 失败日志 run 30007319625 + D-029 设计缺陷分析)
