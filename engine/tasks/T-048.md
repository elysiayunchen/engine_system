# T-048: v6.12.0 多任务卡并行(union gating)+ 会话租约液性修复

> status: done | lane: engine-runtime | decision: D-035 | plan: none | domain: engine-runtime,project-meta,root
> estimated_steps: 22 | checkpoint_plan: per-AC
> depends-on: none

GOAL: 根治「激活一张任务卡后其他 agent 被全面拦截」的多会话撞车(D-035 六项根因):三层门禁(PreToolUse/Stop/pre-commit)从单卡假设改为 union gating(路径 ∈ 任一 active 卡 WRITE-SET 且 ∉ 该卡 FORBIDDEN 即放行);任务/决策卡 bootstrap 恒豁免;pre-commit protected 检查逐卡豁免;session.lock 活性从瞬时 pid 改为 heartbeat TTL(默认 120 分钟,ENGINE_SESSION_TTL_MIN 可调);coordinator 写共享单例时写时验锁;.role=worker 旗标全生命周期清理;worker 拦截面收窄为共享单例(subagent 保持旧语义);guard/SessionStart/context 展示层多卡化;contract/src 修订 + migrator v6.12.0 迁移项 + plugin 镜像同步。

## WRITE-SET

- engine/scripts/**
- engine/bin/**
- engine/prompts/**
- plugin/engine/scripts/**
- plugin/engine/prompts/**
- plugin/engine/bin/**
- plugin/bin/**
- plugin/.claude/**
- plugin/manifest.json
- plugin/AGENTS.md
- contract/src/**
- contract/budget.json
- AGENTS.md
- ENGINE_FILE_SYSTEM_v5.md
- tests/**
- scripts/check.sh
- scripts/check.ps1
- engine/tasks/T-048.md
- engine/tasks/T-048/**
- engine/archive/tasks/T-048-progress.md
- engine/evidence/T-048/**
- engine/changes/CHANGE-2026-07-26-01.md
- engine/changes/CHANGE-2026-07-26-02.md
- engine/CONTEXT.md
- engine/HANDOFF.md
- engine/ENGINE_MAP.md
- engine/handoff-archive-2026-07.md
- engine/AGENT_ADAPTERS.md
- engine/GLOSSARY.md
- engine/ENGINE_DOCTOR.md
- engine/SYSTEM.md
- engine/domains/engine-runtime/INVENTORY.md
- engine/domains/engine-runtime/CONTEXT.md
- engine/domains/engine-runtime/PITFALLS.md
- engine/decisions/D-035.md
- VERSION
- engine/VERSION
- plugin/VERSION
- CHANGELOG.md

## FORBIDDEN

- .git/**
- archive/**
- engine/migrations/**
- install.sh
- install.ps1
- runtime-law.md
- .gitattributes
- engine/decisions/rules.json

AC: AC-1 双 active 卡下 pre-commit union:属第二张卡 WRITE-SET 的路径放行,两卡都不覆盖的路径拦截,字典序靠后的卡自身可提交(protected 逐卡豁免) | verify: bash tests/task-card/test_multi_active_union.sh
AC: AC-2 双 active 卡下 PreToolUse union + bootstrap 恒豁免:第二张卡路径放行;已有 active 卡时新建 T-NNN.md/D-NNN.md 不被拦 | verify: bash tests/multi-session/test_pretooluse_union.sh
AC: AC-3 锁液性 heartbeat:lock 未超 TTL 时第二会话降级 worker(pid 已死也不接管);lock+hb 超 TTL 时 stale 接管 | verify: bash tests/multi-session/test_lock_liveness.sh
AC: AC-4 role 旗标生命周期:coordinator 上位删除自身 .role=worker;Stop 收尾删除本会话旗标;7 天孤儿旗标被 SessionStart 清理 | verify: bash tests/multi-session/test_role_cleanup.sh
AC: AC-5 写时验锁:非持锁会话写共享单例被拦(提示 assume-coordinator);lock stale 时原子接管后放行 | verify: bash tests/multi-session/test_shared_write_lease.sh
AC: AC-6 worker 语义收窄:role=worker 顶层会话可写自己卡 WRITE-SET 内的任务局部文件(progress/checkpoint),仍不可写共享单例;agent_id 子代理保持旧一揽子拦截 | verify: bash tests/multi-session/test_worker_scope.sh
AC: AC-7 展示层多卡:guard 列全部 active 卡;SessionStart 注入 ≤3 张全文 | verify: bash tests/multi-session/test_multi_card_display.sh
AC: AC-8 plugin 镜像 byte-identical + 全仓门禁绿 | verify: bash scripts/check.sh
AC: AC-9 contract 编译幂等 + 预算不超(≤2940 行, 13 规则)+ Doctor 无 FAIL | verify: bash contract/compile.sh && bash engine/scripts/engine-doctor.sh
AC: AC-10 migrator 含 v6.12.0 迁移项且 contract-version 升至 6.12.0 | verify: grep -q '6\.12\.0' engine/scripts/engine-migrate-contract.sh && grep -q 'contract-version: 6.12.0' AGENTS.md

CONSTRAINTS: 不撤销 D-029 架构(锁/角色/分片保留,只修液性与语义);subagent(agent_id 非空)拦截语义不变(v6.5 兼容);contract/src 修订净零(改写 v6.11.0 多会话段,不新增规则条目);老项目(contract-version < 6.12.0)fail-open 兼容;所有 .sh 改动同步 .ps1 孪生;.ps1 字符串字面量禁非 ASCII(T-047 教训);root 与 plugin 镜像 byte-identical;新测试脚本 ASCII-safe。 (source: D-035 审视实证 2026-07-26)
