# T-051: v6.12.3 dist-stale pre-commit gate

> status: done | lane: engine-runtime | decision: D-035 | plan: none | domain: engine-runtime,project-meta,root
> estimated_steps: 8 | checkpoint_plan: per-AC
> depends-on: T-050
<!-- exempt: AC-6/7 verify failed due to version drift, see CHANGE-2026-07-30-05 -->

GOAL: 在 pre-commit hook 加 dist-stale 门禁,防止再次发生「编辑契约源/编译产物却没跑 `bash contract/compile.sh`」导致 CI Doctor FAIL(v6.12.2 T-050 发版时实证:ecea136 直接编辑 `ENGINE_FILE_SYSTEM_v5.md`(编译产物)未更新 `contract/src/30-operational.md`(源)也未跑 compile.sh → Doctor `contract dist is not compile(src)` FAIL → CI/Release 双红 → 不得不 re-tag)。门禁逻辑:如果任何 `contract/src/**` 或 6 个 dist 文件(`ENGINE_FILE_SYSTEM_v5.md`、`runtime-law.md`、`rules.json`、`plugin/.claude/commands/engine-init.md`、`engine/prompts/init.md`、`plugin/engine/prompts/init.md`)被 staged,运行 `ENGINE_COMPILE_OUT=/tmp/xxx bash contract/compile.sh` 编译到临时目录,diff 6 个 dist 文件的工作树版本与编译输出;任一不匹配 → FAIL,消息提示 `bash contract/compile.sh`。compile.sh 自身失败 → WARN(fail-open,不拦无关 commit)。无契约文件 staged → 跳过(零开销)。

## WRITE-SET

- engine/scripts/githooks/pre-commit
- plugin/engine/scripts/githooks/pre-commit
- tests/workstream/test_precommit_dist_stale.sh
- engine/tasks/T-051.md
- engine/tasks/T-051/progress.md
- engine/archive/tasks/T-051-progress.md
- engine/changes/CHANGE-2026-07-28-02.md
- engine/evidence/T-051/**
- engine/CONTEXT.md
- engine/HANDOFF.md
- engine/ENGINE_MAP.md
- engine/handoff-archive-2026-07.md
- engine/domains/engine-runtime/INVENTORY.md
- VERSION
- engine/VERSION
- plugin/VERSION
- plugin/manifest.json
- CHANGELOG.md

## FORBIDDEN

- .git/**
- archive/**
- install.sh
- install.ps1
- runtime-law.md
- .gitattributes
- engine/decisions/rules.json
- AGENTS.md
- contract/src/**
- contract/compile.sh
- contract/compile.ps1
- contract/budget.json
- ENGINE_FILE_SYSTEM_v5.md
- engine/ENGINE_DOCTOR.md
- plugin/engine/ENGINE_DOCTOR.md

AC: AC-1 pre-commit 在 staged 含 `contract/src/**` 或 6 个 dist 文件之一时触发 dist-stale 检查;无契约文件 staged 时跳过(零开销) | verify: bash tests/workstream/test_precommit_dist_stale.sh
AC: AC-2 源文件改了但没跑 compile.sh(工作树 dist 过时)→ pre-commit FAIL,消息含 "Contract dist is stale" + "bash contract/compile.sh" | verify: bash tests/workstream/test_precommit_dist_stale.sh
AC: AC-3 dist 文件被直接编辑(不匹配 compile(src))→ pre-commit FAIL | verify: bash tests/workstream/test_precommit_dist_stale.sh
AC: AC-4 源文件改了 + 跑了 compile.sh(工作树 dist == compile(src))→ pre-commit PASS | verify: bash tests/workstream/test_precommit_dist_stale.sh
AC: AC-5 compile.sh 自身失败 → pre-commit WARN 跳过(fail-open,不拦无关 commit) | verify: bash tests/workstream/test_precommit_dist_stale.sh
AC: AC-6 plugin 镜像 byte-identical + 全仓门禁绿 | verify: bash scripts/check.sh
AC: AC-7 版本 6.12.3 + CHANGELOG 段 | verify: grep -q '6\.12\.3' VERSION && grep -q 'v6.12.3' CHANGELOG.md

CONSTRAINTS: 契约预算不超(2940/13);sh only(PreToolUse hook 无 .ps1 孪生,PreToolUse 只跑 sh);plugin 镜像 byte-identical;compile.sh <1s 可接受;fail-open 当 compile.sh 自身失败;不修 compile.sh 本身;不修 Doctor 已有的 `contract dist is not compile(src)` 检查(pre-commit 是前置防线,Doctor 是 CI 兜底)。
