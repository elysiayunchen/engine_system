# T-052: v6.13.0 .engineignore — 项目级任务卡门禁旁路通道

> status: done | lane: engine-runtime | decision: D-036 | plan: none | domain: engine-runtime,project-meta,root
> estimated_steps: 14 | checkpoint_plan: per-AC
> depends-on: T-051

GOAL: 在 pre-commit hook 加 `.engineignore` 机制(gitignore 语法,repo 根),让非产品路径(跨 agent anchor 文件、`engine update` 拉的工具维护文件、项目级配置)免于任务卡 union 门禁,无需为同步建一次性卡或用 `--no-verify`。匹配方式:复用既有 `match_any_glob`(shell `case` 的 `*` 跨 `/`,尾部 `/**` 预处理剥掉使裸目录也匹配),纯 shell 零子进程(issue 提案的 `git check-ignore --exclude-from` 不可行——该选项不存在)。旁路范围仅 179-192(无卡 block)和 194-213(union_allows);**不碰** 289-343(protected-path)和 353-387(dist-stale)。关键修正(issue 提案漏点):`.engineignore` 路径在 194-213 分支**只跳 WRITE-SET 归属,仍受 FORBIDDEN 约束**——新增 `union_not_all_forbidden` 辅助函数(存在任一卡未禁即放行),替代 `union_allows` 对该路径的调用。`.engineignore` 自身加入 `rules.json` protected_paths(改它需覆盖决策)+ doctor 内容告警(发现 `src/**`/`runtime/**`/`contract/**` 等产品路径即 WARN)。`engine-init` 经 skeleton 生成注释模板。issue #17。

## WRITE-SET

- engine/scripts/githooks/pre-commit
- plugin/engine/scripts/githooks/pre-commit
- engine/decisions/rules.json
- engine/decisions/D-036.md
- engine/scripts/engine-doctor.sh
- engine/scripts/engine-doctor.ps1
- plugin/engine/scripts/engine-doctor.sh
- plugin/engine/scripts/engine-doctor.ps1
- engine/skeleton/.engineignore
- plugin/engine/skeleton/.engineignore
- engine/prompts/init.md
- plugin/engine/prompts/init.md
- .claude/commands/engine-init.md
- plugin/.claude/commands/engine-init.md
- tests/workstream/test_precommit_engineignore.sh
- engine/tasks/T-052.md
- engine/tasks/T-052/progress.md
- engine/archive/tasks/T-052-progress.md
- engine/changes/CHANGE-2026-07-29-01.md
- engine/evidence/T-052/**
- engine/CONTEXT.md
- engine/HANDOFF.md
- engine/ENGINE_MAP.md
- engine/handoff-archive-2026-07.md
- engine/domains/engine-runtime/INVENTORY.md
- VERSION
- engine/VERSION
- plugin/VERSION
- plugin/manifest.json
- CHANGELOG.md
- .engineignore

## FORBIDDEN

- .git/**
- archive/**
- install.sh
- install.ps1
- .gitattributes
- engine/decisions/D-*.md
- engine/tasks/T-*.md
- contract/src/**
- contract/compile.sh
- contract/compile.ps1
- contract/budget.json
- ENGINE_FILE_SYSTEM_v5.md
- runtime-law.md
- engine/ENGINE_DOCTOR.md
- plugin/engine/ENGINE_DOCTOR.md
- AGENTS.md

AC: AC-1 `.engineignore` 不存在时 pre-commit 行为不变(无 active 卡 + 普通文件仍 block,有卡 + 非 WRITE-SET 文件仍 block) | verify: bash tests/workstream/test_precommit_engineignore.sh
AC: AC-2 `.engineignore` 列出跨 agent anchor(如 `GEMINI.md`),无 active 卡时提交该文件 → PASS(旁路 179-192) | verify: bash tests/workstream/test_precommit_engineignore.sh
AC: AC-3 `.engineignore` 路径在有 active 卡时跳 WRITE-SET 归属,但路径在所有 active 卡 FORBIDDEN 中时仍 block(跳 WRITE-SET 不跳 FORBIDDEN,union_not_all_forbidden 生效) | verify: bash tests/workstream/test_precommit_engineignore.sh
AC: AC-4 `.engineignore` 不绕过 protected-path 检查:staged `.engineignore` 自身受 protected 约束,需覆盖决策 D-036 approved 且 scope 含 `.engineignore` | verify: bash tests/workstream/test_precommit_engineignore.sh
AC: AC-5 `.engineignore` 不绕过 dist-stale 契约门禁:staged `contract/src/**` 即使同时有 `.engineignore` 匹配,仍触发 compile 校验 | verify: bash tests/workstream/test_precommit_engineignore.sh
AC: AC-6 doctor 发现 `.engineignore` 含 `src/**` 或 `runtime/**` 或 `contract/**` 时输出 WARN(产品路径滥用告警) | verify: bash tests/workstream/test_precommit_engineignore.sh
AC: AC-7 skeleton 模板存在且镜像一致,含跨 agent anchor 注释样例 | verify: diff engine/skeleton/.engineignore plugin/engine/skeleton/.engineignore && grep -q 'copilot-instructions' engine/skeleton/.engineignore
AC: AC-8 plugin 镜像 byte-identical + 全仓门禁绿 | verify: bash scripts/check.sh
AC: AC-9 版本 6.13.0 + CHANGELOG 段 | verify: grep -q '6\.13\.0' VERSION && grep -q 'v6.13.0' CHANGELOG.md

CONSTRAINTS: 契约预算不超;sh only(pre-commit 无 .ps1 孪生,走 Git Bash);plugin 镜像 byte-identical;`git check-ignore` 批量一次调用(用 `--stdin`,禁每文件 fork,参见 pre-commit L88-90 性能注释);`--no-index` 不读 `.gitignore`/`.git/info/exclude`,只读 `--exclude-from`(避免与真实 gitignore 混淆);不修 compile.sh;不修 Doctor 已有的 `contract dist is not compile(src)` 检查;D-036 须与 `.engineignore` 同提交创建(决策卡 bootstrap 豁免,解鸡生蛋);`.engineignore` 不豁免产品路径(`runtime/**`/`src/**`/`contract/**`),写了 doctor WARN(source: issue #17 + 本卡 D3 决议)。
