# T-053: v6.13.1 engine-verify.ps1 预防性修复 — Remove-Item Env: 改 .NET 原生方法

> status: done | lane: engine-runtime | decision: none | plan: none | domain: engine-runtime
> estimated_steps: 6 | checkpoint_plan: per-AC
> depends-on: T-052

GOAL: 预防性修复 `engine-verify.ps1` 行 107 的 `Remove-Item Env:ENGINE_VERIFY_RECURSE_GUARD -ErrorAction SilentlyContinue`。T-051 CHANGE 行 47 记录:TRAE IDE 的 `safe_rm_alias.ps1` 包装 `Remove-Item`,不识别 `Env:` drive prefix,在 `$ErrorActionPreference = "Stop"` 下抛 terminating error 被 trap 捕获 → exit 1 → 任何 task verify 都报错。当前 TRAE 环境 bug 不复现(疑似 safe_rm_alias.ps1 已修),但作防御性编程改用 .NET 原生方法 `[Environment]::SetEnvironmentVariable('ENGINE_VERIFY_RECURSE_GUARD', $null, 'Process')`,完全绕过 PowerShell provider 机制,不被任何 alias 包装影响,在 PS 5.1/7+ 行为一致。

## WRITE-SET

- engine/scripts/engine-verify.ps1
- plugin/engine/scripts/engine-verify.ps1
- tests/workstream/test_engine_verify_env_cleanup.ps1
- tests/workstream/test_engine_verify_env_cleanup.sh
- engine/tasks/T-053.md
- engine/archive/tasks/T-053-progress.md
- engine/changes/CHANGE-2026-07-29-02.md
- engine/evidence/T-053/**
- engine/CONTEXT.md
- engine/HANDOFF.md
- engine/ENGINE_MAP.md
- engine/domains/engine-runtime/INVENTORY.md
- VERSION
- engine/VERSION
- plugin/VERSION
- plugin/manifest.json
- CHANGELOG.md

## FORBIDDEN

- .git/**
- archive/**
- install.sh
- install.ps1
- .gitattributes
- engine/decisions/D-*.md
- engine/tasks/T-*.md
- contract/src/**
- contract/compile.sh
- contract/compile.ps1
- contract/budget.json
- ENGINE_FILE_SYSTEM_v5.md
- runtime-law.md
- engine/ENGINE_DOCTOR.md
- plugin/engine/ENGINE_DOCTOR.md
- AGENTS.md
- engine/scripts/githooks/pre-commit
- plugin/engine/scripts/githooks/pre-commit
- .engineignore

AC: AC-1 engine-verify.ps1 行 107 使用 [Environment]::SetEnvironmentVariable .NET 原生方法替代 Remove-Item Env: | verify: grep -q "\[Environment\]::SetEnvironmentVariable" engine/scripts/engine-verify.ps1
AC: AC-2 修复后 engine-verify.ps1 无 trap 退出(env cleanup 测试 AC-4 跑 engine-verify.ps1 -Task T-052 递归守卫 exit 0 验证) | verify: pwsh -NoProfile -File tests/workstream/test_engine_verify_env_cleanup.ps1
AC: AC-3 环境变量 ENGINE_VERIFY_RECURSE_GUARD 在 verify 完成后被正确清除(不残留) | verify: pwsh -NoProfile -File tests/workstream/test_engine_verify_env_cleanup.ps1
AC: AC-4 递归守卫仍工作(same task 递归调用 exit 0) | verify: pwsh -NoProfile -File tests/workstream/test_engine_verify_env_cleanup.ps1
AC: AC-5 plugin 镜像 byte-identical | verify: diff engine/scripts/engine-verify.ps1 plugin/engine/scripts/engine-verify.ps1
AC: AC-6 scripts/check.sh CHECK PASSED | verify: bash scripts/check.sh
AC: AC-7 版本 6.13.1 + CHANGELOG 段 | verify: grep -q '6\.13\.1' VERSION && grep -q 'v6.13.1' CHANGELOG.md

CONSTRAINTS: 单行修复 ×2 文件(engine + plugin 镜像);不修 sh 版;不修 trap 逻辑;不修 recursion guard 逻辑(只改清除方式);`[Environment]::SetEnvironmentVariable` 第 3 参数必须为 'Process'(与原 `Env:` provider 语义一致,只清当前进程);不引入新依赖;.ps1 测试在无 PowerShell 环境可 SKIP(与既有 parity 测试一致)。
