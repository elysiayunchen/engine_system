# T-066: v6.18.0 防漂移 P1 — 证据多锚 + drift-check(D-038a/b)

> status: done | lane: engine-runtime | decision: D-038 | plan: none | domain: engine-runtime
> estimated_steps: 12 | checkpoint_plan: per-AC
> depends-on: D-038

GOAL: 实施 D-038a/b 两层防漂移——evidence schema 升级为多锚(output_fingerprint + code_fingerprint + write_set_snapshot + verified_against_commit + evidence_manifest_sha256 + write_provenance)+ 新增 engine drift-check 三步顺序校验(完整性自证 → WRITE-SET 二阶检测 → 代码指纹比对)+ evidence/drift-check 脚本纳入 protected_paths + pre-commit provenance 校验。堵 6 类漂移(时间/锚点/状态/evidence篡改/WRITE-SET二阶/EOL假DRIFT)。

## WRITE-SET

- engine/decisions/rules.json
- engine/scripts/engine-verify.sh
- engine/scripts/engine-verify.ps1
- engine/scripts/engine-drift-check.sh
- engine/scripts/engine-drift-check.ps1
- engine/scripts/engine-doctor.sh
- engine/scripts/engine-doctor.ps1
- engine/scripts/githooks/pre-commit
- plugin/engine/scripts/engine-verify.sh
- plugin/engine/scripts/engine-verify.ps1
- plugin/engine/scripts/engine-drift-check.sh
- plugin/engine/scripts/engine-drift-check.ps1
- plugin/engine/scripts/engine-doctor.sh
- plugin/engine/scripts/engine-doctor.ps1
- plugin/engine/scripts/githooks/pre-commit
- tests/workstream/test_drift_check.sh
- tests/workstream/test_evidence_provenance.sh
- tests/behavior-verify/run-verify-tests.sh
- tests/task-card/run-task-tests.sh
- engine/tasks/T-066.md
- engine/tasks/T-066/progress.md
- engine/archive/tasks/T-066-progress.md
- engine/changes/CHANGE-2026-07-30-02.md
- engine/changes/CHANGE-2026-07-30-03.md
- engine/evidence/T-066/**
- engine/CONTEXT.md
- engine/HANDOFF.md
- engine/handoff-archive-2026-07.md
- engine/ENGINE_MAP.md
- engine/domains/engine-runtime/INVENTORY.md
- VERSION
- engine/VERSION
- plugin/VERSION
- plugin/manifest.json
- CHANGELOG.md

## FORBIDDEN

- .git/**
- archive/**
- install.sh
- install.ps1
- .gitattributes
- engine/decisions/D-*.md
- engine/tasks/T-*.md
- contract/src/**
- contract/compile.sh
- contract/compile.ps1
- contract/budget.json
- ENGINE_FILE_SYSTEM_v5.md
- runtime-law.md
- engine/ENGINE_DOCTOR.md
- plugin/engine/ENGINE_DOCTOR.md
- AGENTS.md
- engine/SYSTEM.md
- .engineignore

AC: AC-1 rules.json 加 protected_paths(engine/evidence/** + engine/scripts/engine-drift-check.*) | verify: bash -c "grep -q 'engine/evidence' engine/decisions/rules.json && grep -q 'engine-drift-check' engine/decisions/rules.json"
AC: AC-2 engine-verify.sh 用 git ls-files -s 算 code_fingerprint + git add 前置检查 | verify: bash -c "grep -q 'git ls-files -s' engine/scripts/engine-verify.sh && grep -q 'git add' engine/scripts/engine-verify.sh"
AC: AC-3 engine-verify.sh 写 write_set_snapshot + verified_against_commit + write_provenance + MANIFEST.json(聚合排除自身) | verify: bash -c "grep -q 'write_set_snapshot' engine/scripts/engine-verify.sh && grep -q 'write_provenance' engine/scripts/engine-verify.sh && grep -q 'MANIFEST' engine/scripts/engine-verify.sh && grep -q 'verified_against_commit' engine/scripts/engine-verify.sh"
AC: AC-4 engine-verify.ps1 孪生(语义一致) | verify: bash -c "grep -q 'ls-files -s' engine/scripts/engine-verify.ps1 && grep -q 'write_provenance' engine/scripts/engine-verify.ps1 && grep -q 'MANIFEST' engine/scripts/engine-verify.ps1"
AC: AC-5 engine-drift-check.sh 新建(三步顺序:manifest自证→WRITE-SET二阶→代码指纹,一步失败仍输出后续摘要) | verify: bash -c "test -f engine/scripts/engine-drift-check.sh && grep -q 'manifest' engine/scripts/engine-drift-check.sh && grep -q 'write_set_snapshot' engine/scripts/engine-drift-check.sh && grep -q 'git ls-files -s' engine/scripts/engine-drift-check.sh"
AC: AC-6 engine-drift-check.ps1 孪生 | verify: bash -c "test -f engine/scripts/engine-drift-check.ps1 && grep -q 'manifest' engine/scripts/engine-drift-check.ps1"
AC: AC-7 pre-commit 加 evidence provenance 校验(writer=engine-verify + commit=HEAD + argv模式;手动Edit需evidence-manual-edit标注+covering decision) | verify: bash -c "grep -q 'write_provenance' engine/scripts/githooks/pre-commit && grep -q 'engine-verify' engine/scripts/githooks/pre-commit && grep -q 'evidence-manual-edit' engine/scripts/githooks/pre-commit"
AC: AC-8 engine-doctor.sh/ps1 集成 drift-check 调用 | verify: bash -c "grep -q 'drift-check' engine/scripts/engine-doctor.sh && grep -q 'drift-check' engine/scripts/engine-doctor.ps1"
AC: AC-9 plugin 镜像 byte-identical(verify/drift-check/pre-commit/doctor 四脚本) | verify: bash -c "diff engine/scripts/engine-verify.sh plugin/engine/scripts/engine-verify.sh && diff engine/scripts/engine-verify.ps1 plugin/engine/scripts/engine-verify.ps1 && diff engine/scripts/engine-drift-check.sh plugin/engine/scripts/engine-drift-check.sh && diff engine/scripts/engine-drift-check.ps1 plugin/engine/scripts/engine-drift-check.ps1 && diff engine/scripts/githooks/pre-commit plugin/engine/scripts/githooks/pre-commit && diff engine/scripts/engine-doctor.sh plugin/engine/scripts/engine-doctor.sh && diff engine/scripts/engine-doctor.ps1 plugin/engine/scripts/engine-doctor.ps1 && echo PASS"
AC: AC-10 测试套件(drift-check 三步 + provenance 校验) | verify: bash tests/workstream/test_drift_check.sh
AC: AC-11 scripts/check.sh CHECK PASSED | verify: bash scripts/check.sh
AC: AC-12 版本 6.18.0 + CHANGELOG 段 | verify: bash -c "grep -q '6\.18\.0' VERSION && grep -q 'v6.18.0' CHANGELOG.md"

CONSTRAINTS: 孪生 sh/ps1 语义一致(D-038 强制);哈希用 `git ls-files -s <path>` 取 index blob sha(已 EOL 归一化),不用 `git hash-object`/`sha256sum`;前置 WRITE-SET 文件须 `git add` 进 index(未 add 则 FAIL 提示);MANIFEST.json 聚合范围含 AC-*.json+DEAD-CODE.json+COPY-PASTE.json+checkpoint.md,排除 MANIFEST.json 自身,按文件名字典序排序;write_provenance.commit 必须=当前 HEAD;drift-check 撤出 pre-commit(只在 doctor/手动/SessionStart 三处触发);三步顺序任一步 FAIL 仍输出后续摘要(标 unverified);文件已删除时 git ls-files -s 返回空→DRIFT 报告;plugin 镜像 byte-identical。
