# T-075: engine prove Phase B — 强化 + 集成 + 测试补全

status: active
lane: main
domain: engine
decision: D-019

## GOAL

将 prove 子系统从"可用骨架"提升到"生产可靠"：补齐审计发现的安全/健壮性缺陷，集成 doctor 健康检查，消除与 review-agent 的代码重复，补全测试覆盖。

## WRITE-SET

- engine/scripts/engine-prove.sh
- engine/scripts/engine-prove.ps1
- engine/scripts/engine-verify.sh
- engine/scripts/engine-doctor.sh
- engine/prove/config.json
- engine/prove/prove-assertions.schema.json
- plugin/engine/scripts/engine-prove.sh
- plugin/engine/scripts/engine-prove.ps1
- tests/workstream/test_prove_infer.sh
- tests/workstream/test_prove_execute.sh

## FORBIDDEN

- contract/src/*
- engine/scripts/engine-review-agent-package.sh
- engine/scripts/engine-review-agent-validate.sh

## AC

AC-1: doctor prove 健康检查 — `engine doctor` 输出含 prove 段（脚本存在 + config 合法 + schema 合法）
  verify: bash -c "cd /tmp && bash $CLAUDE_PROJECT_DIR/engine/scripts/engine-doctor.sh 2>&1 | grep -qi prove"

AC-2: 共享 diff 提取库 — prove 和 review-agent-package 共用同一 diff 提取函数，无重复实现
  verify: bash -c "grep -c 'extract_diff_context' $CLAUDE_PROJECT_DIR/engine/scripts/engine-prove.sh | grep -q '[1-9]'"

AC-3: fingerprint 覆盖新文件 — code_fingerprint 计算包含 WRITE-SET 中新增文件（不仅在 diff range 内的）
  verify: bash tests/workstream/test_prove_infer.sh 2>&1 | grep -q '0 failed'

AC-4: prove 并发锁 — execute 阶段持锁运行，第二个并发 execute 被拒绝（exit 1 + 提示）
  verify: bash -c "grep -q 'lock' $CLAUDE_PROJECT_DIR/engine/scripts/engine-prove.sh"

AC-5: 断言质量评分 — 全部为 syntax 类时输出 WARN（不阻断，但提示覆盖不足）
  verify: bash -c "grep -qi 'syntax-only' $CLAUDE_PROJECT_DIR/engine/scripts/engine-prove.sh"

AC-6: 测试补全 — prove-infer ≥19 assertions, prove-execute ≥23 assertions（含审计 #17/#18 场景）
  verify: bash -c "bash $CLAUDE_PROJECT_DIR/tests/workstream/test_prove_infer.sh 2>&1 | tail -1 | grep -qE '(19|2[0-9]) passed'"

AC-7: verify eval timeout — engine-verify.sh 的 AC verify 命令有 timeout 包裹（默认 120s）
  verify: bash -c "grep -q 'timeout' $CLAUDE_PROJECT_DIR/engine/scripts/engine-verify.sh"

AC-8: 全量回归绿 — prove-infer + prove-execute + review-validate + grounded 四套件 0 failed
  verify: bash -c "bash $CLAUDE_PROJECT_DIR/tests/workstream/test_prove_execute.sh 2>&1 | grep -q '0 failed'"

## CONSTRAINTS

- 不改 contract（budget 满），以 convention 层落地
- ps1 镜像行为对齐（非 byte-identical）
- 不引入 python 依赖到 engine-verify.sh（保持 grep/sed）
- 审计 #2 (allowlist) 和 #12 (independence honor-system) 属架构变更，本卡只做调研记录不做实施
- 审计 #13 (CRLF cross-env) 仅记录为 known limitation，不阻塞
