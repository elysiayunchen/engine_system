# T-077: Gatekeeper 子系统 — 质量门禁 done 过渡硬拦截 + --no-verify 封死

status: active
lane: main
domain: engine
decision: D-019

## GOAL

将所有质量门禁（verify/review/review-agent/prove）从 Doctor 建议性升级为 pre-commit 硬拦截（仅 done 过渡时），使零经验开发者无需判断"该不该跑检查"。同时封死 --no-verify 逃生舱（默认封死，config 可按项目解锁）。

## WRITE-SET

- engine/scripts/engine-gate.sh
- engine/scripts/engine-gate.ps1
- engine/gate/config.json
- engine/bin/engine
- engine/bin/engine.ps1
- engine/scripts/githooks/pre-commit
- engine/scripts/engine-doctor.sh
- engine/scripts/engine-doctor.ps1
- plugin/engine/scripts/engine-gate.sh
- plugin/engine/scripts/engine-gate.ps1
- plugin/engine/gate/config.json
- plugin/engine/scripts/githooks/pre-commit
- plugin/engine/scripts/engine-doctor.sh
- plugin/engine/scripts/engine-doctor.ps1
- plugin/manifest.json
- engine/decisions/rules.json
- engine/domains/federation.json
- install.sh
- install.ps1
- contract/src/30-operational.md
- contract/budget.json
- engine/decisions/D-039.md
- tests/workstream/test_gate_cli.sh
- tests/workstream/test_gate_precommit.sh
- tests/workstream/test_gate_seal.sh
- engine/tasks/T-077.md
- engine/CONTEXT.md
- engine/HANDOFF.md
- engine/ENGINE_MAP.md
- CHANGELOG.md
- VERSION
- engine/VERSION
- plugin/VERSION

## FORBIDDEN

- engine/scripts/engine-verify.sh
- engine/scripts/engine-prove.sh
- engine/scripts/engine-review-pipeline.sh
- engine/scripts/engine-review-agent-package.sh
- engine/scripts/engine-review-agent-validate.sh
- runtime-law.md

## AC

AC-1: done 过渡无 GATE.json → commit 被 block，stderr 含 "engine gate"
  verify: bash tests/workstream/test_gate_precommit.sh 2>&1 | grep -q '0 failed'

AC-2: GATE.json status=block → commit 被 block，stderr 逐条列出失败门禁 + fix 命令
  verify: bash tests/workstream/test_gate_precommit.sh 2>&1 | grep -q '0 failed'

AC-3: GATE.json status=pass + provenance 有效 → commit 成功
  verify: bash tests/workstream/test_gate_precommit.sh 2>&1 | grep -q '0 failed'

AC-4: --no-verify 绕过 → 下次 session 检测 → 后续 commit 被 block
  verify: bash tests/workstream/test_gate_seal.sh 2>&1 | grep -q '0 failed'

AC-5: override_authored=true → --no-verify 不触发封死
  verify: bash tests/workstream/test_gate_seal.sh 2>&1 | grep -q '0 failed'

AC-6: docs-only 卡 → review/prove skipped，仅 verify 强制
  verify: bash tests/workstream/test_gate_cli.sh 2>&1 | grep -q '0 failed'

AC-7: 工具缺失 → skipped 不 block
  verify: bash tests/workstream/test_gate_cli.sh 2>&1 | grep -q '0 failed'

AC-8: exempt 卡 → 跳过 GATE.json 检查
  verify: bash tests/workstream/test_gate_precommit.sh 2>&1 | grep -q '0 failed'

AC-9: ps1 行为镜像对等
  verify: bash -c "diff <(grep -c 'function\|param\|if\|else\|exit' engine/scripts/engine-gate.ps1) /dev/null && echo pass"

AC-10: Doctor cv>=6.24.0 done 卡无 GATE.json → FAIL
  verify: bash -c "grep -q 'check_gate_registry' engine/scripts/engine-doctor.sh"

## CONSTRAINTS

- D-019: 引擎只做文件层聚合+校验，不调 LLM API
- 契约预算需决策卡提升基线（当前 2936/2940）
- pre-commit 新增 ≤50 行
- ps1 行为镜像（非 byte-identical）
- plugin 镜像 byte-identical（config + scripts）
- 错误消息格式：[Engine System] 前缀 + 一句话 + Fix 命令
