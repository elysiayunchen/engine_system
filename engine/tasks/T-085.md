# T-085: TDAI P1 集成 — 任务漂移检测 + 变更胶囊 heat 演进

status: done
lane: main
domain: engine-runtime
decision: D-019

## GOAL

P1-A: 在 Guard 模式增加 4 信号漂移评分（纯 shell <50ms），当 agent 的 prompt 偏离 active 任务时输出 DRIFT ADVISORY/hint（仅建议不阻断）。P1-B: 为变更胶囊增加 META header（heat/related-decisions/related-tasks/domain）+ Evolution section，Doctor 新增 check_capsule_heat 警告高频变更区域。

## WRITE-SET

- engine/scripts/engine-hook-session-start.sh
- engine/scripts/engine-hook-session-start.ps1
- engine/scripts/engine-hook-stop.sh
- engine/scripts/engine-hook-stop.ps1
- engine/scripts/engine-doctor.sh
- engine/scripts/engine-doctor.ps1
- engine/changes/CHANGE-2026-08-01-02.md
- plugin/engine/scripts/engine-hook-session-start.sh
- plugin/engine/scripts/engine-hook-session-start.ps1
- plugin/engine/scripts/engine-hook-stop.sh
- plugin/engine/scripts/engine-hook-stop.ps1
- plugin/engine/scripts/engine-doctor.sh
- plugin/engine/scripts/engine-doctor.ps1
- tests/workstream/test_drift_detect.sh
- tests/workstream/test_capsule_heat.sh
- engine/tasks/T-085.md
- engine/CONTEXT.md
- engine/HANDOFF.md
- engine/ENGINE_MAP.md
- CHANGELOG.md
- VERSION
- engine/VERSION
- plugin/VERSION

## FORBIDDEN

- engine/scripts/engine-verify.sh
- engine/scripts/engine-gate.sh
- engine/scripts/engine-prove.sh
- engine/scripts/engine-canvas.sh
- engine/scripts/githooks/pre-commit
- contract/src/00-core.md
- runtime-law.md

## AC

AC: AC-1 guard 检测 prompt 提到非 active T-NNN 时 score+3 并输出 DRIFT ADVISORY | verify: bash tests/workstream/test_drift_detect.sh 2>&1 | grep -q '0 failed'
AC: AC-2 guard 检测 prompt 与 GOAL 零关键词重叠时 score+2 | verify: bash tests/workstream/test_drift_detect.sh 2>&1 | grep -q '0 failed'
AC: AC-3 score 0 时静默(无 drift 输出) | verify: bash tests/workstream/test_drift_detect.sh 2>&1 | grep -q '0 failed'
AC: AC-4 胶囊 META header 格式正确(heat/created/updated/domain) | verify: bash tests/workstream/test_capsule_heat.sh 2>&1 | grep -q '0 failed'
AC: AC-5 Doctor check_capsule_heat: heat>=5 WARN + heat>=3无决策 WARN | verify: bash tests/workstream/test_capsule_heat.sh 2>&1 | grep -q '0 failed'
AC: AC-6 fail-open: drift/capsule 检测失败不阻断 guard/doctor 主流程 | verify: bash tests/workstream/test_drift_detect.sh 2>&1 | grep -q '0 failed' && bash tests/workstream/test_capsule_heat.sh 2>&1 | grep -q '0 failed'
AC: AC-7 PS1 孪生行为对等 | verify: pwsh -NoProfile -Command "& { $null = [System.Management.Automation.PSParser]::Tokenize((Get-Content 'engine/scripts/engine-doctor.ps1' -Raw), [ref]$null); exit 0 }"
AC: AC-8 既有测试不回归(gate 25 + canvas 18 + failure 12) | verify: bash tests/workstream/test_gate_cli.sh 2>&1 | grep -q '0 failed' && bash tests/workstream/test_canvas.sh 2>&1 | grep -q '0 failed'

## CONSTRAINTS

- 零 LLM：drift 纯 grep/bag-of-words，capsule 纯文件解析
- 零阻断：drift 只输出建议，不改变 guard exit code
- 零新依赖：纯 bash/sed/grep/awk
- fail-open：所有新增逻辑 || true
- 实现规格：docs/tdai-engine-deep-implementation-spec.md Part III §3.3-§3.4
- PS1 孪生：行为镜像
- plugin 镜像：byte-identical
