# T-090: 修复 review pipeline 分发哈希

> status: active | lane: main | decision: D-019 | plan: none | domain: project-meta,engine-runtime
> Created: 2026-08-02 | issue: CI Install dry-run checksum failure

## GOAL

同步 `engine-review-pipeline.ps1` 的实际文件哈希到 `plugin/manifest.json`，使本地安装器的硬 checksum 校验与 root/plugin 分发内容一致。

## WRITE-SET

- plugin/manifest.json
- engine/tasks/T-090.md
- engine/tasks/T-090/progress.md
- engine/workstreams/T-090/agents/a-codex/**

## FORBIDDEN

- .git/**
- contract/**
- engine/CONTEXT.md
- engine/HANDOFF.md
- engine/ENGINE_MAP.md
- engine/tasks/T-088.md
- engine/tasks/T-089.md
- engine/evidence/**
- engine/scripts/**
- plugin/engine/scripts/**

## AC

AC: AC-1 | manifest hash for engine-review-pipeline.ps1 equals the normalized source hash | verify: python3 -c "import hashlib,json; d=json.load(open('plugin/manifest.json')); e=next(x for x in d['files'] if x['src']=='engine/scripts/engine-review-pipeline.ps1'); b=open('engine/scripts/engine-review-pipeline.ps1','rb').read().replace(b'\\r\\n',b'\\n'); assert e['sha256']==hashlib.sha256(b).hexdigest()"
AC: AC-2 | the clean local installer passes checksum verification | verify: bash install.sh --local plugin
AC: AC-3 | all existing manifest entries still point to present plugin files | verify: python3 -c "import json,os; d=json.load(open('plugin/manifest.json')); assert all(os.path.isfile(os.path.join('plugin',x['src'])) for x in d['files'])"

## CONSTRAINTS

- Keep this hotfix limited to the manifest hash; preserve unrelated dirty and untracked worktree changes.
- Do not alter version files; v6.27.0 is already published by T-088.
