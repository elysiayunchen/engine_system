# T-088 worker context

- Scope: CI checkout provenance, installer/manifest parity, task-card fixture, and SessionStart line budget.
- Coordinator-owned singleton files were not edited; this shard records the worker write-back.
- Existing progress and INVENTORY changes were included only where they directly remove current Doctor CI failures.
