# T-090 worker context

- Scope is limited to the stale `plugin/manifest.json` hash for `engine-review-pipeline.ps1`.
- Preserve unrelated dirty and untracked worktree changes.
- Local verification is green: manifest hash/coverage and clean install passed.
